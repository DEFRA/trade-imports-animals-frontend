{% extends "standalone/live-animals/shared/layout.njk" %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% block journeyContent %}
  {% include "standalone/live-animals/shared/error-summary.njk" %}

  <form method="post" novalidate>
    <input type="hidden" name="crumb" value="{{ crumb }}" />

    {{ govukRadios({
      name: "commercialTransporter",
      fieldset: {
        legend: {
          text: "Search for an approved commercial transporter",
          isPageHeading: true,
          classes: "govuk-fieldset__legend--l"
        }
      },
      hint: { text: "Selecting a transporter copies their name, address and approval number into this notification." },
      errorMessage: errors.commercialTransporter and { text: errors.commercialTransporter },
      items: transporterOptions
    }) }}

    {{ govukButton({ text: "Save and continue" }) }}
  </form>
{% endblock %}
