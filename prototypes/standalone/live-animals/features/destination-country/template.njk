{% extends "standalone/live-animals/shared/layout.njk" %}
{% from "govuk/components/select/macro.njk" import govukSelect %}
{% from "standalone/live-animals/shared/save-actions.njk" import saveActions %}

{% block journeyContent %}
  {% include "standalone/live-animals/shared/error-summary.njk" %}

  <h1 class="govuk-heading-l">{{ heading }}</h1>

  <form method="post" novalidate>
    <input type="hidden" name="crumb" value="{{ crumb }}" />

    {{ govukSelect({
      id: "destinationCountry",
      name: "destinationCountry",
      label: { text: "Destination country", classes: "govuk-label--s" },
      hint: { text: "The country the consignment travels on to after Great Britain." },
      value: values.destinationCountry,
      errorMessage: errors.destinationCountry and { text: errors.destinationCountry },
      items: countryItems
    }) }}

    {{ saveActions(hubHref) }}
  </form>
{% endblock %}
