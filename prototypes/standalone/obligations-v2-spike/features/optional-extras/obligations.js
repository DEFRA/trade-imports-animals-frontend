/**
 * Optional extras — the obligations this feature owns. Pure data; imports
 * nothing outward. A multi-select; optional (saves empty).
 */
export const extras = { id: 'extras' }

export const obligations = [extras]
