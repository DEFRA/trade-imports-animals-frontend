{# Shared GDS error summary — the only user-visible face of a mandate.
   Context: errorSummary = { titleText, errorList:[{ text, href }] } or null. #}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}

{% if errorSummary %}
  {{ govukErrorSummary(errorSummary) }}
{% endif %}
