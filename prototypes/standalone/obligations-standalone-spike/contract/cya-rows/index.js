/**
 * Barrel for the CYA composition folder-module: per-type value
 * formatting (format-value.js), per-page row builders (page-rows.js)
 * and the Flow-order assembly with change links and soft prompts
 * (assemble.js).
 */

export { cyaRows } from './assemble.js'
