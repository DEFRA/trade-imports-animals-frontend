{% extends "standalone/live-animals/shared/layout.njk" %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% block journeyContent %}
  {% include "standalone/live-animals/shared/error-summary.njk" %}

  <form method="post" novalidate>
    <input type="hidden" name="crumb" value="{{ crumb }}" />

    {{ govukInput({
      id: "countyParishHoldingCph",
      name: "countyParishHoldingCph",
      label: {
        text: "County Parish Holding (CPH)",
        isPageHeading: true,
        classes: "govuk-label--l"
      },
      hint: { text: "For example, 123456789 or 123/456/789." },
      classes: "govuk-input--width-10",
      value: values.countyParishHoldingCph,
      errorMessage: errors.countyParishHoldingCph and { text: errors.countyParishHoldingCph }
    }) }}

    {{ govukButton({ text: "Save and continue" }) }}
  </form>
{% endblock %}
