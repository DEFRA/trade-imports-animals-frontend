# Services

Each external backing system the prototype reads reference data from gets one
service under `services/<name>/`, folder-per-service:

- `services/<name>/index.js` — the stable, synchronous interface the controllers
  call. Controllers depend on this seam only; it holds no data.
- `services/<name>/stub.js` — the vendored reference data, standing in for the real
  system (the swap point). The backing system is named per service in the table
  below, not in an inline comment.
- `services/<name>/client.js` — present only where a real backend exists (`countries`,
  `ports`); fetches the live reference data.

Controllers consume the interface and hold no inline reference-data constants. Each
service's real backing system is documented here, not inline in code.

## Run mode

A global `LIVE_ANIMALS_MODE` (`stub` | `real`, default `stub`, read in
`services/mode.js`) decides what backs the seam. In `stub` mode every service serves
its `stub.js` data. In `real` mode the services that have a `client.js` (`countries`,
`ports`) are primed once at boot — `routes.js` `register` awaits each service's
`prime()`, which fetches from reference-data and caches the result — so the interface
stays synchronous and no consumer changes. A failed prime fails boot loudly (no silent
fallback to stub). Services with no real backend keep serving their vendored data in
both modes.

## Stub completeness vs the real frontend

Real data is adopted only where the real source is **richer** than the prototype:
`countries` (14 → 31 live entries) and `ports` (3 → 78) were captured from
reference-data. For the other services the real frontend currently holds only a
**skeleton subset** while the prototype models the full V4 set, so the prototype keeps
the fuller data (the "fullest stub") — item-8 adoption does **not** regress it to the
skeleton:

- `certification-purposes` — prototype 16-value V4 set; real frontend 3.
- `document-types` — prototype 14 V4 document types; real frontend 2.
- `import-reason-purpose` — prototype 5 reasons + 11 purposes (V4); real frontend 2
  reasons, no purpose.

When a real MDM service for these lands, reconcile toward it / V4 — do not adopt the
interim frontend skeleton.

Storage shape is not uniform across these enums. Most code→label enums
(`certification-purposes`, `import-reason-purpose`, `countries`) store the code
and look the label up at check-answers. The `transport-reference` enums are the
exception: `meansOfTransport` and `transporterType` are persisted as their V4
display label, so their check-answers rows render the raw stored value with no
lookup — do not add one.

| Service                  | Backing system                                                                          | What it stubs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Consumers                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------ | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ports`                  | MDM ports of entry (BCP reference list)                                                 | The list of live-animal ports of entry, in reference-data order — used for the port-of-entry select options and validation membership                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `features/transport/port-of-entry.controller.js`                                                                                                                                                                                                                                                                                                                             |
| `document-types`         | MDM document/attachment types                                                           | The accompanying-document type enum (V4, spec ruling c-010) — used for the document-type select options and validation membership — and the attachment file-format enum, which since the real upload leg (c-034) is the value domain of the system-derived `accompanyingDocumentAttachmentType` (`features/documents/upload-config.js` derives it from the uploaded file's extension; the user-facing attachment select is gone)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `features/documents/controller.js`, `features/documents/upload-config.test.js`                                                                                                                                                                                                                                                                                               |
| `document-uploads`       | trade-imports-animals-backend document-upload endpoints (broker for cdp-uploader)       | The upload lifecycle behind the documents page: `upload` (initiate + file POST, returns `uploadId`), `scanStatus` (`PENDING` / `COMPLETE` / `REJECTED`) and `remove`. Real mode calls the backend's `/notifications/{ref}/document-uploads` and `/document-uploads/{id}` endpoints (`TRADE_IMPORTS_ANIMALS_BACKEND_URL`), mirroring the skeleton's document client. Stub mode cans the lifecycle and settles on an explicit refresh signal, not read counts (server-side gate/render reads would otherwise consume the pending state before a user ever saw it): every read answers `PENDING` until a read carrying `refresh: true` — the controller passes it when the GET arrives via the `?attempt=N` refresh link — settles the upload by filename convention: a name containing `virus` settles `REJECTED`, `never-scans` stays `PENDING` even through refreshes, anything else `COMPLETE`; once settled it stays settled. An unknown (e.g. post-restart) `uploadId` settles straight from the filename, and the page treats an entry with no `uploadId` as `COMPLETE`. Stub mode discards the file bytes — the notification links documents by `uploadId` reference only (c-004) | `features/documents/controller.js`                                                                                                                                                                                                                                                                                                                                           |
| `certification-purposes` | MDM certification purposes                                                              | The V4 sixteen-value animals-certified-for code→label set (spec ruling c-009) — used for the additional-details select options and validation membership, and for the check-answers display label                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `features/additional-details/controller.js`, `features/check-answers/controller.js`                                                                                                                                                                                                                                                                                          |
| `import-reason-purpose`  | MDM / fixed policy value sets for import reason + internal-market purpose               | The V4 five-value reason-for-import enum (spec ruling c-008) and the V4 eleven-value internal-market purpose enum (c-012), both code→label — used for the import-reason and import-purpose select options and validation membership, and for the check-answers display labels                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `features/import-reason/controller.js`, `features/import-purpose/controller.js`, `features/check-answers/controller.js`                                                                                                                                                                                                                                                      |
| `transport-reference`    | Transport reference enums (means of transport, transporter types)                       | The V4 four-value means-of-transport enum, its overland-means activating subset, and the V4 two-value transporter-type enum (c-012) — used for the merged arrival-details page's means radios and the transporters select options and validation membership, and for the check-answers transited-countries reveal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `features/transport/port-of-entry.controller.js`, `features/transport/transporters.controller.js`, `features/check-answers/controller.js`                                                                                                                                                                                                                                    |
| `countries`              | MDM countries (EU/EEA/EFTA reference list, MDM group GBNAG_SPS_EX)                      | The country code→label reference set (spec ruling c-018) — the origin subset used for the origin and transited-countries select/checkbox options and validation membership, the check-answers country labels, and the GB-inclusive country-NAME list used for the address-block country selects and validation membership                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `features/origin/controller.js`, `features/check-answers/controller.js`, `features/transport/transit-countries.controller.js`, `features/transport/private-transporter-details.controller.js`, `features/commodities/animal-identification.controller.js`                                                                                                                    |
| `address-book`           | Trader profile / gov.identity saved parties                                             | The saved-party option lists for each consignment role — consignor, consignee, importer, place-of-origin, destination, contact and approved commercial transporter (spec ruling c-018) — each record carrying the full V4 Standard Address Block, and the commercial transporter its approval number, so a chosen party is saved by copy (c-020) with every field preserved. Used for the party select options and validation membership                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `features/addresses/consignors-select.controller.js`, `features/addresses/consignees-select.controller.js`, `features/addresses/importers-select.controller.js`, `features/addresses/place-of-origin-select.controller.js`, `features/addresses/destinations-select.controller.js`, `features/contact/controller.js`, `features/transport/transporters-select.controller.js` |
| `commodities`            | MDM commodities (picklist, types, species, and the commodity-keyed applicability lists) | The vendored commodity picklist stand-in (V4 code-plus-name strings, spec ruling c-018), the commodity-type and species select enums, and the commodity-keyed applicability lists that gate conditional fields — number-of-packages (V4, spec c-021), the four per-animal identifier-type lists and per-animal permanent address (V4 Level 3, frame:"enclosing"), plus the notification-level unweaned-animal and CPH lists (frame:"anyItem"). Used for the commodity/type/species select options and validation membership, and as the `activatedBy.includes` membership lists the obligation model and page-side reveals share                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `features/commodities/select.controller.js`, `features/commodities/details.controller.js`, `features/commodities/obligations.js`, `features/additional-details/obligations.js`, `features/additional-details/controller.js`, `features/cph-number/obligations.js`, `features/cph-number/controller.js`                                                                       |

The `commodities` service surface changed at inc-062 (the table's commodities
row otherwise stands): the flat type/species enums gave way to per-commodity
species sets behind the batch search — `search(query)` matches a common name,
commodity code or scientific name and returns whole commodity groups, while
`speciesFor(name)` / `isCommoditySpecies(name, value)` back the species
checkbox membership and validation (the commodity-type enum left with
typeSelection, c-037). Its page consumers are now
`features/commodities/search.controller.js` and
`features/commodities/consignment-details.controller.js`.
