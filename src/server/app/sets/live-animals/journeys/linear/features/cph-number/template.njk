{% extends "shared/layout.njk" %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "shared/save-actions.njk" import saveActions %}

{% block journeyContent %}
  {% include "shared/error-summary.njk" %}

  <form method="post" novalidate>
    <input type="hidden" name="crumb" value="{{ crumb }}" />
    <input type="hidden" name="concurrencyToken" value="{{ concurrencyToken }}" />

    {{ govukInput({
      id: "countyParishHoldingCph",
      name: "countyParishHoldingCph",
      label: {
        text: copy.cph.label,
        isPageHeading: true,
        classes: "govuk-label--l"
      },
      hint: { text: copy.cph.hint },
      classes: "govuk-input--width-10",
      value: values.countyParishHoldingCph,
      errorMessage: errors.countyParishHoldingCph and { text: errors.countyParishHoldingCph }
    }) }}

    {{ saveActions(hubHref, copy = sharedCopy.saveActions) }}
  </form>
{% endblock %}
