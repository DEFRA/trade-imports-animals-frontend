/**
 * Barrel for the quote folder-module — the premium formula (system
 * handler's engine) and the deterministic reference (contract status +
 * submit).
 */
export { calculatePremium } from './premium.js'
export { makeReference } from './reference.js'
