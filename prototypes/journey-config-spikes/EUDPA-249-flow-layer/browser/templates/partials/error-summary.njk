{# Adapted from prototype-layouts (obligations-standalone-spike/templates/partials/error-summary.njk).

   Standard GDS error summary. errorSummary = [{ text, href }]. #}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}

{% if errorSummary and errorSummary.length %}
  {{ govukErrorSummary({
    titleText: errorSummaryTitle or "There is a problem",
    errorList: errorSummary
  }) }}
{% endif %}
