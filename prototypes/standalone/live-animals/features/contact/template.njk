{% extends "standalone/live-animals/shared/layout.njk" %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "standalone/live-animals/shared/save-actions.njk" import saveActions %}

{% block journeyContent %}
  {% include "standalone/live-animals/shared/error-summary.njk" %}

  <form method="post" novalidate>
    <input type="hidden" name="crumb" value="{{ crumb }}" />

    {{ govukRadios({
      name: "contactAddress",
      fieldset: {
        legend: {
          text: "Contact address for consignment",
          isPageHeading: true,
          classes: "govuk-fieldset__legend--l"
        }
      },
      hint: { text: "Selecting a contact copies their name and address into this notification." },
      errorMessage: errors.contactAddress and { text: errors.contactAddress },
      items: contactOptions
    }) }}

    {{ saveActions(hubHref) }}
  </form>
{% endblock %}
