// Vendored MDM live-animal ports of entry (BCP reference list) — the swap point when the real reference-data service lands (spec ruling c-018).
export const PORTS = ['ABERDEEN', 'EAST MIDLANDS AIRPORT', 'EDINBURGH']
