{% extends "standalone/obligations-v2-spike/pages/_shared/layout.njk" %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% block journeyContent %}
  {% include "standalone/obligations-v2-spike/pages/_shared/error-summary.njk" %}

  <h1 class="govuk-heading-l">{{ heading }}</h1>

  <form method="post" novalidate>
    <input type="hidden" name="crumb" value="{{ crumb }}" />

    {{ govukRadios({
      name: "claimType",
      fieldset: { legend: { text: "What type of claim was it?", classes: "govuk-fieldset__legend--s" } },
      items: options
    }) }}

    {{ govukInput({
      id: "claimAmount", name: "claimAmount",
      label: { text: "Approximate claim amount" },
      classes: "govuk-input--width-5",
      inputmode: "numeric",
      prefix: { text: "£" },
      value: values.claimAmount
    }) }}

    {{ govukButton({ text: buttonText }) }}
  </form>
{% endblock %}
