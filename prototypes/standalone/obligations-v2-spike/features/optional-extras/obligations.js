/**
 * Optional extras — the obligation def this feature owns. Pure data; imports
 * nothing outward. A multi-select; optional (saves empty).
 */
export const extras = { id: 'extras' }

export const defs = [extras]
