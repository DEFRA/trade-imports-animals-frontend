{% extends "shared/layout.njk" %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "govuk/components/tag/macro.njk" import govukTag %}
{% from "govuk/components/notification-banner/macro.njk" import govukNotificationBanner %}
{% from "govuk/components/pagination/macro.njk" import govukPagination %}

{% block journeyContent %}
  {% if deletionSucceeded %}
    {{ govukNotificationBanner({
      type: "success",
      titleText: sharedCopy.notificationActions.delete.successTitle,
      text: sharedCopy.notificationActions.delete.successBody,
      role: "alert"
    }) }}
  {% endif %}

  {% if copySucceeded %}
    {{ govukNotificationBanner({
      type: "success",
      titleText: sharedCopy.notificationActions.copy.successTitle,
      text: sharedCopy.notificationActions.copy.successBody,
      role: "alert"
    }) }}
  {% endif %}

  <h1 class="govuk-heading-xl">{{ copy.title }}</h1>
  <p class="govuk-body">{{ copy.body }}</p>

  <form method="post" action="{{ startAction }}" novalidate>
    <input type="hidden" name="crumb" value="{{ crumb }}" />
    {{ govukButton({ text: copy.startButton, isStartButton: true }) }}
  </form>

  <h2 class="govuk-heading-m">{{ copy.notificationsHeading }}</h2>
  <div class="govuk-grid-row notification-list__layout">
    <div class="govuk-grid-column-one-third">
      <aside class="notification-list__filters" aria-labelledby="filter-notifications-heading">
        <h3 class="govuk-heading-m" id="filter-notifications-heading">{{ copy.search.heading }}</h3>

        <form method="get" action="{{ listAction }}" class="notification-list__search-form" data-testid="notification-search-form">
          {% if sort %}
            <input type="hidden" name="sort" value="{{ sort }}" />
          {% endif %}
          {{ govukInput({
            id: "referenceNumber",
            name: "referenceNumber",
            label: {
              text: copy.search.label,
              classes: "govuk-label--s"
            },
            value: referenceNumber
          }) }}
          {{ govukButton({ text: copy.search.button }) }}
        </form>
      </aside>
    </div>

    <div class="govuk-grid-column-two-thirds">
      <div class="notification-list__toolbar">
        <p class="govuk-label govuk-label--s notification-list__results">{{ resultsLabel }}</p>

        <form method="get" action="{{ listAction }}" class="notification-list__sort-form">
          {% if currentPage > 1 %}
            <input type="hidden" name="page" value="{{ currentPage }}" />
          {% endif %}
          {% if referenceNumber %}
            <input type="hidden" name="referenceNumber" value="{{ referenceNumber }}" />
          {% endif %}
          <div class="govuk-form-group govuk-!-width-auto">
            <label class="govuk-label govuk-label--s" for="sort">{{ copy.sort.label }}</label>
            <select class="govuk-select" id="sort" name="sort">
              {% for option in sortOptions %}
                <option value="{{ option.value }}" {% if option.value == sort %}selected{% endif %}>{{ option.text }}</option>
              {% endfor %}
            </select>
          </div>
          {{ govukButton({
            text: copy.sort.update,
            classes: "govuk-button--secondary"
          }) }}
        </form>
      </div>

      {% if notificationRows.length %}
        {% for row in notificationRows %}
          <div class="govuk-summary-card notification-list">
        <div class="govuk-summary-card__title-wrapper">
          <h3 class="govuk-summary-card__title">{{ row.reference }}</h3>
          <ul class="govuk-summary-card__actions">
            {% for action in row.actions %}
              <li class="govuk-summary-card__action">
                {% if action.postAction %}
                  <form method="post" action="{{ action.postAction }}" class="app-form-inline">
                    <input type="hidden" name="crumb" value="{{ crumb }}" />
                    {% if action.idempotencyKey %}
                      <input type="hidden" name="idempotencyKey" value="{{ action.idempotencyKey }}" />
                      <input type="hidden" name="copyOrigin" value="{{ action.copyOrigin }}" />
                    {% endif %}
                    <button type="submit" class="govuk-link app-link-button">
                      {{ action.text }}<span class="govuk-visually-hidden"> {{ copy.actionHidden(row.reference) }}</span>
                    </button>
                  </form>
                {% else %}
                  <a class="govuk-link" href="{{ action.href }}">
                    {{ action.text }}<span class="govuk-visually-hidden"> {{ copy.actionHidden(row.reference) }}</span>
                  </a>
                {% endif %}
              </li>
            {% endfor %}
          </ul>
        </div>
        <div class="govuk-summary-card__content">
          <dl class="govuk-grid-row notification-list__card-row">
            <div class="govuk-grid-column-one-third">
              <dt class="govuk-body-s govuk-!-font-weight-bold govuk-!-margin-bottom-1">{{ copy.table.commodity }}</dt>
              <dd class="govuk-body govuk-!-margin-bottom-0 govuk-!-text-break-word">{{ row.commodity }}</dd>
            </div>
            <div class="govuk-grid-column-one-third">
              <dt class="govuk-body-s govuk-!-font-weight-bold govuk-!-margin-bottom-1">{{ copy.table.origin }}</dt>
              <dd class="govuk-body govuk-!-margin-bottom-0 govuk-!-text-break-word">{{ row.origin }}</dd>
            </div>
            <div class="govuk-grid-column-one-third">
              <dt class="govuk-body-s govuk-!-font-weight-bold govuk-!-margin-bottom-1">{{ copy.table.arrival }}</dt>
              <dd class="govuk-body govuk-!-margin-bottom-0 govuk-!-text-break-word">{{ row.arrival }}</dd>
            </div>
          </dl>
          <dl class="govuk-grid-row notification-list__card-row">
            <div class="govuk-grid-column-one-third">
              <dt class="govuk-body-s govuk-!-font-weight-bold govuk-!-margin-bottom-1">{{ copy.table.consignee }}</dt>
              <dd class="govuk-body govuk-!-margin-bottom-0 govuk-!-text-break-word">{{ row.consignee }}</dd>
            </div>
            <div class="govuk-grid-column-one-third">
              <dt class="govuk-body-s govuk-!-font-weight-bold govuk-!-margin-bottom-1">{{ copy.table.consignor }}</dt>
              <dd class="govuk-body govuk-!-margin-bottom-0 govuk-!-text-break-word">{{ row.consignor }}</dd>
            </div>
            <div class="govuk-grid-column-one-third">
              <dt class="govuk-body-s govuk-!-font-weight-bold govuk-!-margin-bottom-1">{{ copy.table.status }}</dt>
              <dd class="govuk-body govuk-!-margin-bottom-0 govuk-!-text-break-word">
                {{ govukTag({ text: row.status.text, classes: row.status.classes }) }}
              </dd>
            </div>
          </dl>
          <dl class="govuk-grid-row notification-list__card-row">
            <div class="govuk-grid-column-one-third">
              <dt class="govuk-body-s govuk-!-font-weight-bold govuk-!-margin-bottom-1">{{ copy.table.created }}</dt>
              <dd class="govuk-body govuk-!-margin-bottom-0 govuk-!-text-break-word">{{ row.created }}</dd>
            </div>
            <div class="govuk-grid-column-one-third">
              <dt class="govuk-body-s govuk-!-font-weight-bold govuk-!-margin-bottom-1">{{ copy.table.submitted }}</dt>
              <dd class="govuk-body govuk-!-margin-bottom-0 govuk-!-text-break-word">{{ row.submitted }}</dd>
            </div>
          </dl>
        </div>
          </div>
        {% endfor %}
      {% elif not referenceNumber %}
        <p class="govuk-body">{{ copy.emptyText }}</p>
      {% endif %}

      {% if pagination %}
        {{ govukPagination(pagination) }}
      {% endif %}
    </div>
  </div>
{% endblock %}
