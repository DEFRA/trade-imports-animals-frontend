/**
 * Field view barrel — re-exports the same public names the original single
 * `to-view.js` had so import sites resolve identically.
 */

export { fieldToView } from './registry.js'
export { attachError } from './errors.js'
