# SIMPLIFICATION-PLAN.md — obligations-standalone-spike

**Status: ANALYSIS ONLY. Nothing has been applied. Awaiting human rulings before any change.**

Generated by a sequenced multi-agent workflow: 10 layer readers mapped real complexity → each
candidate was cross-checked against `coverage-matrix.json` (which obligation ids the touched files
handle) **and** the cited `obligations.md` line ranges → an adversarial skeptic argued why each breaks
an obligation, a parity Ruling, or a standalone convention. 40 candidates survived to verdict.

## Verdict summary

| Verdict              | Count | Meaning                                                                                                |
| -------------------- | ----- | ------------------------------------------------------------------------------------------------------ |
| `kill`               | 2     | Breaks an obligation outright, or unravels a convention the spike exists to demonstrate. Do not apply. |
| `needs-human-ruling` | 38    | Sound engineering **or** an unearned-looking seam — but the trade is yours to sign off.                |
| `safe`               | 0     | —                                                                                                      |

**Zero auto-safe is the headline finding, and it is not the skeptics being timid.** This is a
paradigm-_demonstration_ spike. Its explicit deliverables — README "interrogation Level 1" barrels,
the two-evaluator split, EXTENDING.md teaching seams, the honesty ledger, the invariant test tiers —
mean that a mechanism which looks unearned in a one-journey app is often _the demonstration itself_.
Every skeptic that reached `needs-human-ruling` did so by naming a specific demonstration artifact the
change would erode. The cross-check found **no** implement/reduce obligation silently broken among the
38 (only the killed `flow.json title` candidate broke obligations) — so this is not a correctness
minefield; it is a **"how much demonstration surface do you want to keep"** decision.

## The 2 kills (do not apply)

1. **Drop the 12 duplicate `title` fields in flow.json** — cross-check **fail**. The `addons` page's
   `title` feeds the hub's "Add to your policy" row via `journey/hub-view.js:60` (a read site the
   candidate missed), so deletion leaves that row label `undefined`, breaking `DEF-26`, `STATUS-21/14/22`,
   `OPEN2-X2_1/X2_2` and the `task-list-with-linear-tasks` e2e spec that navigates by that label.
2. **Delete `engine/scope/index.js` sub-barrel** — the barrel-per-folder-module is a _universal_,
   deliberately-demonstrated convention (every one of the ~16 folder-modules has a thin barrel face,
   several single-export). Removing this one makes `scope/` the sole folder-module without a face and
   licenses unravelling the whole convention; it also narrows the engine's advertised `demos` surface.

## The rulings that unlock everything — decide these first

Most of the 38 collapse into **six cross-cutting policy questions**. Answer these and the individual
items resolve in bulk; the per-item detail below is the evidence for each.

### Ruling A — Barrel/export surface: "advertised capability" or "observed usage"?

The spike's README (53–65) frames every folder-module barrel as an interrogation surface that advertises
the module's _full_ public vocabulary "by identity", beyond what any current caller imports. Under that
reading, unused exports are **not dead** — they are the demonstration. If you instead want barrels
trimmed to consumed names, that is a deliberate re-scoping of the paradigm.
**Governs:** dead barrel re-exports (validation/lib-fields/journey), store `IN_PROGRESS/SUBMITTED`,
orchestrator's 4 dead re-exports, engine identifier/scope/`humaniseName` surface, `PAGE_MANDATES/ENGINE_MANDATES`.
→ ~7 items.

### Ruling B — Thin single-file / single-consumer barrels: inviolable, or inline them?

`i18n/index.js` (one source file), `contract/cya-rows/index.js`, `orchestrator/apply-answers/index.js`,
`flow-eval/navigation/index.js` are all thin barrels the skeptics defend as the self-similarity
demonstration (store keeps the same split for its single source file — the author's revealed preference).
Inlining any one singles it out as the exception. One ruling covers all of them (and the killed scope barrel).
→ ~5 items.

### Ruling C — Demonstration seams for the two-evaluator / injectable / portable-data story

`createJourneyFlowConditions` (the construction-injection twin of the ObligationEvaluator, a named
handled*by artifact for IMPLEMENT-scope `OPEN1-X2_1`), the `conditions` injection forward through the
contract barrel, `obligationStateOver`, and `toFieldErrors`'s injectable resolver each make an
injection/separability point \_visible*. They are unused in one journey by design. Keep them, or accept
a narrower demonstration.
→ ~4 items.

### Ruling D — Invariant test tiers & test-hygiene: freeze or refactor?

Several dedup/DRY candidates touch the protected tiers (rename-survival freshness, completability
state-space oracle-independence, alignment-walker plumbing) or the parity-facts pin (`EXPECTED_TYPES`
freezes phone-as-`tel`). Behaviour is preserved in each, but oracle-independence and tamper-obviousness
are the point of those tiers. Decide whether test DRY is worth trading them.
→ ~7 items (cyaRows dedup, contract fixtures, six model-read helpers, enumerate-states, clear()+store test,
EXPECTED_TYPES, alignment-walker unexports).

### Ruling E — Documented parity twins & scored grafts (routes + templates)

`claims-add.njk` deletion overturns **graft-9** (a scored decision to keep bespoke templates as
exhibits); the dynamic `{% extends layout %}` seam mirrors spike-a for **graft-12** line-for-line
diffability; `getConfirmation`'s in-handler gate is the documented "guarded in both paradigms" parity
twin; folding `submit.js` into CYA collides with **Ruling 1** (the freeze is where this spike _diverges_
from spike-a). These re-shape human-owned self-description artifacts (DESIGN-DECISION grafts,
parity-facts.json, README ledger, PLAN module ledger).
→ ~5 items.

### Ruling F — Genuinely-dead behaviour with no demonstration story

A residue where the "demonstration" defence is weakest and the item is closest to a real win — but each
still edits a ground-truth artifact (coverage-matrix/PLAN/DESIGN-DECISION) or a paradigm-visible file,
so it wants your nod: `reconcileJourney` (dead load-triggered entry — but the _sole_ in-code realiser of
PERSIST-18's reload-idempotency; wiring it into the GET path is the better fix), `decodeFieldName` (dead
half of a named two-way seam; leaves DESIGN-DECISION.md:149 stale), `blocksSave()` (dead second gate;
under-scopes its own matrix repoint), `expandSlots` flags / fixed-point change-tracking outputs (behind
the protected EVAL-35 tag).
→ items concentrated in the `medium`-risk group below.

## How to use this document

Skim the six rulings above, then read the per-item detail (grouped kills-first, then by risk then title).
Each item carries: current shape → proposed shape → obligation ids affected (with why still satisfied) →
effort/risk → skeptic's strongest objection. Mark each item apply / hold / drop, or answer the governing
ruling and I will apply the bucket. **I will not touch any of this until you tell me which rulings/items
to action.**

## Recommendation

If the intent is to keep this spike as a faithful paradigm demonstration, my recommendation is: **apply
nothing from Rulings A, B, C, D, E** (the "unused" surface, thin barrels, injection seams, invariant
tiers and parity twins are the deliverable), and cherry-pick from **Ruling F** only the items you agree
are dead _with the better remediation_ (e.g. wire `reconcileJourney` into the GET path rather than
delete it; delete `blocksSave()` only with the full `STATUS-29/ARCH-36/37` matrix repoint). That is a
small, honest cleanup that strengthens rather than thins the demonstration. The style-hardening pass
(separate workflow) is where the real low-risk win lives.

---

## Delete the engine/scope/index.js sub-barrel and the unused `demos` namespace re-export

- **Verdict:** `kill` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** engine/scope/index.js, engine/evaluate.js, engine/evaluate.test.js, engine/index.js, engine/index.test.js, coverage-matrix.json, PLAN.md

**Current shape.** engine/scope/index.js:1-13 is a 3-line re-export barrel (registry, journey-rules, `export * as demos from './expressiveness.js'`). Its only consumers are engine/evaluate.js:3 (journeyScopeRegistry), engine/evaluate.test.js:5 (ENGINE_MANDATORY_ALWAYS) and engine/index.js:24-30. Nothing in the spike or e2e consumes `demos` via any barrel — expressiveness.test.js:4 imports './expressiveness.js' directly, and the only pin on the barrel's `demos` entry is engine's own surface-smoke test index.test.js:16. load-model.test.js:6 already imports journey-rules directly, proving the barrel is optional.

**Proposed shape.** Delete engine/scope/index.js. evaluate.js and evaluate.test.js import from './scope/journey-rules.js'; engine/index.js re-exports createScopeRegistry from './scope/registry.js' and {journeyScopeRegistry, createJourneyScopeRegistry, ENGINE_MANDATORY_ALWAYS} from './scope/journey-rules.js', and drops the `demos` entry (expressiveness.js stays as the graft-5 demo module exercised by its own test). Update index.test.js:6-29 surface list, remove the engine/scope/index.js row from coverage-matrix.json SHAPE-15 handled_by (registry.js + journey-rules.js already cover it), and fix PLAN.md:105/227 inventory rows.

**Rationale.** A barrel inside a layer that already has a barrel (engine/index.js is the doc-shaped face; the standalone conventions only mandate the contract barrel). It has two importers, both trivially redirectable, and its one distinctive job — namespacing `demos` — has zero consumers. Flagging touches_protected only because evaluate.js gets a one-line import-specifier swap (no split/shrink of the accepted 161-line graft).

**Obligations affected.**

- `SHAPE-15` (implement, **still-satisfied**): The obligation (DOC:196 — scoping/mandate status 'NOT on the obligation. Computed by the evaluation engine') is carried by model/obligations.json + engine/scope/registry.js + engine/scope/journey-rules.js + engine/load-model.js, all untouched; its test model/obligations.test.js does not import the sub-barrel. engine/scope/index.js is pure re-export plumbing, so deleting it and removing it from SHAPE-15 handled_by leaves the behaviour and test intact.
- `DEF-X2_1` (implement, **still-satisfied**): DOC:113-114 (JourneyEvaluator layers on ObligationEvaluator output) is implemented in evaluate.js + flow-eval; evaluate.js only swaps its journeyScopeRegistry import from './scope/index.js' to './scope/journey-rules.js' — same object, same pipeline — and engine/index.js still exports evaluateObligations. Test engine/evaluate.test.js keeps ENGINE_MANDATORY_ALWAYS via the same one-line source swap.
- `ARCH-12` (reduce, **still-satisfied**): The reduced claim (DOC ~1181: one evaluator/orchestrator set per language, exposed through the engine and flow-eval barrels) is pinned by engine/index.test.js; the proposal updates only the surface list (drops 'demos', splits the scope re-exports across registry.js/journey-rules.js sources) while the barrel still ships the whole evaluator set and the end-to-end through-the-barrel test at index.test.js:32-46 is untouched. demos was never part of the evaluator set ('never registered against journey obligations').
- `TOOL-20` (implement, **still-satisfied**): DOC ~1933 (evaluators pure functions, models pure data); an import-specifier swap in evaluate.js and re-export source changes in engine/index.js alter no purity property; test engine/evaluate.test.js unchanged in substance.

**Skeptic — strongest objection.** The candidate's load-bearing premise — "a barrel inside a layer that already has a barrel... the standalone conventions only mandate the contract barrel" — is factually false about this spike. `find -name index.js` shows barrel-per-folder-module is a UNIVERSAL, deliberately-demonstrated paradigm convention: every one of the 9 nested folder-modules carries its own thin re-export barrel face (orchestrator/apply-answers, flow-eval/navigation, routes/endings, routes/claims, contract/cya-rows, lib/quote, lib/fields, engine/scope), several with a single trivial consumer or a single export (contract/cya-rows/index.js exports exactly one symbol; lib/quote/index.js two). engine/scope/index.js is not an anomalous "redundant barrel-in-a-barrel" — it is exactly consistent with 7+ sibling nested folder-modules whose parents also have barrels. Deleting it makes scope/ the SOLE folder-module whose siblings must reach past its face into ./scope/journey-rules.js and ./scope/registry.js directly, breaking the self-containment/folder-module-has-a-face seam the spike exists to demonstrate; and the candidate's own justification ("two importers, both trivially redirectable") applies verbatim to nearly every other nested barrel, so accepting it licenses unravelling the whole convention. Separately, the `demos` re-export it drops is part of the engine/index.js "doc-shaped barrel narrative" (DESIGN-DECISION.md:345) and the interrogation-Level-1 surface README.md:55 frames as paradigm IP ("importing the barrel answers 'what can this do'"), pinned by engine/index.test.js — narrowing that advertised surface is a paradigm-visible loss, not dead-code removal.

---

## Drop the 12 title fields that duplicate heading in flow.json; default title to heading at the consumer seam

- **Verdict:** `kill` | **Effort:** S | **Risk:** low | **Obligation cross-check:** fail
- **Files touched:** model/flow.json, contract/view.js, journey/hub-view.js, routes/claims/handlers.js

**Current shape.** Every page in model/flow.json carries both `title` and `heading`, and they are byte-identical on 12 of 14 pages (e.g. flow.json:36-38 email 'Give us your email to begin' twice, flow.json:59-61 about-you, flow.json:110-112 your-vehicle, flow.json:163-165 driving-history, flow.json:230-232 cover-type, flow.json:276-278 optional-extras, flow.json:305-307 addons, flow.json:340-341, 360-362, 388-391, 403-407, 427-431). Only 'claims' (title 'Your claims' vs heading 'Claims you have added', flow.json:189-190) and 'quote-summary' ('Get your quote' vs 'Your quote', flow.json:454-455) genuinely differ. model/skeleton-flow.json already omits title/heading entirely, proving the schema tolerates absence. Consumers: contract/view.js:72 (`title: page.title`), journey/hub-view.js:48,81 (section hint text), routes/claims/handlers.js:29,34.

**Proposed shape.** Delete `title` from the 12 pages where it equals `heading`, keeping the two genuine divergences (claims, quote-summary). At the three read sites use `page.title ?? page.heading` (contract/view.js:72, journey/hub-view.js:48 and :81, routes/claims/handlers.js:29/34). flow.test.js keeps its existing pins unchanged — 'distinguishes the three claims buttons' (flow.test.js:150-157) still pins claims.title, and the parity-copy spot-checks pin headings. Rendered output (browser <title>, breadcrumbs, hub hints) is byte-identical since title===heading on every affected page.

**Rationale.** Twelve near-duplicate copy strings in the polished Flow is the largest redundancy in the model layer: every copy edit must be made twice or the fields silently drift, and no obligation in the coverage matrix requires a page title distinct from its heading (the two pages that need it keep it). A nullish fallback at the consumer seam is one token per site and keeps all journey knowledge in data — the default is schema shape, not journey content.

**Obligations affected.**

- `DEF-26` (implement, **broken**): DOC 88-91: top-level Sections appear on the Task List. The 'Add to your policy' section is represented on the hub by addonPickerItem (journey/hub-view.js:60, `title: { text: picker.title }`) — a read site NOT in the candidate's edit list. The addons page (flow.json:304-308) has title===heading so its title is deleted, leaving the hub row title undefined.
- `STATUS-21` (implement, **broken**): DOC 1243-1244: Task List renders each top-level Section with its status and a link. Status tag and href survive at hub-view.js:60-66, but the row label becomes undefined for the add-to-your-policy section under the proposed shape.
- `STATUS-14` (implement, **broken**): Behaviour (Section-level status on the hub) is itself unchanged, but its cited test journey/hub-view.test.js fails at lines 41-45 and 60 (pins 'Add to your policy' as an item title.text), so coverage is no longer green under the proposed shape.
- `STATUS-22` (implement, **broken**): NA-hiding behaviour is unchanged (flow.json appliesWhen data untouched), but its handled_by e2e prototypes/e2e/task-list-with-linear-tasks.spec.js:64 navigates via `task(page, 'Add to your policy')`, which no longer matches — the e2e vehicle covering this obligation goes red.
- `OPEN2-X2_1` (implement, **broken**): Signpost/coming-up data in flow.json is untouched, but the same task-list e2e spec in its handled_by set breaks at the 'Add to your policy' step before reaching the signposting assertions.
- `OPEN2-X2_2` (implement, **broken**): Cannot-start-yet-via-signposting behaviour (quoteItem, hub-view.js:92-105, uses flow.hub.\* copy, not page.title) is unchanged, but its cited test journey/hub-view.test.js fails on the picker-row title pins in the same file.
- `OPEN2-1` (implement, **still-satisfied**): DOC option (b) sketch ('every node has id, title, EITHER children OR presents', catalogue lines 1931-1933) is a recorded leaning about recursive children-vs-presents shape, and the spike already treats title as optional — model/skeleton-flow.json omits title/heading entirely and stays green — so 12 polished pages dropping a duplicate title does not change the decided schema shape.
- `OPEN2-X1_1` (implement, **still-satisfied**): The unified-recursive-Container leaning survives: nodes keep kind/id and children-or-presents; title becoming optional matches the existing skeleton precedent.
- `OPEN2-15` (implement, **still-satisfied**): 'One shape for skeleton and polished Flows' (DOC: 'they use the same shape') is strengthened, not weakened — skeleton already omits title, so optional title narrows the shape gap; flow.test.js:181 structural pin unaffected.
- `SHAPE-2` (implement, **still-satisfied**): Journey-layer rendering of user-facing obligations uses presents labels/hints (flow.json) and heading (templates/page.njk:14) — all untouched; contract/view.js:72 gets the `?? heading` fallback so pageTitle/breadcrumb output via routes/page.js:82,85 is byte-identical.

**Skeptic — strongest objection.** cross-check found a broken implement/reduce obligation

---

## Collapse extract-inputs' never-varied `ignore` option and never-consumed output fields

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** tests/helpers/extract-inputs.js, tests/helpers/extract-inputs.test.js

**Current shape.** tests/helpers/extract-inputs.js:11 exports `IGNORED_NAMES` and extract-inputs.js:82 takes `{ ignore = IGNORED_NAMES }` — but every call site in the spike and e2e (alignment-walker.test.js:100,176,190 and extract-inputs.test.js) calls `extractInputs(html)` with no options; the seam has exactly one configuration ever used. rawControls (extract-inputs.js:31-39) also extracts `id` (never read by anything) and `describedBy` (emitted at line 107 but consumed nowhere — the alignment walker's a11y checks use the separate `errorWiring` regex; the only references are `describedBy: undefined` assertions in extract-inputs.test.js:60,67,102).

**Proposed shape.** extractInputs(html) with the crumb skip hardcoded (`name === 'crumb'`), no options object, no IGNORED_NAMES export; rawControls drops the `id` and `describedBy` fields; the output record becomes `{ name, kind, inputType?, values?, options? }`. extract-inputs.test.js drops the three `describedBy: undefined` lines (the crumb-ignoring test at line 53 stays and still pins the behaviour).

**Rationale.** An options seam with one registrant-for-life and two output fields no consumer reads is over-generality in a helper whose whole justification (per its header) is being a minimal regex extractor; no obligation id on tests/helpers/extract-inputs.js (TEST-8/10/11/12) needs configurable ignore lists or aria plumbing in the extraction record.

**Obligations affected.**

- `TEST-8` (implement, **still-satisfied**): Forward alignment (every presents entry has an HTML input) is proven in alignment-walker.test.js:104-113 via control.name and control.kind. Removing the ignore option (hardcoding the crumb skip — the sole config ever used), id, and describedBy leaves name/kind untouched. Still handled by extract-inputs.js, still tested by alignment-walker.test.js.
- `TEST-10` (implement, **still-satisfied**): Reverse alignment (no HTML input lacking a presents entry, alignment-walker.test.js:115-122) depends on the crumb still being suppressed so it is not flagged as an orphan control. The proposed hardcoded name==='crumb' skip preserves exactly the default IGNORED_NAMES=['crumb'] behaviour; the crumb-ignoring unit test (extract-inputs.test.js:52) is retained. Behaviour and coverage intact.
- `TEST-11` (implement, **still-satisfied**): Type alignment (alignment-walker.test.js:124-137) reads control.inputType, control.values and control.options — none of which are the dropped fields (id/describedBy). The proposed record keeps { name, kind, inputType?, values?, options? }, so widget-satisfies-type checks are unchanged and still tested.
- `TEST-12` (implement, **still-satisfied**): A11y/required alignment for hard mandates is proven by the separate errorWiring() and errorSummaryLinks() regex functions (alignment-walker.test.js:153-169, extract-inputs.test.js:114-138), NOT by the record's describedBy field. Those functions are untouched by the candidate. The record's describedBy is consumed nowhere but the two assertions being dropped, so removing it does not weaken TEST-12's aria/error-id wiring coverage.
- `TEST-2` (implement, **still-satisfied**): The model-to-HTML alignment harness's extractor is pinned by extract-inputs.test.js. All behavioural pins survive — crumb-ignoring (line 52), radio/checkbox folding, date-triple folding, select options, textarea/file classification, error wiring, summary links. Only the three `describedBy: undefined` clutter assertions (lines 60/67/102) are dropped, matching the removal of a field no consumer reads; the extractor's justification (minimal regex extraction) is unaffected.
- `TEST-7` (implement, **still-satisfied**): One-walker-per-page alignment (Flow + HTML as sole inputs) is unaffected: extractInputs signature narrows from (html, options?) to (html) but every call site already passed html only, so the walker's per-page invocation is byte-for-byte equivalent. Coverage in alignment-walker.test.js and the extractor unit test both remain green.

**Skeptic — strongest objection.** The candidate doesn't merely delete unearned configurability — it erases IGNORED_NAMES, a named/frozen/exported constant that encodes the spike's core model-backed-control-vs-plumbing distinction (documented as an 'explicit ignored-names allowlist' in PLAN.md:350), reducing it to a magic-string `name === 'crumb'`; and it reshapes the pinning of extract-inputs.js, the extractor underpinning the paradigm-load-bearing alignment-walker invariant tier. Neither breaks an obligation, Ruling, or e2e spec (errorWiring() supplies TEST-12's a11y oracle independently, and no call site ever uses `ignore` or reads id/describedBy), so it is sound engineering — but a less destructive middle path (retain the named constant, drop only the param and dead fields) exists, so removing the documented named seam and altering test-tier shape warrants human sign-off.

---

## Consolidate 8 independent flow.json re-parses and 3 premium-id derivations into one shared flow-lookup module

- **Verdict:** `needs-human-ruling` | **Effort:** M | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** routes/flow-model.js, routes/shell.js, routes/page.js, routes/endings/index.js, routes/endings/check-your-answers.js, routes/endings/confirmation.js, routes/endings/submit.js, routes/endings/quote-summary.js, routes/claims/page-model.js

**Current shape.** Eight files each do `const flow = JSON.parse(modelJson().flow)` and then re-derive the same lookups in code: routes/shell.js:18; routes/page.js:26-39 (parse + `collectPages` walker + `questionPages` + `cyaPath`); routes/endings/index.js:15,20; routes/endings/check-your-answers.js:20-24 (deep `premiumId` chain via sections['get-your-quote'].children['quote-summary'].presents[0].obligation); routes/endings/confirmation.js:18-21 (byte-identical copy of that premiumId chain); routes/endings/submit.js:18,24; routes/endings/quote-summary.js:19-31 (a SECOND page-tree walker `collect`, duplicating page.js's `collectPages`, plus a third premiumId derivation); routes/claims/page-model.js:10-16.

**Proposed shape.** One small routes/flow-model.js (folder-module sized, ~30 lines) that parses `modelJson().flow` once and exports: `flow`, `pages` (the flattened kind==='page' list, one walker), `pageById(id)`, `premiumId`, `cyaPath()`, `confirmationPath()`. All eight files import from it; the two walkers, both cyaPath copies and all three premiumId derivations collapse to one site each. claims/page-model.js keeps its claims-specific derivations (page, typeEntry, CLAIM_NAMES) but sources `flow` from the shared module. The verbatim-serving contract (`modelJson()` returning raw text for TOOL-22/23) is untouched.

**Rationale.** The identical 3-line premiumId navigation exists verbatim in two files and in a third variant form, and the page-tree flattener exists twice — a flow.json shape change today means eight edit sites. Nothing in the DOC or the matrix pins per-file parsing; the obligations on these files (SHAPE-2, SUBMIT-_, NAV-_, ARCH-5) are behavioural and fully covered by the existing route tests, which pass unchanged. Only shell.js's parity-diffable line 18 changes (one import line vs spike-a), a negligible diff cost.

**Obligations affected.**

- (none of the touched files’ implement/reduce obligations are disturbed; see full cross-check in workflow journal)

**Skeptic — strongest objection.** The candidate creates a NEW routes-root derivation module (routes/flow-model.js exporting premiumId, cyaPath, confirmationPath and the flattened `pages` list) that becomes a second authority on journey structure sitting OUTSIDE the contract barrel — which contract/index.js explicitly documents as the paradigm centrepiece: "exactly 21 pinned exports... the ONLY module routes import... interrogation Level 1... The surface-drift test pins the export list." Those very derivations are the class the contract already owns: navigation.js exports firstPagePresentingObligation and changeTarget (premiumId is literally "the obligation presented by quote-summary"). Whether shared flow derivations belong in the pinned/interrogable contract barrel or in an unpinned routes util is a paradigm-visible architecture decision, not an auto-safe DRY win. It also edits shell.js, a file whose docstring curates it as "line-for-line diffable against spike-a/routes/shell.js" — a deliberate paradigm-contrast exhibit — so changing its parse line to an import alters a demonstration artifact Sam maintains by hand.

---

## Dead `conditions` option threaded through the contract layer with zero callers

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** contract/status.js, contract/navigation.js

**Current shape.** contract/status.js destructures `conditions` from evaluate() options (status.js:118) and threads it into statusOptions and flowJourneyState (status.js:124,131); contract/navigation.js does the same in nextAfter (navigation.js:63-64) and sectionEntry (navigation.js:76,81). Grep across routes/, journey/, orchestrator/, tests/, all contract tests and prototypes/e2e: no caller ever passes `conditions` — every route calls evaluate(journey) / nextAfter(page.id, evaluation) / sectionEntry(sectionId, evaluation) bare. The injectable registry seam is exercised only by flow-eval's own unit tests calling flow-eval functions directly (e.g. container-status.test.js:141, next-after.test.js:63).

**Proposed shape.** Drop `conditions` from the contract layer entirely: evaluate/nextAfter/sectionEntry stop accepting or forwarding it, letting the flow-eval functions fall through to their journeyFlowConditions default parameter (which stays injectable for flow-eval's fixture tests — the seam the tests use survives where it is used). statusOptions in status.js collapses to passing no options to containerStatus/collectStatuses.

**Rationale.** An over-general seam duplicated at a layer that never varies it: the registry injection point is a flow-eval testing affordance, and re-exposing it through the contract implies a capability (per-request condition sets) no obligation, route, or e2e needs — the coverage matrix maps the appliesWhen obligations (SHAPE-28, EVAL-30, STATUS-9) to flow-eval/applies-when.js, not to the contract plumbing.

**Obligations affected.**

- `DEF-30` (implement, **still-satisfied**): canSubmit=state===FULFILLED in evaluate(); flowJourneyState keeps {submitted} and conditions was always undefined, so the default registry fires identically; test flow-eval/journey-state.test.js calls flow-eval directly
- `DEF-38` (implement, **still-satisfied**): evaluate()'s pinned output key set (obligations/fulfilments/drops...) is unchanged by dropping a dead option; store/journey-repository.test.js untouched
- `EVAL-28` (implement, **still-satisfied**): journeyState incl. Submitted: flowJourneyState(flow, result, {submitted}) keeps submitted (journey-state.js:34 defaults submitted=false, conditions defaults internally); flow-eval/journey-state.test.js owns the seam directly
- `STATUS-2` (implement, **still-satisfied**): collectStatuses still computes containerStatus for every container; passing no options is identical to {conditions:undefined} because container-status.js:105 defaults conditions=journeyFlowConditions; test flow-eval/container-status.test.js builds its own registry via createFlowConditionRegistry
- `STATUS-34` (implement, **still-satisfied**): CYA Submit gate rides evaluate().canSubmit whose computation is unchanged; model/messages.test.js untouched
- `STATUS-X1_1` (implement, **still-satisfied**): return-to-hub on subtree exhaustion is nextAfter's withinTask/hubPath logic, unedited; only the dead conditions forward is removed; flow-eval/navigation/next-after.test.js exercises the seam directly (next-after.js:43 defaults conditions)
- `STATUS-X1_4` (implement, **still-satisfied**): Submitted transition is journey.status===SUBMITTED -> {submitted} -> journeyState, all retained; flow-eval/journey-state.test.js untouched
- `NAV-33` (implement, **still-satisfied**): journey-level Not Started comes from flowJourneyState with identical effective inputs (conditions was never supplied by any caller); flow-eval test untouched
- `NAV-34` (implement, **still-satisfied**): journey-level In Progress — same flowJourneyState call, same defaults; flow-eval/journey-state.test.js untouched
- `NAV-35` (implement, **still-satisfied**): journey Fulfilled + Submit gate — evaluate() still derives canSubmit from FULFILLED state; behaviour byte-identical for all callers
- `NAV-39` (implement, **still-satisfied**): one-way Submitted flip lives in journey.status/submitted handling, untouched by removing the conditions thread; Ruling 1 (post-submit freeze) behaviour unchanged
- `STATUS-9` (implement, **still-satisfied**): appliesWhen gating in nextAfter/sectionEntry resolves via applies-when.js:98 default conditions=journeyFlowConditions — the only registry any caller ever used; matrix tests it at flow-eval/applies-when.test.js which constructs its own registry; contract/guards.js untouched
- `STATUS-23` (implement, **still-satisfied**): sectionEntry still resolves entry mode via flow-eval sectionEntry with identical effective options; test flow-eval/navigation/section-entry.test.js calls flow-eval directly
- `STATUS-28` (implement, **still-satisfied**): Fulfilled-Section degrade behaviour lives in flow-eval sectionEntry modes; contract sectionEntry still forwards flow+evaluation; conditions default identical; section-entry.test.js untouched
- `STATUS-36` (implement, **still-satisfied**): never-hard-code-next-page: contract nextAfter still queries flow-eval nextPageAfter; dropping {conditions} (always undefined) hits the same next-after.js:43 default; next-after.test.js exercises the injectable seam directly (line 63)
- `NAV-10` (implement, **still-satisfied**): default firstApplicablePage entry mode still resolves through contract sectionEntry -> flow-eval with identical effective inputs; section-entry.test.js untouched
- `NAV-11` (implement, **still-satisfied**): unforked renderer entry resolution: routes still call sectionEntry(sectionId, evaluation) bare — exactly as today; behaviour byte-identical
- `NAV-13` (implement, **still-satisfied**): Task List click via configurable entry mode: contract sectionEntry keeps flow lookup + entry resolution; only the dead conditions key is dropped; model/flow.test.js untouched
- `NAV-24` (implement, **still-satisfied**): entry-primitive equivalence on fresh state tested at flow-eval/navigation/section-entry.test.js which calls flow-eval directly with its own options; contract behaviour identical
- `NAV-X2_1` (implement, **still-satisfied**): three-primitives-sufficient: nextAfter still composes flow-eval nextPageAfter + taskGroupOfPage; removing the dead option changes no resolution; next-after.test.js keeps the injectable seam at flow-eval
- `OPEN2-7` (implement, **still-satisfied**): first-declared-page Section entry rides contract sectionEntry -> flow-eval with identical effective options; section-entry.test.js untouched

**Skeptic — strongest objection.** The `conditions` forward makes the contract barrel a lossless facade over flow-eval: a caller can inject an alternative flow-condition registry THROUGH the barrel — the only surface routes are permitted to touch — to gate a swapped-in model/flow.json, which is the portable-data-model / injectable-registry demonstration named in applies-when.js and PLAN.md:259. Dropping it from contract/status.js and contract/navigation.js means the appliesWhen dimension is model-swap-injectable only at the flow-eval unit level, not end-to-end through the barrel; injecting now requires bypassing the barrel, contradicting the "routes are plumbing over the barrel" convention. Because this edits a named paradigm-visible seam and removes a portable-data-model affordance whose demonstrative intent the docs (DESIGN-DECISION documents `evaluate(journey)` without conditions, yet PLAN names the injectable seam as a paradigm goal) leave unsettled, a human should sign off even though behaviour, obligations, tests and parity are all provably untouched.

---

## Dead barrel re-exports across the support-layer barrels (validation, lib/fields, journey)

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** validation/index.js, lib/fields/index.js, journey/index.js

**Current shape.** Three barrels export names with zero consumers via the barrel, verified by grep over the whole spike + prototypes/e2e: validation/index.js:17-23 re-exports saveCheck, mandateMissingCode, checkFormat, decodeDateParts, isBlank, formatCodesFor, toFieldErrors — external consumers import only checkSave (contract/mutation.js:11, routes/page.js via contract), candidateValue (contract/mutation.js:10) and isBlank (contract/cya-rows/_); the tests that use the other five import them from the source files directly (e.g. i18n/resolve.test.js:7-8 imports from '../validation/format-checks.js' and '../validation/save-check.js'). lib/fields/index.js:4-5 re-exports slotToView and attachError — every consumer (contract/view.js:79, tests/helpers/render-page.js:91, templates/_.test.js) uses only slotViews. journey/index.js:2,14 re-exports hubShape and journeyCookieOptions — no file outside journey/ imports either (hub-view/config use hubShape internally; journey-context.test.js:10 imports journeyCookieOptions from './journey-context.js').

**Proposed shape.** Trim each barrel to its consumed surface: validation/index.js exports checkSave, candidateValue, isBlank (plus toFieldErrors only if kept for docs symmetry — currently barrel-unconsumed too); lib/fields/index.js drops the two re-export lines (keeps the import lines 1-2 that slotViews needs); journey/index.js drops hubShape and journeyCookieOptions. All colocated tests keep importing from source files unchanged.

**Rationale.** The paradigm sells 'barrel = the module's public seam'; barrels that also re-export internals nobody consumes blur exactly that demonstration and cost a reader a false wider surface. Pure deletion — no import path anywhere changes.

**Obligations affected.**

- (none of the touched files’ implement/reduce obligations are disturbed; see full cross-check in workflow journal)

**Skeptic — strongest objection.** The spike documents the barrel as an interrogation surface answering "what can this module do" (README:53-59) and explicitly prizes re-exports "by identity" beyond current consumption (README:65), with the barrel set standing as ARCH-6/ARCH-12 architectural evidence. Under that convention, validation/index.js re-exporting the full validation vocabulary and journey/index.js the full journey-shell API is the paradigm demonstration — the barrel advertises the folder-module's complete public API, not just what one cross-folder caller happens to import today (colocated tests reaching into source is the expected white-box pattern, not proof the surface is dead). Trimming each barrel to "currently-consumed cross-folder names" silently redefines the seam from "advertised capability" to "observed usage," eroding exactly the "barrel = public seam" demonstration the candidate claims to sharpen — a paradigm-visible call the PLAN.md Rulings never settle, made worse by the candidate's own hedge over whether toFieldErrors should stay.

---

## Deduplicate the cyaRows test suite pinned twice (view.test.js vs cya-rows/index.test.js)

- **Verdict:** `needs-human-ruling` | **Effort:** M | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** contract/view.test.js, contract/cya-rows/index.test.js

**Current shape.** The cya-rows folder split left two near-identical suites. contract/view.test.js:103-198 ('contract/view — cyaRows (spike-a parity)') and contract/cya-rows/index.test.js:56-128 both pin: the full 18-key parity row list verbatim (view.test.js:106-125 == cya-rows/index.test.js:59-78), the 'Change recent claims' accessible name + own-flow claims/add-on hrefs (view.test.js:144-154 vs index.test.js:91-110), and the soft-prompt shape with the same 'Add at least one claim' / provenance assertions (view.test.js:190-197 vs index.test.js:113-122). Both files also duplicate the FULL fixture and evaluationWith helper (view.test.js:9-48 vs index.test.js:14-50).

**Proposed shape.** Make contract/cya-rows/index.test.js the single home for cyaRows behaviour: move the assertions only view.test.js has (value formatting incl. 'Ford Focus 2018'/'Not provided'/'None'/'0' defaults at view.test.js:126-141 and 156-167, the empty-claims none-row 169-172, add-on Incomplete 174-177, dropped Change actions post-submit 179-188) into the cya-rows suite, then delete the whole cyaRows describe block and the FULL fixture from view.test.js. view.test.js keeps pageViewModel + resolveReasons only. No product code changes; every unique assertion survives exactly once.

**Rationale.** The 18-item spike-a parity row list is load-bearing and currently must be edited in two places for any copy or ordering change; the duplication is a split remnant, not extra coverage — both suites drive the identical cyaRows(evaluationWith(FULL)) path.

**Obligations affected.**

- `STATUS-X1_3` (implement, **still-satisfied**): 'CYA renders soft prompts for missing items before Fulfilled'. Its assertion (soft prompt copy 'Add at least one claim' + provenance 'You answered "yes" for Had claims' + pagePath('claims') href + email prompt present) is DUPLICATED verbatim in cya-rows/index.test.js:113-122, and the no-prompts-when-Full case is at index.test.js:124-127. Deleting the view.test.js copy (190-197) removes only a redundant second pin; behaviour stays fully tested. Also backstopped by e2e/early-cya-access.spec.js.
- `STATUS-33` (implement, **still-satisfied**): 'Make CYA reachable before the Journey is Fulfilled'. cyaRows returning a full/partial row set + prompts for a mid-journey (unsubmitted) evaluation demonstrates early reachability; cya-rows/index.test.js already drives cyaRows(evaluationWith({hadClaims:'yes'})) and cyaRows(evaluationWith(FULL)) and pins prompts. handled_by also lists e2e/early-cya-access.spec.js. No product code touched; coverage relocated, not lost.

**Skeptic — strongest objection.** The "duplication" is partly what keeps the target file inside the paradigm's own ≤150-line folder-module budget, and the dedup forces two paradigm/Ruling-visible concessions. contract/cya-rows/index.test.js is deliberately 128 lines; absorbing the unique view.test.js assertions (value formatting 126-141, absence defaults 156-167, none-row 169-172, add-on Incomplete 174-177, post-submit freeze 179-188 ≈ 48 lines) plus extending its helper lands it at ~176 lines — over the standalone ≤150-line-per-file convention the spike exists to demonstrate. Worse, the post-submit-freeze test (view.test.js:179-188), which is the unit pin for PLAN.md Ruling #1 (post-submit freeze ON), cannot be relocated as a pure move: view.test.js's evaluationWith (line 9) carries a { submit } option calling repository.submit(), but the target index.test.js evaluationWith (line 14) does NOT — the candidate itself flags this. Relocation therefore requires mutating a helper to carry a Ruling-load-bearing assertion, not a mechanical copy. Finally, view.test.js imports cyaRows through the contract barrel (./view.js re-export) while index.test.js imports it direct (./index.js), so deleting the view.test.js block drops the only assertion exercising cyaRows through the "contract barrel + thin routes" seam.

---

## Delete dead PAGE_MANDATES / ENGINE_MANDATES constants

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** engine/mandates.js

**Current shape.** engine/mandates.js:14-15 exports PAGE_MANDATES = ['hard','soft'] and ENGINE_MANDATES = ['mandatory','optional']. Grep across the whole spike, tests, e2e specs and the spike's markdown docs finds zero references — they are not in the engine barrel, not in mandates.test.js, not in index.test.js's pinned surface, and no coverage-matrix obligation names them (the axis domains are already expressed by MANDATE_COMPOSITION itself).

**Proposed shape.** Delete the two export lines. No test, matrix or doc updates needed.

**Rationale.** Fully dead exports: never imported, never tested, never pinned by an obligation. Pure noise in the module that claims to be the mandate-composition demonstration.

**Obligations affected.**

- (none of the touched files’ implement/reduce obligations are disturbed; see full cross-check in workflow journal)

**Skeptic — strongest objection.** engine/mandates.js self-identifies in its docstring as the "two-dimensional mandate composition" demonstration, and PAGE_MANDATES=['hard','soft'] / ENGINE_MANDATES=['mandatory','optional'] are the only place the two orthogonal axis domains are named as first-class sets rather than scattered implicitly across the rows of MANDATE_COMPOSITION. Deleting them strips the named axis-vocabulary from the very module whose stated purpose is to demonstrate those two axes. Whether a deliberately pedagogical spike — one that elsewhere over-declares and pins its demonstration surface (four-rows-exactly, curated barrel, honesty ledger) — should keep documentary vocabulary constants that no code consumes is a paradigm/judgement call the three PLAN.md Rulings do not settle, so it warrants human sign-off rather than an automatic "safe".

---

## Delete the frozen EXPECTED_TYPES list in obligations.test.js — the type-companion contract is already enforced by load-model at runtime

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** model/obligations.test.js

**Current shape.** model/obligations.test.js:57-72 hardcodes EXPECTED_TYPES, a sorted 14-entry list of every `type` string in obligations.json, and obligations.test.js:116-119 asserts the data matches it under the name 'uses only types with a companion in the type registry'. The test never consults the registry — it is a second, manually-maintained copy of the type inventory. The real contract lives in engine/load-model.js:72, which fails loading for any record whose type has no companion, is proven by engine/load-model.test.js:96, and runs against the committed obligations.json on every runtime path (contract/status.js:50, orchestrator/index.js:33) and throughout the test suite (tests/helpers/script-runner.js:30).

**Proposed shape.** Remove EXPECTED_TYPES and the 'uses only types with a companion in the type registry' test from model/obligations.test.js. An unregistered type already fails every contract/orchestrator/invariant test via loadJourneyModel(), and the parity data inventory stays frozen by the EXPECTED_NAMES pin (obligations.test.js:22-53) plus the option-domain and indexedBy pins. Net: -21 lines and one fewer list to hand-extend on every new type, with zero loss of defect-catching power.

**Rationale.** Two mechanisms do the same job: a frozen test list that must be manually edited whenever a type is added, and a runtime registry check that actually verifies the companion exists. The test's name promises the registry check but delivers only the duplicate list, so it adds maintenance friction (every legitimate type addition fails it) while catching nothing load-model does not already catch on the same committed file.

**Obligations affected.**

- `FLOW-26` (reduce, **still-satisfied**): The deleted test ('uses only types with a companion in the type registry', obligations.test.js:116-119) is the one test anchored to FLOW-26, but it never consults the registry — it compares data against a hand-copied EXPECTED_TYPES list. The actual contract survives with strictly stronger coverage: engine/load-model.js:71-73 rejects any record whose type lacks a typeCompanions entry, and this runs on every runtime and invariant path (loadJourneyModel is called by contract/status.js, orchestrator/index.js, tests/helpers/script-runner.js, alignment-walker, completability, flow-eval, validation — 22 files confirmed by grep). engine/load-model.test.js:51-60 asserts every type in the REAL committed catalogue has a companion in the REAL registry (the check the deleted test's name promised but didn't deliver), load-model.test.js:95-97 pins the negative (type 'hologram' → 'no companion in the type registry'), and lib/fields/registry.test.js:53 pins the unknown-type text-input fallback that implements the doc's 'type space stays open' half (obligations.md:1391-1395). Residual honesty note: the frozen equality also incidentally pinned the aggregate parity type inventory (a swap of one registered type for another, e.g. tel→text on phone, would have failed it and will no longer fail any obligations.test.js pin) — but no catalogued implement/reduce obligation mandates freezing the parity type inventory; FLOW-26 itself argues for an open space, and DOC line 194 says type is not even canonically modelled.

**Skeptic — strongest objection.** The change is sold as "zero loss of defect-catching power," but that is false. EXPECTED_TYPES is a set-equality on the used-type inventory, so beyond the (redundant) companion check it uniquely freezes the parity type inventory. `phone` is the only record typed 'tel', and phone-as-'tel' is an explicit parity fact (parity-facts.json:41). Changing phone to 'text' is caught today by EXPECTED_TYPES and by NOTHING else after deletion — load-model passes because 'text' is registered, and no per-record type binding is asserted. So the deletion removes the sole guard against a genuine parity widget regression AND deletes one leg of the catalogue-contract schema-freeze that demonstrates the portable-data paradigm. That is a parity-fact pin and a paradigm-visible seam a human should sign off before removing, not a free win.

---

## Drop repository.has() — a near-duplicate of get() whose one production caller does a has-then-get double lookup

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** store/journey-repository.js, store/journey-repository.test.js, store/index.test.js, journey/journey-context.js, journey/journey-context.test.js

**Current shape.** store/journey-repository.js:69-71 exposes `has(journeyId)` alongside `get(journeyId)`. Its only production caller is journey/journey-context.js:50-52, which calls `repository.has(journeyId)` and then immediately `repository.get(journeyId)` — two Map lookups (plus a structuredClone) to answer one question `get` alone answers. Remaining callers are test conveniences: store/journey-repository.test.js:35,41,105, store/index.test.js:17-18, journey/journey-context.test.js:66,77. No obligation id pins `has` — every store obligation in coverage-matrix.json points at store/journey-repository.test.js as a whole, and the PERSIST/SUBMIT semantics are carried by create/get/saveFulfilments/submit.

**Proposed shape.** Remove `has` from the repository object. In journey-context.js `currentJourney`, replace the has+get pair with a single lookup: `const journey = journeyId ? repository.get(journeyId) : undefined; if (journey) return journey; return startJourney(h, options)`. Rewrite the handful of test assertions to `expect(repository.get(id)).toBeDefined()` / `toBeUndefined()`. Repository API shrinks from 6 methods to 5 with zero behaviour change.

**Rationale.** `has` is `get !== undefined` with a second name; it earns nothing, and its sole production use site actually pays for it twice (redundant lookup + clone-discard). Removing it makes the persistence seam's contract smaller and leaves exactly one way to ask 'does this journey exist'.

**Obligations affected.**

- `DEF-21` (implement, **still-satisfied**): Fulfilments map scoped to journey keyed by obligation id — handled_by includes journey-context.js and store/journey-repository.js, test store/journey-repository.test.js. The load-or-create path still resumes/creates the same journey document; get() returns the same journey-scoped fulfilments. No change to scoping semantics.
- `DEF-22` (implement, **still-satisfied**): Concurrent-journey isolation (journey, not user/session, is the uniqueness scope). currentJourney still resumes exactly the cookie's journey or mints a fresh one; the never-shared-between-cookie-less-requests test (journey-context.test.js:81-86) exercises the changed path and its assertion is unchanged. Isolation preserved.
- `ARCH-16` (implement, **still-satisfied**): Zero-many concurrent journeys per Flow. Same handled_by trio; the has→get consolidation does not alter how many journeys exist or which one a request resolves to. Test store/journey-repository.test.js unaffected.

**Skeptic — strongest objection.** The store is the DOC's explicitly-named "journey persistence seam" and the spike's deliberately non-precedential demonstration of a datastore contract (its docstring calls the real successor an urgent, precedent-sensitive Defra-wide decision). Its API deliberately mirrors JS Map (create/get/has/clear), and has/get model the real-datastore distinction between a cheap existence-probe and a full document load-plus-deep-copy. Dropping `has` shrinks that demonstrative contract surface and bakes "always fetch-and-clone the whole document to check existence" into the seam meant to inform the real datastore choice — and it does so by rewriting assertions inside store/journey-repository.test.js, the single test anchoring ~40 store obligations. Behaviour is genuinely preserved, so this is sound engineering, but it trades away a facet of a paradigm-visible seam and reshapes an obligation-bearing test tier — a call a human should ratify, not an automated simplifier.

---

## Drop the one-line cya-rows barrel and the double re-export hop into the contract barrel

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** contract/cya-rows/index.js, contract/view.js, contract/cya-rows/index.test.js

**Current shape.** contract/cya-rows/index.js is a 9-line barrel whose entire content is `export { cyaRows } from './assemble.js'`. cyaRows then hops again through contract/view.js:18 (`export { cyaRows } from './cya-rows/index.js'`) before reaching the pinned barrel at contract/index.js:18 — three files to trace one function. Consumers of the folder barrel: view.js:18 and the colocated test at cya-rows/index.test.js:5 only.

**Proposed shape.** Delete contract/cya-rows/index.js. contract/view.js re-exports from './cya-rows/assemble.js' directly (keeping the [view] grouping the PLAN.md/README surface tables document), and the colocated suite imports './assemble.js' (optionally renamed cya-rows.test.js/assemble.test.js at the implementor's discretion). The 21-export barrel surface and index.test.js SURFACE list are untouched.

**Rationale.** The folder-module convention caps file size; it does not require a barrel per folder, and a barrel re-exporting a single symbol from a sibling adds an indirection level with zero fan-in benefit — assemble.js already composes page-rows.js and format-value.js internally.

**Obligations affected.**

- `STATUS-X1_3` (implement, **still-satisfied**): CYA soft-prompt rendering. Its handled_by is contract/view.js and its NAMED test is contract/view.test.js, which imports cyaRows from './view.js' (line 2), not from the deleted barrel. view.js still re-exports cyaRows (now from './cya-rows/assemble.js', same function). missingPrompts flow through cyaRows() in assemble.js is untouched. Behaviour + named test unchanged.
- `STATUS-33` (implement, **still-satisfied**): Early-reachable CYA with soft prompts (Rulings item 2). Backed by cyaRows()->missingPrompts in assemble.js and the early-cya-access.spec.js e2e which reaches CYA through routes -> contract/index.js barrel (21-export surface unchanged, cyaRows still pinned). The barrel deletion changes only an internal re-export hop; the reachable behaviour is identical.

**Skeptic — strongest objection.** The `contract/cya-rows/index.js` barrel is not unearned indirection — it is one instance of a paradigm-visible convention the spike demonstrates uniformly: every folder-module presents an `index.js` barrel as its single public face (16 barrels, including nested ones at `engine/scope/`, `flow-eval/navigation/`, `orchestrator/apply-answers/`, `routes/claims/`, `lib/quote/`). PLAN.md explicitly lists thin single-folder barrels as intentional paradigm line items — `i18n/index.js` (8 lines), `lib/quote/index.js` (8 lines), `engine/scope/index.js` (15 lines) — each the identical shape (a near-empty re-export fronting one folder-module) as the cya-rows barrel the candidate calls "zero fan-in benefit." The barrel is the self-containment demonstration: `contract/cya-rows/` hides its `page-rows.js`/`format-value.js`/`assemble.js` composition behind one name (`cyaRows`), so consumers import the folder, never its internals. Deleting it repoints `view.js` at `./cya-rows/assemble.js`, leaking an implementation-file name as the folder's public entry and singling out one folder-module to break the convention its siblings all follow — exactly a "seam that looks unearned in a one-journey spike but exists to demonstrate the paradigm."

---

## Drop the unconsumed change-tracking outputs (changed/ran, spawned/dropped) from the fixed-point sub-passes

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** orchestrator/system-handlers.js, orchestrator/system-handlers.test.js, orchestrator/scope-exit-wipe.js, orchestrator/scope-exit-wipe.test.js

**Current shape.** system-handlers.js:73-77 runs a per-record isDeepStrictEqual purely to compute a `changed` flag, and collects `ran`, returning { fulfilments, changed, ran } (line 80) — but fixed-point.js:59-65 ignores both and performs its own authoritative whole-map isDeepStrictEqual at line 65 as the sole convergence check. Likewise reconcileDerived (scope-exit-wipe.js:77-87) returns spawned/dropped arrays that fixed-point.js:56 discards (only .fulfilments is used). Grep confirms no consumer of any of these fields in tests/, contract/ or routes/ — only each module's own unit test asserts them. The paradigm-visible audit trail (drops, wiped) is separate and IS surfaced through commit and contract/mutation.js:97-100.

**Proposed shape.** createSystemHandlerRun's runner writes next[record.id] = { value } unconditionally and returns { fulfilments } (drop changed/ran and the node:util import); reconcileDerived returns { fulfilments } (drop the spawned/dropped accumulators). Rewrite the affected unit-test assertions to check map contents instead of the flags (e.g. 'reports no change when stable' becomes expect(second.fulfilments).toEqual(first.fulfilments), already half-asserted at system-handlers.test.js:51; the dedupe test already uses vi.fn call counts). Convergence semantics unchanged: the inFlight dedupe plus fixed-point's own comparison, pinned at fixed-point.test.js:125-151, are untouched.

**Rationale.** Two change-detection mechanisms do the same job: the runner's per-record deep-equal duplicates the loop's whole-map deep-equal one line downstream, and the winner is always the loop's. spawned/dropped mimic the 'wipes as data' idea but no caller ever logs or surfaces them (unlike wiped, which flows to the contract). Instrumentation nobody reads is weight without signal; the wipe/spawn behaviour itself stays fully pinned via fulfilments assertions and the INDEX-19..21 tests.

**Obligations affected.**

- `INDEX-19` (implement, **still-satisfied**): reconcileDerived spawn behaviour ('fresh blank fulfilment when controller adds an id'). Removing the returned `spawned` array is safe: scope-exit-wipe.test.js:58-70 already pins the spawn via map content (expect(fulfilments['id-starter']).toEqual({starter:{value:''}})). The mutation itself is unchanged; only the redundant audit array assertion is dropped.
- `INDEX-20` (implement, **still-satisfied**): Drop-and-wipe on controller removal. Pinned by map content at scope-exit-wipe.test.js:84-97 (fulfilments['id-starter'] toBeUndefined). Dropping the `dropped` array leaves the delete-and-wipe mutation and its map assertion intact.
- `INDEX-21` (implement, **still-satisfied**): No-rehydration on re-add (Yes-No-Yes). Pinned by the re-select test (test.js:107-123) asserting a FRESH blank via map content, not via spawned/dropped. Untouched by the field removal.
- `FULF-11` (implement, **still-satisfied**): 'Adding a controller value spawns a blank derived fulfilment' — same reconcileDerived spawn as INDEX-19; behaviour preserved, coverage relocated onto the map-content assertion that already exists.
- `FULF-12` (implement, **still-satisfied**): 'Removing a controller value drops and wipes' — same as INDEX-20; map-content assertion (toBeUndefined) carries the coverage after the `dropped` array assertion is removed.
- `EVAL-38` (implement, **still-satisfied**): 'Use fulfilments as the only cache for system-handler results.' Proposed runner still writes results into the unified fulfilments map (next[record.id]={value}); test.js:25/43 assert the priced value in the map. The `changed` flag is orthogonal to caching semantics.
- `OPEN1-20` (implement, **still-satisfied**): 'Store system-handled fulfilments in the unified map.' Unified-map write unchanged; map assertions preserved. `changed`/`ran` played no role.
- `OPEN1-22` (implement, **still-satisfied**): 'Treat system-handled obligations as first-class.' Firing/dedupe/out-of-scope behaviour is pinned via fulfilments map contents (test.js:25,62) and vi.fn call counts (test.js:82-92), none of which depend on the discarded flags.
- `OPEN1-23` (implement, **still-satisfied**): Per-field (not per-source) serialisation via unified-map write with no metadata — unchanged by removing change-tracking outputs.
- `OPEN1-25` (implement, **still-satisfied**): 'Ship MVP without per-fulfilment metadata.' The `{value}`-only write is retained (and made unconditional); no metadata added or removed.
- `EVAL-4` (implement, **still-satisfied**): Handler fires while in scope during the fixed-point pass; asserted via priced map value and the never-fires-out-of-scope test (fulfilments[premiumId] toBeUndefined at test.js:62). The redundant `ran:[]` assertion at line 63 is the only thing removed.
- `EVAL-X1_3` (implement, **still-satisfied**): System-handler dereference/write path on system-handlers.js, tested by system-handlers.test.js; behaviour is verified through map contents and call counts, not the change-tracking flags.
- `SHAPE-X1_3` (implement, **still-satisfied**): Persist lookup id as canonical data, orchestrator dereferences on demand; the unified-map write demonstrating this is untouched, only the changed/ran instrumentation goes.

**Skeptic — strongest objection.** This is behaviour-neutral engineering that erodes a paradigm-visible "mutations-as-data" seam and edits the return contracts of sub-passes guarded by a protected quirk — both require human (specifically Sam's) sign-off. (1) scope-exit-wipe.js is built as a deliberate symmetry: its docstring thesis is "returning the wipes as data" (lines 6-9), and reconcileDerived surfaces spawn/drop mutations in the SAME record shape ({obligationId, name, fulfilmentId}) that wipeOutOfScope surfaces wiped. The candidate keeps `wiped` (it reaches the contract) but strips `spawned`/`dropped` from its sibling in the same file — collapsing a uniform three-way "every lifecycle mutation is a first-class data record" demonstration into an asymmetric one-off. In a one-journey spike that seam exists to demonstrate the pattern, not to feed a live consumer; "nobody reads two of the three today" is exactly the unearned-seam trap. The `ran` array is likewise the literal embodiment of DOC obligations.md:516-518 / EVAL-41 "audit trail is first-class... ran fraud-flag check at 10:03" — a deferred-obligation demonstration seam someone built on purpose. (2) fixed-point.js carries the protected "EVAL-35 — TODO review with Sam" tag and its docstring states it "ships as INPUT to that review, not fait accompli" (lines 9-12). The return contracts of system-handlers and scope-exit-wipe ARE part of that unreviewed sketch; reshaping them pre-empts the review the tag reserves for Sam, even though the pinning test at fixed-point.test.js:14-18 still passes because the file's text is untouched.

---

## Duplicate isBlank in routes/claims diverges from the canonical validation/isBlank

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** routes/claims/page-model.js, routes/claims/view-models.js

**Current shape.** validation/format-checks.js:13-24 defines the canonical save-time isBlank (handles null, strings, arrays, part-objects) and it is already the spike-wide blankness authority — imported by contract/cya-rows/page-rows.js:3, contract/cya-rows/format-value.js:1. Meanwhile routes/claims/page-model.js:20-21 defines a second, weaker isBlank (`value === undefined || String(value).trim() === ''`) consumed only by routes/claims/view-models.js:5,29 — for the exact same 'blank amount -> label only' rendering that page-rows.js:55 does with the validation one. The two disagree on null (String(null) === 'null' → NOT blank) and arrays.

**Proposed shape.** Delete the local isBlank from routes/claims/page-model.js:20-21; have routes/claims/view-models.js import isBlank from '../../validation/index.js' like the cya-rows files already do. No behaviour change for the claims list (amount values there are strings/undefined) but one blankness definition spike-wide.

**Rationale.** Two near-identical blankness mechanisms with silently divergent semantics is drift waiting to happen; validation/isBlank is already the exported, tested, obligation-pinned (TOOL-13) definition and the claims copy adds nothing.

**Obligations affected.**

- `TOOL-13` (reduce, **still-satisfied**): The canonical isBlank lives in validation/format-checks.js (handled_by for TOOL-13) which the proposal does NOT touch. Its unit test format-checks.test.js:136-139 (isBlank([]) etc.) is untouched. The change only redirects a caller to this same authority.
- `TOOL-14` (reduce, **still-satisfied**): TOOL-14 is the hand-built presentsForEach template loop; claimRows() in view-models.js IS that loop. Its map/output is unchanged for string/undefined amounts (the only values claimAmount can hold), so the 'Claim N — £amount' vs 'Not provided' output is byte-identical. Tested by lib/fields/index.test.js and covered by routes/claims/index.test.js assertions, both still pass by construction.

**Skeptic — strongest objection.** The candidate's proposed import target — `import { isBlank } from '../../validation/index.js'` in routes/claims/view-models.js — pierces the spike's signature paradigm seam: "contract barrel + thin routes." contract/index.js:6-8 documents the contract barrel as "the ONLY module routes import," and this is empirically exact — every cross-folder import from routes/ resolves to just two barrels, contract/index.js and journey/index.js; there is not a single routes→validation import in the tree. The proposal would introduce the first one, letting the routes layer reach past the contract barrel into a deeper folder-module. The candidate defends this as "the same cross-folder pattern cya-rows already uses," but that analogy is false: contract/cya-rows/page-rows.js lives INSIDE the contract folder, so its import of validation is contract-internal machinery, not routes-layer behaviour. In a one-journey paradigm spike, the deliberately-minimal local isBlank in page-model.js plausibly exists precisely to keep the claims routes folder self-contained off the validation layer — exactly the kind of "unearned-looking seam that demonstrates the paradigm" the brief warns about.

---

## Fold routes/endings/submit.js into check-your-answers.js — CYA is the only surface whose GET and POST live in different files

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** routes/endings/submit.js, routes/endings/check-your-answers.js, routes/endings/index.js

**Current shape.** routes/endings/submit.js is 27 lines whose only code is the 8-line `submitCheckYourAnswers` (lines 20-27) plus its own flow re-parse (line 18); it exists solely to host the CYA POST, forcing check-your-answers.js to export `renderCya` (check-your-answers.js:28) as a cross-file seam with exactly one external consumer (submit.js:3,26). Every other surface pairs its GET and POST in one file: quote-summary.js hosts getQuoteSummary + submitQuoteSummary, page.js hosts both generic handlers, claims/handlers.js hosts all five. The parity twin does the same: spike-a/routes/endings has no submit.js — its CYA POST lives inside check-your-answers.js (spike-a check-your-answers.js:71-77).

**Proposed shape.** Move submitCheckYourAnswers (and its valuable graft-3 doc-comment about the stale-recheck 200) into routes/endings/check-your-answers.js, make renderCya file-private again, update the import in routes/endings/index.js:6, and delete submit.js. Merged file lands ~70 lines, well under the 150-line folder-module ceiling. submit.test.js keeps its name and passes unchanged (it imports only test-server). One matrix path (routes/endings/submit.js in SUBMIT-4/5/6/10/18 etc. handled_by lists) folds into check-your-answers.js — the matrix already tolerates this class of drift per its predates-split note.

**Rationale.** The file split earns nothing: it created a one-consumer export (renderCya), a duplicate flow parse, and an inconsistency with every sibling surface AND with the spike-a file it is meant to diff against. Folding it restores parity and removes a seam nobody varies.

**Obligations affected.**

- `DEF-31` (implement, **still-satisfied**): Freeze-after-submit. The actual freeze is enforced by store/journey-repository.js, contract/submit.js and routes/guard.js (none touched). submit.js is only the CYA POST handler; relocating submitCheckYourAnswers verbatim into check-your-answers.js preserves the submit() delegation and confirmation redirect. Test store/journey-repository.test.js untouched; the route-level submit.test.js POSTs /check-your-answers and passes unchanged. handled_by lists routes/endings/submit.js, which becomes a stale path (matrix predates-split note tolerates this drift) — non-blocking.
- `SUBMIT-4` (implement, **still-satisfied**): Fulfilled->Submitted flip on CYA submit. Logic in contract/submit.js + store/journey-repository.js (untouched); test store/journey-repository.test.js untouched. The route handler that triggers it moves file but keeps identical body (submit(journey) then redirect). handled_by path submit.js now stale but non-blocking.
- `SUBMIT-5` (implement, **still-satisfied**): Persisted doc status flip to submitted — same as SUBMIT-4: behaviour in contract/store, handler merely relocated, journey-repository.test.js unchanged.
- `SUBMIT-6` (implement, **still-satisfied**): submittedAt timestamp on submit — set in store/journey-repository.js + contract/submit.js (untouched); confirmation.js (also handled_by) untouched. Handler relocation is behaviour-neutral.
- `SUBMIT-10` (implement, **still-satisfied**): Exclude post-submit extension points from MVP — an absence obligation honoured by contract/submit.js + store; folding the route handler adds nothing and removes nothing. Test journey-repository.test.js unchanged.
- `SUBMIT-18` (implement, **still-satisfied**): MVP submit limited to state flip/timestamp/immutability/read-only CYA — enforced across store, contract/submit.js, contract/guards.js (untouched). The route handler relocation preserves the redirect-on-ok / renderCya-on-stale surface. journey-repository.test.js unchanged.
- `SUBMIT-X1_1` (implement, **still-satisfied**): Adopt extension points only on concrete demand — same store/contract enforcement, handler-only relocation, journey-repository.test.js unchanged.
- `STATUS-35` (implement, **still-satisfied**): Server-side Journey-Fulfilled re-check on CYA submit (obligations.md quote: 'On CYA Submit: the Engine re-checks Journey-Fulfilled; if yes, transition to Submitted. If no, re-render CYA with the missing items called out.'). The re-check is submit() in contract/submit.js + contract/guards.js (untouched); the handler that calls it and branches (redirect vs renderCya) moves file verbatim. Test contract/submit.test.js untouched; submit.test.js proves the never-reaches-confirmation and redirect branches over POST /check-your-answers and passes unchanged.
- `STATUS-X1_5` (implement, **still-satisfied**): Re-render CYA with missing items when submit re-check fails (the graft-3 stale-recheck 200, never a 500/redirect-elsewhere). This is exactly submitCheckYourAnswers' else branch: renderCya(result.evaluation, h, {errorSummary}). Moved verbatim (with its doc-comment) into check-your-answers.js — renderCya is co-located there, so the seam disappears rather than breaking. submit.test.js 'stale gap' case (200 + govuk-error-summary + 'Add at least one claim') imports only test-server and passes unchanged. Behaviour and Rulings-item-2 stale branch preserved.
- `NAV-36` (implement, **still-satisfied**): Submitted is final — enforced by store, contract/submit.js, contract/guards.js, routes/guard.js (all untouched). submit.test.js re-POST-of-submitted case relies on routes/guard.js intercepting before the handler, which is path/method-based and independent of the handler's host file. Relocation is behaviour-neutral.

**Skeptic — strongest objection.** The fold is presented as "restoring parity with spike-a", but submit is precisely the surface where this spike is DESIGNED to diverge from spike-a (Ruling 1 freeze = Outcome A; spike-a unfrozen = dead Outcome B), so the parity rationale is inverted. And routes/endings/submit.js is not an accidental seam — PLAN.md:333 plans it as a distinct module with its own 10-obligation set and its own test, DESIGN-DECISION.md:120,167 names it as one of four ending surfaces, and it is the designated home of Ruling 2's hard gate plus the first-class graft-3 stale-recheck. Folding it desyncs PLAN.md's module ledger and 10 coverage-matrix rows into a non-mechanically-resolvable dead path — a drift class the "predates-split" note (file→folder of the same stem) does not actually license.

---

## Fold six copies of model-read + page-walk boilerplate into one tests/helpers module

- **Verdict:** `needs-human-ruling` | **Effort:** M | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** tests/helpers/model.js, tests/alignment-walker.test.js, tests/reachability.test.js, tests/completability.test.js, tests/flow-equivalence.test.js, tests/rename-survival.test.js, tests/helpers/enumerate-states.js

**Current shape.** Identical plumbing is copy-pasted across the tiers and helpers: `collectPages` is verbatim-duplicated at tests/alignment-walker.test.js:40-49 and tests/reachability.test.js:30-39 (with a near-twin `collectAppliesWhen` at reachability.test.js:41-49); the fs+path+fileURLToPath+JSON.parse model-file read is repeated in alignment-walker.test.js:27-37, completability.test.js:27-37, reachability.test.js:14-21, flow-equivalence.test.js:17-23, rename-survival.test.js:21-28 and helpers/enumerate-states.js:17-24 — six independent dirname computations reading the same model/ folder.

**Proposed shape.** Add tests/helpers/model.js exporting `readModel(file)` (pure fs+JSON, no engine import, so tier 1 stays a 'pure JSON walk') and the one `collectPages(container)` walker; each tier replaces its local copy with an import. Assertions and tier semantics are untouched — only the load/walk plumbing moves. ~50-60 duplicated lines collapse to one ~20-line helper.

**Rationale.** Six copies of the same fs/dirname/JSON boilerplate and two byte-identical tree walkers demonstrate nothing about the paradigm — the tiers' value is their assertions, not their file-loading. The duplication is pure maintenance surface: a model-path change today means six edits. touches_protected because the edited files ARE the invariant tiers, even though no assertion changes.

**Obligations affected.**

- (none of the touched files’ implement/reduce obligations are disturbed; see full cross-check in workflow journal)

**Skeptic — strongest objection.** rename-survival (Tier 5, the flagship SHAPE-6/PERSIST-24 dual-identifier proof) depends on `catalogue()` re-parsing obligations.json on every call so its in-place `.name` mutations get a fresh, private copy. That freshness guarantee is currently inline and tamper-obvious. Folding it into a shared helpers/model.js `readModel` relocates a load-bearing "must never memoise / must return a deep-fresh copy" contract into a file whose name promises only "read the model," where the obvious future optimization (memoise the parse) would silently share mutable state across all tiers and corrupt the flagship proof. Compounding this, the "six identical copies" are actually four different contracts (whole-object read, inline one-shot, variadic model+fixtures reader, per-call fresh `.obligations` function — flow-equivalence's variadic reader must survive anyway), so the fold is partial and erodes the independent-oracle property (flow-equivalence:14, "a bug shared by both cannot certify itself") that the paradigm-load-bearing tiers exist to demonstrate.

---

## Fold the one-consumer apply-answers/index.js barrel into direct imports

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** apply-answers/index.js, orchestrator/index.js

**Current shape.** orchestrator/apply-answers/index.js is a 14-line pure re-export barrel over field-names.js, page-answers.js and indexed-collection.js. Its sole importer is orchestrator/index.js (lines 3-8 and the re-export at line 25); every test in the folder already imports the source files directly, and nothing outside orchestrator/ reaches into apply-answers at all.

**Proposed shape.** Delete apply-answers/index.js. orchestrator/index.js imports applyAnswers from './apply-answers/page-answers.js', addFulfilment/markCollectionReviewed/removeFulfilment from './apply-answers/indexed-collection.js', and re-exports encodeFieldName from './apply-answers/field-names.js'. The folder-module and its <=150-line files are unchanged; the paradigm's load-bearing barrel is contract/index.js, which this does not touch.

**Rationale.** A barrel with exactly one consumer earns nothing: it adds a hop to every read of the write path and hides which file owns which function. The standalone conventions mandate a contract barrel, not internal sub-barrels — orchestrator/index.js is itself the folder's public face, so an inner barrel is double indirection.

**Obligations affected.**

- `DEF-4` (implement, **still-satisfied**): Orchestrator-wraps-pure-engine lives in the settle/commit functions of orchestrator/index.js and engine/\*, none of which change; only an import specifier moves. Test engine/scope/journey-rules.test.js untouched.
- `DEF-12` (implement, **still-satisfied**): Handled by page-answers.js logic (via the apply-answers folder alias) + model/engine; the barrel carries no logic. Test model/obligations.test.js imports nothing from the barrel.
- `DEF-13` (implement, **still-satisfied**): Behaviour is in engine/load-model.js + apply-answers source files, all unchanged; test engine/load-model.test.js unaffected.
- `DEF-14` (implement, **still-satisfied**): Handled by fulfilment-ids.js + indexed-collection.js (which imports fulfilment-ids.js directly, not the barrel); unchanged.
- `DEF-21` (implement, **still-satisfied**): Store + apply-answers write path unchanged; store/journey-repository.test.js does not touch the barrel.
- `DEF-38` (implement, **still-satisfied**): Store + write-path behaviour unchanged; only the import route into page-answers.js/indexed-collection.js changes.
- `SHAPE-14` (implement, **still-satisfied**): Handled by load-model + apply-answers source modules; barrel deletion does not alter canonicalise-and-write behaviour.
- `SHAPE-21` (implement, **still-satisfied**): Convergent-write propagation lives in page-answers.js/canonical-value.js; matrix test 'orchestrator/apply-answers.test.js' = the folder's page-answers.test.js etc., which import source files directly (grep-verified, no test imports the barrel).
- `EVAL-1` (implement, **still-satisfied**): evaluateObligations call in applyPageAnswers (orchestrator/index.js:61) is untouched; only lines 3-8/25 import specifiers change.
- `EVAL-4` (implement, **still-satisfied**): Fixed-point/system-handler wiring in commit() and fixed-point.js unchanged; fixed-point.js (a protected quirk) is not touched by this candidate.
- `EVAL-19` (implement, **still-satisfied**): Prune-on-write path via runToFixedPoint in commit() unchanged; engine/prune.test.js and tests/rename-survival.test.js unaffected.
- `EVAL-31` (implement, **still-satisfied**): Render-then-await-input spans applyAnswers (page-answers.js) + applyPageAnswers (orchestrator/index.js) + contract/view.js encodeFieldName import — the contract imports from orchestrator/index.js, which still exports encodeFieldName under the proposal.
- `INDEX-1` (implement, **still-satisfied**): Single-cardinality handling is in model + engine + page-answers.js; no logic in the barrel (DOC 413-423 describes fulfilment shape, not file layout).
- `INDEX-2` (implement, **still-satisfied**): Handled by load-model + evaluate + apply-answers sources, all unchanged.
- `INDEX-21` (implement, **still-satisfied**): No-rehydration-on-re-added-id lives in scope-exit-wipe.js + removeIndexedFulfilment/addIndexedFulfilment entry points, whose bodies are unchanged; e2e invalidation.spec.js drives the same routes.
- `FULF-4` (implement, **still-satisfied**): Engine + apply-answers source behaviour unchanged; engine/evaluate.test.js unaffected.
- `FULF-6` (implement, **still-satisfied**): Same as FULF-4 — engine + source modules unchanged, test unaffected.
- `FULF-7` (implement, **still-satisfied**): Opaque-id plumbing is fulfilment-ids.js + indexed-collection.js (direct source import '../fulfilment-ids.js'); barrel not on this path.
- `FULF-8` (implement, **still-satisfied**): DOC 542-543 mandates orchestrator-minted opaque ids — minted in fulfilment-ids.js, threaded via addFulfilment in indexed-collection.js and addIndexedFulfilment in orchestrator/index.js; the proposal imports addFulfilment from indexed-collection.js directly, same function, same tests (fulfilment-ids.test.js, e2e).
- `FULF-9` (implement, **still-satisfied**): Removal-by-shared-id via removeFulfilment: same function relocated import, removeIndexedFulfilment body unchanged, orchestrator/index.test.js imports it from './index.js' where it remains defined.
- `FLOW-10` (implement, **still-satisfied**): DOC 1083-1085 partial-save behaviour is in page-answers.js write semantics + routes; unchanged, folder tests import sources directly.
- `STATUS-30` (implement, **still-satisfied**): DOC 1265-1267 area: write-then-recalculate on POST is the commit() invariant (write -> runToFixedPoint -> save) in orchestrator/index.js lines 40-56, untouched by the import rewiring.
- `SUBMIT-7` (implement, **still-satisfied**): DOC 823-824 storage-layer freeze (PLAN.md Ruling 1) is repository.saveFulfilments rejecting submitted journeys — store/journey-repository.js + commit(), neither touched; post-submit-freeze.spec.js e2e unaffected.
- `ARCH-11` (implement, **still-satisfied**): Orchestrator-as-sole-side-effecting-wrapper is exactly what orchestrator/index.js's commit() enforces; deleting an inner pure re-export barrel changes no side-effect routing — all mutating paths still flow through commit.

**Skeptic — strongest objection.** The candidate's load-bearing premise is factually false: it asserts "the standalone conventions mandate a contract barrel, not internal sub-barrels — an inner barrel is double indirection." But the spike demonstrably runs on internal, few-consumer folder-module barrels _everywhere_, including nested ones. engine/scope/index.js and flow-eval/navigation/index.js are sub-folder-modules WITHIN engine/ and flow-eval/, each fronted by its own index.js barrel; engine/index.js is even a protected "doc-shaped barrel" [graft]. DESIGN-DECISION.md lines 73-112 show the paradigm as a self-similar tree where every folder-module (engine, flow-eval, orchestrator, store, journey, contract, validation, i18n, lib/fields, lib/quote, plus the nested scope/ and navigation/) presents through an index.js. apply-answers/index.js is therefore NOT an anomalous unearned hop — it is one instance of the pervasive folder-module-has-a-barrel convention that a _paradigm demonstration_ spike exists to make legible. Deleting it makes apply-answers the SINGLE folder-module in the whole spike reached through deep file paths (./apply-answers/page-answers.js, ./indexed-collection.js, ./field-names.js) from orchestrator/index.js — breaking the self-similarity that is the deliverable. Worse, the barrel is CURATED, not a dumb pass-through: it re-exports field-names/page-answers/indexed-collection but deliberately omits canonical-value.js, encoding the folder's public/private boundary. The rationale's "the barrel carries no logic" misreads this — it carries no runtime logic but it carries the folder's API boundary and the module-level rename-survival seam (rename page-answers.js → touch only the barrel today; touch orchestrator/index.js, the risk-7 write-invariant module, after the change).

---

## Hardcoded 'There is a problem' fallback in error-summary.njk duplicates flow.defaults and is dead on every product path

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** templates/partials/error-summary.njk, templates/partials/error-summary.test.js

**Current shape.** templates/partials/error-summary.njk:8 renders `titleText: errorSummaryTitle or "There is a problem"`. Every product render path already supplies errorSummaryTitle from the model: contract/view.js:76 (pageViewModel → flow.defaults.errorSummaryTitle, model/flow.json:8), routes/endings/check-your-answers.js:43, routes/claims/handlers.js:57. The only thing exercising the fallback is the test written for it — templates/partials/error-summary.test.js:22-28 ('falls back to the standard GDS title when none is given'). Grep across the spike + e2e confirms no caller omits the title.

**Proposed shape.** Change line 8 to `titleText: errorSummaryTitle` and delete the fallback test case in error-summary.test.js (keep the other two cases: renders-with-links, renders-nothing-when-empty). One source of that copy remains: model/flow.json defaults, threaded through the contract/routes as everywhere else.

**Rationale.** Two sources for the same copy — model data (always supplied) plus a template literal (never reached in product) — is exactly the near-duplicate mechanism the portable-data-model convention forbids: page.njk:5 boasts 'Zero hardcoded copy' yet the partial it includes carries a hardcoded English string. No obligation in the reverse index needs a template-side default (TOOL-13/ARCH-35/FLOW-7/FLOW-8 all concern the round-trip summary itself), and the only pin is the test that exists to pin the fallback. Removing it repairs the layer's own claim at trivial cost.

**Obligations affected.**

- `FLOW-8` (implement, **still-satisfied**): DOC (obligations.md:1341-1343) requires the blocked page to 're-render with a GDS error summary + per-field errors'. The GDS summary is govukErrorSummary in error-summary.njk; its titleText is still supplied on every re-render path from flow.defaults.errorSummaryTitle ('There is a problem'). Both re-render callers verified: page-POST-blocked (routes/page.js:101-105, viewContext spreads ...viewModel which sets errorSummaryTitle at contract/view.js:76, then only adds errorSummary via extras) and CYA-POST stale-recheck (routes/endings/submit.js:26 -> renderCya, extras only add errorSummary, base title at check-your-answers.js:43 survives). Rendered title is unchanged; the removed 'or "There is a problem"' literal was never reached. Test coverage lives in model/flow.test.js plus e2e/obligations/mandate-composition.spec.js which drive the real product path (model-supplied title), not the deleted unit fallback case.
- `ARCH-35` (implement, **still-satisfied**): 'Enforce hard mandates at Save and continue' — the user-visible face is the error summary re-render. pageViewModel (contract/view.js:76) unconditionally sets errorSummaryTitle for the fullName-on-gate re-render, so the summary still renders with its title after removing the dead template default. Test pin is engine/mandates.test.js + e2e mandate-composition.spec.js, both untouched and both exercise the model-supplied title, not the removed literal.
- `FLOW-7` (implement, **still-satisfied**): 'Block Save and continue on unfulfilled in-scope hard mandates' (DOC:1341-1342). The blocking logic is in engine/mandates.js + validation/save-check.js; error-summary.njk only renders the resulting summary. Removing the unreachable fallback does not touch the block decision or the rendered title (still supplied from flow.defaults on the routes/page.js re-render). Test pin model/flow.test.js untouched.
- `STATUS-29` (implement, **still-satisfied**): 'Validate hard mandates on Page POST' (DOC:1341). Validation happens in validation/save-check.js before the error-summary partial renders. The title-default deletion is presentation-only and behaviour-preserving; the summary still shows 'There is a problem' via the model on the POST re-render. Test pin engine/mandates.test.js untouched.
- `TOOL-13` (reduce, **still-satisfied**): Reduced obligation (no generator built; hand-written templates follow generator-shaped conventions). The hand-built partial still emits the GDS error summary with model-threaded title; dropping a template literal that duplicates model/flow.json is fully consistent with the 'follow the generator-shaped conventions' reduction and does not lose the wired-validation behaviour, which is pinned by validation/format-checks.test.js (untouched).

**Skeptic — strongest objection.** The change's entire justification is a category error, and stripping it away turns the change from a 'repair' into a net loss of a paradigm seam. 'There is a problem' is the govuk-frontend error-summary component's canonical FRAMEWORK default, not journey copy — the portable-data-model convention (and page.njk's 'Zero hardcoded copy' boast) governs journey knowledge (labels/hints/headings/reasons in model/\*.json), not GDS framework defaults baked into a shared GDS partial, which is exactly what a generator-shaped TOOL-13-reduced partial would carry. So there is no near-duplicate to collapse. Meanwhile error-summary.njk:3 documents the title as optional ('errorSummaryTitle?') precisely so the reusable partial defends its own GDS-valid, a11y-titled output regardless of caller; line 8 is that backstop. The candidate deletes the backstop, leaves line 3's optional-contract comment lying (edits only line 8), and simultaneously deletes the one test that guards the branch — so any future page/route that forgets the wiring silently ships a title-less error summary with nothing to catch it. The 'benefit' is illusory; the cost is a self-containment seam plus a test-tier branch — a convention-interpretation and paradigm-seam judgement a human must rule on, not an auto-'safe'.

---

## Inline the one-caller obligationStateOver seam into mutation.js

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** contract/status.js, contract/mutation.js, contract/status.test.js

**Current shape.** contract/status.js:90-97 exports obligationStateOver — a thin adapter that defaults obligations from journeyModel(), destructures scopeRegistry/externalState, and returns evaluateObligations(...).obligations. Its only product caller is contract/mutation.js:76 (candidateState); the only other references are its own pin at status.test.js:133-139. It is not on the 21-export barrel surface and no route or e2e file touches it (verified spike-wide + prototypes/e2e).

**Proposed shape.** Delete the export from status.js; candidateState in mutation.js calls evaluateObligations from '../engine/index.js' directly (obligations ?? journeyModel().obligations, passing scopeRegistry/externalState). Drop the status.test.js:133-139 pin — the same-POST reveal in/out-of-scope behaviour is already behaviour-pinned through checkSave at mutation.test.js:38-67, so no coverage is lost. status.js JSDoc line 86-89 ('the candidate seam') goes with it.

**Rationale.** A named, exported, individually-tested 'seam' with exactly one consumer earns nothing over a direct call — it adds an export, a doc block and a test that restate what the checkSave tests already prove on real inputs.

**Obligations affected.**

- `FLOW-1` (implement, **still-satisfied**): Orthogonal page/Engine mandate composition is enforced by checkSave; candidateState (which serves checkSave) keeps identical scoping after the inline, and the composition behaviour is pinned by mutation.test.js:21-67, not by the removed seam test.
- `FLOW-2` (implement, **still-satisfied**): Mandate/validation enforcement path runs through checkSave -> candidateState; behaviour unchanged by inlining evaluateObligations directly; pinned by mutation.test.js checkSave cases.
- `FLOW-3` (implement, **still-satisfied**): Same checkSave save-gate obligation; the same-POST reveal scoping it relies on is preserved and behaviour-pinned by mutation.test.js:38-67 on real payloads.

**Skeptic — strongest objection.** `obligationStateOver` is an author-named, JSDoc-documented "candidate seam" that carries its own dedicated unit test (status.test.js:133-139) demonstrating that the ObligationEvaluator can be re-run standalone over an arbitrary candidate map — WITHOUT the flow-eval spine that `evaluate()` bolts on. That is the contract-layer embodiment of the paradigm's two-evaluator split (the closing brief names the two-evaluator split as exactly the kind of seam that may exist to demonstrate the paradigm rather than to earn its keep in a one-journey spike). Inlining it hides the evaluator's standalone separability inside a private helper and deletes the one test that asserts that separability directly, replacing it with integration-level `checkSave` assertions. Whether the spike wants that visible seam + its focused test tier is a judgement call about the paradigm's intended shape that the three PLAN.md Rulings do not settle and that Sam should sign off.

---

## Merge routes/claims/handlers.js into routes/claims/index.js — a 5-export file with exactly one consumer

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** routes/claims/index.js, routes/claims/handlers.js

**Current shape.** routes/claims/handlers.js (79 lines) exports getClaimsList, postClaimsList, getClaimsAddForm, postClaimsAddForm, getRemoveClaim (handlers.js:25-79), all consumed only by the route table in routes/claims/index.js:3-8,22-54 (55 lines), which contains no logic of its own — it is a path→handler mapping. Verified by grep: no other file in the spike or prototypes/e2e imports from claims/handlers.js. The parity twin spike-a/routes/claims.js is a single file.

**Proposed shape.** Inline the five handlers into routes/claims/index.js next to their routes (as endings/quote-summary.js already does for its surface) and delete handlers.js. Merged file is ~120 lines after de-duplicating the import blocks — under the 150-line folder-module ceiling. page-model.js and view-models.js stay split (pure model derivations vs view shaping, both multi-consumer). index.test.js passes unchanged (it drives HTTP via test-server, importing nothing from either file).

**Rationale.** The handlers/index split is indirection with one caller: every handler name is written three times (definition, import, table entry) to move 79 lines one file away, below the size threshold that justified splitting page-model and view-models out. Collapsing it removes five single-consumer exports and matches both the endings-surface convention in this spike and spike-a's single claims file.

**Obligations affected.**

- (none of the touched files’ implement/reduce obligations are disturbed; see full cross-check in workflow journal)

**Skeptic — strongest objection.** The merge dissolves a paradigm-visible seam that the STANDALONE CONVENTIONS name explicitly — "contract barrel + thin routes (routes are plumbing over contract/index.js)." routes/claims/index.js is deliberately a pure path→handler route table with zero behaviour (the candidate concedes this); handlers.js holds the bodies that call the contract barrel. The claims folder is the ONLY place a single surface is shown fully layered (route table / handler bodies / page-model / view-models), which is the demonstration that even one surface earns the full folder-module decomposition. Merging handlers.js into index.js injects every contract-barrel call, view render and redirect into the route table, collapsing the "thin routes are pure plumbing" layer. Crucially the candidate's supporting precedent is inverted: endings/quote-summary.js does NOT inline a route table — endings/index.js is itself a thin barrel importing handler bodies from separate per-surface files (quote-summary.js etc.), the SAME seam as claims/handlers.js→index.js. So the endings folder corroborates the seam; the merge would make claims diverge from the endings convention it claims to match, not converge on it.

---

## Remove dead IN_PROGRESS/SUBMITTED re-exports from the store barrel

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** store/index.js

**Current shape.** store/index.js:8-12 re-exports `createJourneyRepository`, `IN_PROGRESS`, `SUBMITTED` from journey-repository.js. Grep across the whole spike + prototypes/e2e shows every external import from `store/` names only `createJourneyRepository` or `journeyRepository` — nobody imports the constants via the barrel. The contract layer compares `journey.status` against flow-eval's own `SUBMITTED` (contract/status.js:9,130; contract/guards.js:4,49 import from ../flow-eval/index.js), and flow-eval defines its own IN_PROGRESS ('inProgress') with a different value from the store's ('in-progress'). The constants ARE used inside journey-repository.js (lines 40, 54, 86) and by its direct test (journey-repository.test.js:4-5).

**Proposed shape.** Barrel becomes: import `createJourneyRepository`, re-export it, and export the `journeyRepository` singleton — nothing else. Constants stay exported from journey-repository.js for its own internals and direct test. The barrel then exports exactly what its consumers actually use, and the store's status vocabulary stops looking like a public seam it isn't.

**Rationale.** Dead exports on the barrel advertise a cross-layer status vocabulary that no consumer uses (contract deliberately reads flow-eval's constants instead), and the store IN_PROGRESS ('in-progress') vs flow-eval IN_PROGRESS ('inProgress') name collision with different values is a foot-gun the barrel currently invites; shrinking the barrel to its two real exports removes both.

**Obligations affected.**

- (none of the touched files’ implement/reduce obligations are disturbed; see full cross-check in workflow journal)

**Skeptic — strongest objection.** The change makes the store barrel the ONLY folder-module barrel in the spike that hides its module's public status vocabulary, silently diverging it from the paradigm its sibling flow-eval/index.js exemplifies. That barrel deliberately re-exports its ENTIRE constant vocabulary — CONTAINER_STATUSES, NOT_APPLICABLE, NOT_STARTED, IN_PROGRESS, FULFILLED, journeyState, JOURNEY_STATES, SUBMITTED — including constants no external consumer imports, demonstrating the folder-module rule that "the barrel is the folder's complete public face, including its vocabulary constants." The store barrel currently conforms (createJourneyRepository + IN_PROGRESS + SUBMITTED = factory + its persistence status vocabulary). journey-repository.js frames status as a first-class, documented, precedential concept: "The journey persistence seam (obligations.md:651-689)... the one-way in-progress -> submitted flip... freezes the document (SUBMIT-4..7, Rulings item 1)." Publishing IN_PROGRESS/SUBMITTED on the barrel demonstrates that the store owns its own status vocabulary — deliberately distinct-valued from flow-eval's IN_PROGRESS='inProgress' — which is the two-vocabulary design point, not an accident. The candidate reframes that distinctness as a "foot-gun the barrel invites," but the name collision is inherent at the module level and REMAINS after the change (journey-repository.js still exports IN_PROGRESS='in-progress'), so no foot-gun is removed — only one demonstration surface of the two-vocabulary split. Choosing between "barrel = complete public face incl. constants" (align store UP, keep the re-export, matching flow-eval) versus "barrel = only what consumers use" (align flow-eval DOWN, and this is step one) is a paradigm-shape judgement the three PLAN.md Rulings do not settle, and it touches the named "contract barrel / folder-module barrel" convention.

---

## Remove the unreachable pre-submit redirect inside getConfirmation — the wired guard already owns that decision

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** routes/endings/confirmation.js

**Current shape.** routes/endings/confirmation.js:26-28 does `if (!evaluation.submitted) return h.redirect(BASE)`. Since Step 11.6 landed, every surface-carrying route registers wrapped in withGuard (routes.js:37), and contract/guards.js:80-82 returns BASE for surface 'confirmation' whenever the journey is not frozen — so the pre-handler always redirects before this handler branch can run. Both mechanisms are separately test-pinned for the same behaviour: guard.test.js:117-126 ('gates confirmation back to the start page') and confirmation.test.js:16-21 ('redirects a pre-submit visit'), the latter exercising the guard, not this branch, because createTestServer registers the guarded plugin.

**Proposed shape.** Delete the two-line branch (and drop the now-unused BASE import) so getConfirmation only renders; update the file doc-comment (lines 12-13) to say the gate lives in contract/guards.js rule 2. All existing tests pass unchanged. Note the accepted trade-off: spike-a/routes/endings/confirmation.js:6 keeps its in-handler gate (spike-a has no global guard), so this file diverges from its parity twin by ~3 lines — the obligations paradigm's answer to the same obligation is the guard, which is the point the spike demonstrates.

**Rationale.** Two live mechanisms enforcing one rule is exactly the near-duplicate-code-path smell: the handler branch is dead in the assembled app (guard fires first on every registration path, including the test server), and its survival invites future edits to the wrong copy. The obligations the matrix pins on confirmation.js (SUBMIT-6 rendering, ARCH-5 presentational copy) are untouched; the gate obligation stays handled and pinned in contract/guards.js + guard.test.js + the post-submit-freeze e2e spec.

**Obligations affected.**

- (none of the touched files’ implement/reduce obligations are disturbed; see full cross-check in workflow journal)

**Skeptic — strongest objection.** confirmation.js is a documented parity twin, not incidental redundancy. Three doc-comments — routes/endings/confirmation.js:10-16, contract/guards.js:28-30 (rule 2), and routes/endings/confirmation.test.js:4-6 — each frame this as "the one route status-guarded in BOTH paradigms (spike-a parity)". parity-facts.json:554-555 pins the confirmation status-guard as one of exactly two exceptions to the pre-submit "everything open" rule, citing spike-a's in-handler gate (routes/endings/confirmation.js:5-7) as the parity evidence. spike-a keeps its in-handler gate (it has no global guard). The candidate deletes the obligations twin's in-handler gate, making it the only route-ending file that no longer structurally mirrors its spike-a twin, and it only rewrites ONE of the three "status-guarded in BOTH paradigms" doc-comments — leaving guards.js rule 2 and confirmation.test.js's header describing a shape the code no longer carries. It also removes a defense-in-depth backstop: if a future edit drops the confirmation route's surface metadata or reorders withGuard, getConfirmation would render a terminal "Quote confirmed" panel for a non-submitted journey with reference/premium undefined. This is a paradigm-visible seam and a documented parity-twin correspondence being re-shaped — the candidate itself concedes it "deliberately diverges from the spike-a parity twin" and "a human may still want to sign it off as a design call."

---

## Retire clear() and store/index.test.js — test hygiene with no consumer beyond the store's own tests

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** store/journey-repository.js, store/journey-repository.test.js, store/index.test.js

**Current shape.** store/journey-repository.js:92-95 exposes `clear()` documented as 'Test hygiene only'. Its only callers in the entire spike + e2e are the test that tests clear itself (store/journey-repository.test.js:101-106) and the afterEach in store/index.test.js:5-7. The one test suite that shares the `journeyRepository` singleton across tests, routes/guard.test.js:92-96, never clears it and doesn't need to. store/index.test.js's two tests pin (a) the singleton round-trips a create/get — already exercised for real by routes/guard.test.js via the production wiring — and (b) that two factory calls produce independent Maps, which is closure semantics. coverage-matrix.json never cites store/index.test.js as the pinning test for any obligation (all store obligations cite store/journey-repository.test.js).

**Proposed shape.** Delete `clear()` and its dedicated test case, and delete store/index.test.js entirely (or, minimally, fold a one-line singleton smoke assertion into journey-repository.test.js if reviewers want the singleton export named in a test). Repository API drops to create/get/saveFulfilments/submit — exactly the four operations the DOC's persistence obligations describe.

**Rationale.** A 'test hygiene only' method whose only hygiene consumer is a test file that itself pins trivial behaviour is pure self-referential surface: no obligation needs it, no production or e2e path calls it, and the singleton's real behaviour stays pinned by routes/guard.test.js. Removing both leaves the persistence seam's API identical to what the coverage matrix says it is on the hook for.

**Obligations affected.**

- (none of the touched files’ implement/reduce obligations are disturbed; see full cross-check in workflow journal)

**Skeptic — strongest objection.** store/index.test.js is not self-referential surface — it is the store folder-module's contract-barrel test, and the spike keeps a dedicated barrel test for every other folder-module (contract/index.test.js is cited 6x in the matrix; engine/ and fields/ likewise). Deleting it makes store the sole barrel with no dedicated test, breaking the uniformity that is the paradigm demonstration. It also uniquely pins two things journey-repository.test.js cannot, because that suite imports only the factory, never index.js: the app-level singleton export (index.js:15) round-tripping, and the factory-vs-singleton independence — the duality the barrel doc advertises ("production shares the singleton; tests build their own instances") that underwrites the test-tier isolation guarantee and store/index.js's handled_by role for ARCH-16 (concurrent isolated journeys). clear() is the affordance that keeps that shared singleton hygienic under test; removing both shrinks the demonstrated store contract below its siblings. Whether that barrel test earns parity with the other folder-module barrel tests, or is genuinely trivial as the candidate argues (test #1 is admittedly redundant with guard.test.js), is a test-tier-shape and paradigm-visible-seam judgement call the Rulings do not settle and a human should sign off.

---

## Share the copy-pasted journey/fixture setup across the six contract test files

- **Verdict:** `needs-human-ruling` | **Effort:** M | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** contract/test-helpers.js, contract/status.test.js, contract/mutation.test.js, contract/guards.test.js, contract/submit.test.js, contract/navigation.test.js, contract/view.test.js, contract/cya-rows/index.test.js

**Current shape.** Six sibling suites each re-implement the same setup mechanism with slight drift: the COMPLETE fixture is byte-identical in status.test.js:32-40, guards.test.js:11-19 and submit.test.js:10-18; the repository-create + byName-fulfilments + evaluate helper appears five ways (status.test.js:17-29 setup/byName, mutation.test.js:14-18, guards.test.js:21-29, submit.test.js:20-28, navigation.test.js:18-25 evaluationWith) and the indexed-aware variant twice more (view.test.js:9-26, cya-rows/index.test.js:14-28, both with the FULL fixture duplicated at view.test.js:28-48 / index.test.js:30-50).

**Proposed shape.** One colocated contract/test-helpers.js exporting byName (indexed-aware via identifiers.recordOfName cardinality), setup(values, { submit }), evaluationWith, COMPLETE and FULL; each test file imports what it uses and deletes its local copy (~100 duplicated lines removed). Pure test-file refactor — every assertion body is untouched.

**Rationale.** Seven slightly-different copies of the same journey-arrangement mechanism means a repository or identifiers API change fans out to seven files, and the drift (indexed-aware vs not, submit-aware vs not) already shows the copies diverging; one helper keeps arrangement knowledge in one place without weakening any behavioural pin.

**Obligations affected.**

- `SUBMIT-8` (implement, **still-satisfied**): Canonical test = contract/guards.test.js (post-submit read-only-CYA-only surface). Refactor relocates only the submittedEvaluation/setup arrange helper; the guardPage assertions for the freeze are untouched, so coverage is preserved (possibly relocated).
- `SUBMIT-9` (implement, **still-satisfied**): Canonical test = contract/submit.test.js (CYA POST hard-gate: never trusts the button, re-checks server-side). setup(values,{submit}) unification returns options as a superset; the ok/missing/errorSummary assertions are unchanged.
- `STATUS-33` (implement, **still-satisfied**): Canonical test = contract/view.test.js (CYA reachable before Fulfilled with soft prompts). Indexed-aware evaluationWith is a behaviour-preserving superset; cyaRows prompt assertions untouched.
- `STATUS-35` (implement, **still-satisfied**): Canonical test = contract/submit.test.js (server-side re-check on Submit). Only the fixture/setup arrangement is shared out; the transition-to-submitted and stale-recheck assertions are unchanged.
- `STATUS-X1_3` (implement, **still-satisfied**): Canonical test = contract/view.test.js (incomplete CYA renders defined soft-prompt content). Assertions on prompts.because/href/name are untouched by the helper extraction.
- `STATUS-X1_5` (implement, **still-satisfied**): Canonical test = contract/submit.test.js (failed re-check re-renders CYA, no error/redirect). The stale-recheck assertion body is preserved; only the COMPLETE/byName arrange moves to test-helpers.js.
- `NAV-26` (implement, **still-satisfied**): Canonical test = contract/navigation.test.js (CYA change-link/target behaviour). Unified evaluationWith defaults submit:false and is indexed-aware; navigation's single-cardinality fixtures shape identically, assertions unchanged.
- `NAV-27` (implement, **still-satisfied**): Canonical test = contract/navigation.test.js. Same as NAV-26 — nextAfter/changeTarget assertions untouched; only the evaluationWith arrange helper is colocated.
- `NAV-28` (implement, **still-satisfied**): Canonical test = contract/navigation.test.js. Behaviour-preserving helper unification; add-on task-boundary/section-entry assertions unchanged.
- `NAV-29` (implement, **still-satisfied**): Canonical test = contract/navigation.test.js. Assertions preserved; the only change is where the journey-arrangement helper lives.
- `NAV-37` (implement, **still-satisfied**): Canonical test = contract/guards.test.js (only read-only CYA after submit). submittedEvaluation arrange relocates but the resolve-every-route-to-CYA assertions are byte-for-byte preserved.
- `TOOL-21` (implement, **still-satisfied**): Canonical test = contract/navigation.test.js (primitives re-exported by identity, Level-1 interrogation). The toBe(flowEval.\*) identity assertions do not use the shared setup at all and are untouched.

**Skeptic — strongest objection.** The change makes contract/cya-rows/index.test.js — the colocated test for a SUB-folder-module its own docstring calls "the CYA composition folder-module" — reach UP out of its own folder into a new ../test-helpers.js that holds the PARENT folder's test scaffolding. Today that test imports only product surface (../status.js, ../../store, ../../journey/paths, ./index.js); the proposal adds an upward dependency on a sibling folder's TEST code. That measurably degrades the "each folder-module is independently comprehensible and move/rename-survivable" demonstration that DESIGN-DECISION §Module roles and the tests/ tiers (rename-survival et al.) exist to showcase — and it introduces a THIRD kind of file (shared test infra) into a folder whose paradigm role is stated as exactly "contract barrel + thin concern modules" (module #24 / TOOL-21), each with a colocated test. In a one-journey spike the seven near-identical inline-arrange copies are the deliberate flip side of self-containment: each \*.test.js re-derives its arrangement from the public store+contract+journeyModel surface so it reads top-to-bottom with no chase into shared infra, and the "repository/identifiers API change fans out to seven files" DRY payoff is precisely the hypothetical the paradigm trades away on purpose.

---

## Trim test-only query surface from the identifier index, scope registry and engine barrel

- **Verdict:** `needs-human-ruling` | **Effort:** M | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** engine/identifiers.js, engine/identifiers.test.js, engine/load-model.test.js, engine/scope/registry.js, engine/scope/registry.test.js, engine/scope/journey-rules.js, engine/scope/journey-rules.test.js, engine/index.js, engine/index.test.js

**Current shape.** Several surface methods exist only so their own unit tests can call them: identifiers.js:50 hasName, :53 ids(), :54 size (only callers: identifiers.test.js:20-26,51 and load-model.test.js:47-48 — product code uses idOf/nameOf/recordOfName/recordOfId/hasId/names only); scope/registry.js:34 has() (only caller: registry.test.js:20,28); journey-rules.js:68 createJourneyScopeRegistry's export + barrel entry (only caller: journey-rules.test.js:150; production uses the journeyScopeRegistry singleton); and engine/index.js:13 barrel entries humaniseName + scopeAnswered have no consumer outside engine (both are used internally via direct '../reasons.js' imports; rename-survival.test.js needs only `reason` + `pruneFulfilments` from the barrel).

**Proposed shape.** Remove ids()/size/hasName from createIdentifierIndex (rewrite the pinning assertions in identifiers.test.js/load-model.test.js against names().length and idOf-throws), remove registry has() (assert via rulesFor().length in registry.test.js), make createJourneyScopeRegistry module-local (keep the singleton export; drop the fixture-independence test or rebuild via createScopeRegistry), and drop humaniseName/scopeAnswered (and createJourneyScopeRegistry) from the engine/index.js barrel + index.test.js surface list. Keep hasId (load-model.js:97) and names() (load-model.js:127) — they have real callers.

**Rationale.** Speculative API surface with exactly one consumer each — the module's own test — is coverage padding, not demonstration: no DOC obligation in the reverse index needs a membership/list/size surface beyond what load-model.js actually consumes, and the doc-shaped barrel gets shorter without losing anything any layer imports. Spread across files but every edit is a 1-3 line deletion.

**Obligations affected.**

- `SHAPE-6` (implement, **still-satisfied**): Dual id+name carrying (DOC 984-1004) is pinned by model/obligations.test.js and the untouched flagship tests/rename-survival.test.js; the one removed assertion (identifiers.test.js:51 hasName('fullName')===false after rename) is rewritten to idOf-throws, which pins the identical behaviour: the old name no longer resolves while nameOf(id) returns the new name (identifiers.test.js:49-50 untouched).
- `FULF-5` (implement, **still-satisfied**): 'Real storage uses id' is pinned by identifiers.test.js resolution + rename-survival.test.js keying on FULL_NAME_ID/EMAIL_ID — neither depends on ids()/size/hasName; the membership-test rewrite keeps hasId + names() assertions which remain on the surface.
- `FULF-15` (implement, **still-satisfied**): Id-origin-mixing opacity (DOC 758-759) is exercised through recordOfId/idOf resolution and rename-survival, untouched; removed accessors play no part in the pin.
- `PERSIST-8` (implement, **still-satisfied**): Fulfilments-keyed-by-id is tested via identifiers.test.js idOf/nameOf round-trips and rename-survival's stored() map keyed on UUID ids — none of the deleted methods carry this pin.
- `PERSIST-26` (implement, **still-satisfied**): Name-uniqueness enforcement (DOC 1000-1004) is pinned by the constructor-throws test (identifiers.test.js:35-42, 'Duplicate obligation name') which the proposal does not touch; identifiers.js:19-27 duplicate guards unchanged.
- `PERSIST-27` (implement, **still-satisfied**): Cosmetic-rename safety (DOC 1006-1009) is pinned by the SHAPE-6 rename test (rewritten equivalently) plus the untouched tests/rename-survival.test.js tier; production rename path (same id, new name) unchanged.
- `PERSIST-29` (implement, **still-satisfied**): Tooling-generated UUIDs (DOC 1016-1018) lives in the catalogue/model authoring side; identifiers.test.js pins only that the index consumes them — resolution assertions untouched.
- `PERSIST-30` (implement, **still-satisfied**): Up-front UUID adoption (DOC 1020-1023) is demonstrated by the whole dual-identifier index existing and being consumed by load-model; the index's construction and duplicate guards are unchanged.
- `SHAPE-16` (implement, **still-satisfied**): Most-restrictive-wins (DOC 197): cited test registry.test.js pins it via registration-order/rulesFor mechanics (lines 5-15), not has(); the two has() assertions (:20,:28) are rewritten to rulesFor().length which pins the same no-rules-registered/rules-registered fact — rulesFor('premium')===[] at :19 already carries the default-scope pin.
- `DEF-3` (implement, **still-satisfied**): Pure sync evaluation engine: cited test journey-rules.test.js — its pins are the rule-firing cases (lines 31-147), all untouched; only the non-cited fixture-independence case (:149-155) is dropped when createJourneyScopeRegistry goes module-local, and the singleton at :133 plus every registration is unchanged.
- `DEF-4` (implement, **still-satisfied**): Engine wrapped by orchestrator: same cited test; orchestrator files untouched and the journey registry the orchestrator consumes (journeyScopeRegistry singleton) is unchanged.
- `SHAPE-17` (implement, **still-satisfied**): Mandatory/optional flips in scope: pinned by journey-rules.test.js claimAmount/driverDob cases (lines 100-143), untouched by the export change.
- `EVAL-11` (implement, **still-satisfied**): Per-obligation inScope boolean: pinned by the fire()-based scope cases in journey-rules.test.js, none of which use createJourneyScopeRegistry (they use the singleton via rulesFor).
- `EVAL-12` (implement, **still-satisfied**): Status meaningful only in scope: pinned by the null-outside-scope assertions (e.g. :86-87, :101-102), untouched.
- `ARCH-12` (reduce, **still-satisfied**): One evaluator/orchestrator set per language (reduce): the engine barrel remains the single JS surface and index.test.js keeps pinning the exact export list — just three names shorter; the end-to-end barrel-only run (index.test.js:32-46) uses only retained names (loadJourneyModel, journeyScopeRegistry, evaluateObligations, unfulfilledMandatory, ENGINE_MANDATORY_ALWAYS).

**Skeptic — strongest objection.** The change edits the contract barrel's exact-surface pin (engine/index.test.js:6-29, "the doc-shaped barrel (smoke)") to drop humaniseName, scopeAnswered and createJourneyScopeRegistry, and simultaneously shrinks the identifier-index boundary API (ids()/size/hasName) and the scope registry's has(). The candidate justifies all of these as "test-only coverage padding," but the barrel's own docstring (engine/index.js:1-8) declares it a curated demonstration surface — "the plain primitives interrogation Level 1 exposes" — and line 13 re-exports the reasons.js group (reason, reasonCodes, humaniseName, scopeAnswered) as a coherent whole. humaniseName/scopeAnswered are the reason-authoring/provenance primitives (DOC §J; reasons.js:83-92). "Unused by a one-journey spike's product code" is exactly the unearned-seam-that-demonstrates-the-paradigm case the brief flags: the identifier index deliberately returns a complete, self-describing name↔id boundary value (Graft 1), and the barrel is a demonstration-driven surface, not a consumption-driven one. Whether these are padding or deliberate paradigm demonstration is the crux disagreement, and it is a judgement call the Rulings do not settle.

---

## Trim the flow-eval barrel to its real consumers and inline the single-consumer navigation barrel

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** flow-eval/index.js, flow-eval/navigation/index.js

**Current shape.** flow-eval/index.js exports 9 names nothing outside the layer imports: presentedObligations (index.js:6), createFlowConditionRegistry / createJourneyFlowConditions / journeyFlowConditions (index.js:9-11), rollUpChildStatuses / CONTAINER*STATUSES / NOT_STARTED (index.js:15-18), SECTION_ENTRY_MODES (index.js:28), JOURNEY_STATES (index.js:30). Grep across the whole spike + prototypes/e2e shows every external importer (contract/*, journey/\_, orchestrator/apply-answers/page-answers.js, tests/helpers/script-runner.js) uses only expandSlots, isReadOnly, containerApplies, containerStatus, journeyState, FULFILLED, IN_PROGRESS, NOT_APPLICABLE, SUBMITTED and the five navigation functions; all flow-eval unit tests import the defining files directly. flow-eval/navigation/index.js:1-9 is a pure re-export file whose ONLY importer is flow-eval/index.js:22-29.

**Proposed shape.** flow-eval/index.js exports exactly the externally-consumed surface: { expandSlots, isReadOnly } from presents.js, { containerApplies } from applies-when.js, { containerStatus, FULFILLED, IN_PROGRESS, NOT_APPLICABLE } from container-status.js, { journeyState, SUBMITTED } from journey-state.js, and the five navigation functions plus nothing else, imported DIRECTLY from navigation/{first-applicable-page,first-unfulfilled-page,first-page-presenting-obligation,next-after,section-entry}.js; delete navigation/index.js. The constants/factories stay exported from their defining files where their unit tests pin them (CONTAINER_STATUSES in container-status.test.js:46, JOURNEY_STATES in journey-state.test.js:59, SECTION_ENTRY_MODES in section-entry.test.js:53).

**Rationale.** The barrel doubles as the package's declared interface, and today it lies: two thirds of its surface has no consumer, so a reader cannot tell which primitives the rest of the spike actually stands on. Unlike the contract barrel (pinned by contract/index.test.js), no test pins the flow-eval barrel, so the dead names earn nothing. The nested navigation/index.js is a pass-through with one caller that immediately re-exports — pure indirection; the folder-module convention needs one barrel per module (flow-eval/index.js), not one per sub-folder.

**Obligations affected.**

- `DEF-37` (implement, **still-satisfied**): DOC 110-114 requires the JourneyEvaluator to be a collection of small pure Flow-layer primitives. Those primitives live in container-status.js etc. and stay exported; trimming dead re-exports from flow-eval/index.js changes no primitive and no test (engine/evaluate.test.js never imports the barrel).
- `DEF-X2_1` (implement, **still-satisfied**): Layering (Flow consumes ObligationEvaluator output, never the reverse) is enforced by the barrel importing only within flow-eval — its header comment 'imports nothing from engine/ or orchestrator/ at runtime' remains true under the proposed shape.
- `EVAL-3` (implement, **still-satisfied**): DOC 249-253: pure sync Flow-layer functions over ObligationEvaluator output. The functions and their purity are defined in presents.js/container-status.js/journey-state.js/navigation/\*, none of which change.
- `EVAL-6` (implement, **still-satisfied**): DOC 259-261: both evaluators independently unit-testable with plain fixtures. Every flow-eval unit test imports its defining file directly (verified — only collection-review.test.js imports an index, and that is engine/index.js), so barrel changes cannot affect test isolation or coverage.
- `EVAL-23` (implement, **still-satisfied**): DOC 448-474: expose individual primitives, not one evaluate(); the doc names firstApplicablePage, firstUnfulfilledPage, firstPagePresentingObligation, containerStatus, journeyState — all five stay individually exported from flow-eval/index.js (navigation ones now imported directly from defining files instead of via navigation/index.js). Callers still invoke the primitive they need; contract/navigation.test.js identity assertions (flowEval.firstApplicablePage toBe ...) still hold because the same function objects are re-exported. coverage-matrix handled_by should drop flow-eval/navigation/index.js — a bookkeeping edit, not a coverage loss.
- `EVAL-29` (implement, **still-satisfied**): DOC 472-474: the common shape for future primitives is demonstrated by next-after.js and journey-state.js themselves; nextAfter remains barrel-exported and its test imports the defining file. Untouched by the trim.
- `ARCH-10` (implement, **still-satisfied**): DOC 1175-1177: JourneyEvaluator as pure primitives over the Flow tree using ObligationEvaluator output — a property of the primitive implementations, not the re-export list. No behaviour change.
- `ARCH-12` (reduce, **still-satisfied**): DOC 1180-1182: one evaluator/orchestrator set per language per Service. The matrix note says the single set is 'exposed through the engine and flow-eval package barrels' — flow-eval/index.js survives as the one package barrel (now truthful about its surface); no second set is created. Reduce-scope, non-blocking anyway.
- `OPEN1-X2_1` (implement, **still-satisfied**): DOC 2085-2088: construction injection extends to the JourneyEvaluator. The pattern is createFlowConditionRegistry/createJourneyFlowConditions in applies-when.js plus the injectable `conditions` option defaulting to journeyFlowConditions in container-status.js/first-applicable-page.js/next-after.js — all consumed and tested via direct './applies-when.js' imports (applies-when.test.js:36-160, container-status.test.js:141, journey-state.test.js:98). Removing the three factory names from the barrel removes only never-used re-exports; behaviour and tests are byte-identical. Matrix handled_by for this id should drop flow-eval/index.js.
- `NAV-1` (implement, **still-satisfied**): DOC 1552-1578: expose three pure navigation functions over the Container tree. handled_by lists navigation/index.js, but the primitives are defined in first-applicable-page.js etc. (tested there — first-applicable-page.test.js imports the defining file) and remain exposed to the rest of the spike via flow-eval/index.js and contract/navigation.js, whose imports are unchanged. Handling relocates from the deleted sub-barrel to flow-eval/index.js; the deleted file's doc-comment breadcrumb (obligations.md:1287-1318) should be carried onto the flow-eval/index.js export block.
- `NAV-X2_1` (implement, **still-satisfied**): DOC 1580-1583: the three primitives cover every runtime navigation question. Sufficiency is demonstrated by next-after.js and contract/navigation.js composing only those primitives — neither changes; next-after.test.js imports the defining file. Deleting the pass-through sub-barrel introduces no new traversal mechanism.

**Skeptic — strongest objection.** flow-eval/navigation/index.js is not "pure indirection with one caller" — it is a named handled_by artifact pinned in coverage-matrix.json for THREE obligations (EVAL-23 "Expose JourneyEvaluator as individual primitives", NAV-1 "Expose three pure navigation primitives over the Container tree", NAV-X2_1 "Three primitives are sufficient for all runtime navigation resolution"). Its own header documents it as the "Flat barrel for the navigation folder-module." It is therefore a paradigm-visible seam: navigation/ is the one nested folder-module inside flow-eval, and its barrel is the clearest demonstration in the whole spike that the folder-module convention RECURSES — each sub-folder that groups related primitives owns a barrel that is its published interface, and the parent barrel (flow-eval/index.js) composes from child barrels rather than reaching into leaf files. Deleting it makes flow-eval/index.js import directly from navigation/{first-applicable-page,...}.js, coupling the parent to the navigation folder's internal file layout — exactly the coupling a folder-module barrel exists to prevent. The prompt explicitly flags such seams ("a seam that looks unearned in a one-journey spike may exist precisely to demonstrate the paradigm — e.g. the contract barrel"). Additionally, the candidate concedes it must rewrite the ground-truth coverage-matrix handled_by lists for those three obligations — dismissed as "a bookkeeping edit" — but editing the spike's parity/traceability ledger to make a simplification pass is itself a sign-off action, not a free move.

---

## Trim the orchestrator barrel's four dead re-exports (scope-exit-wipe and system-handlers pairs)

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** orchestrator/index.js

**Current shape.** orchestrator/index.js:27-28 re-exports reconcileDerived, wipeOutOfScope, createSystemHandlerRun and systemHandlers. Grep across contract/, routes/, journey/, lib/, flow-eval/, tests/ and prototypes/e2e finds zero external importers of any of the four; the only real consumers import the source files directly (fixed-point.js:3-4, and each module's own test). The barrel's genuinely-consumed exports are the five entry points, encodeFieldName (contract/mutation.js:5, contract/view.js:6, tests/helpers/script-runner.js:11) and runToFixedPoint (tests/completability.test.js:11).

**Proposed shape.** Delete lines 27-28 of orchestrator/index.js so the barrel exports exactly its external surface: applyPageAnswers, addIndexedFulfilment, markIndexedCollectionReviewed, removeIndexedFulfilment, reconcileJourney (until item 1 lands), encodeFieldName and runToFixedPoint. runToFixedPoint MUST stay — the completability invariant tier imports it via the barrel.

**Rationale.** The barrel's doc comment sells itself as 'the ONLY side-effecting layer' whose surface is the mutating entry points; re-exporting the internal sub-passes advertises them as public API and invites callers to bypass the risk-7 write->fixed-point->save invariant. Removing them makes the real contract legible at a glance and is verified consumer-free.

**Obligations affected.**

- `ARCH-11` (implement, **still-satisfied**): DOC (matched at obligations.md:1177-1182) says the orchestrator is 'the side-effecting wrapper that triggers system-handled obligations, collects callbacks, writes to fulfilments, and re-runs the evaluators'. That is the commit/runToFixedPoint path (index.js:39-56, fixed-point.js), which the deletion of lines 27-28 does not touch. Removing the re-exports of the internal sub-passes narrows the advertised surface to the mutating entry points, which strengthens rather than weakens 'sole side-effecting wrapper'. Test engine/evaluate.test.js imports sources directly, unaffected.
- `DEF-4` (implement, **still-satisfied**): DOC lines 31-33: engine 'wrapped by an orchestrator that handles I/O'. The wrapping is applyPageAnswers/commit -> runToFixedPoint -> repository.save, all untouched; the deleted lines are re-exports with zero external importers (verified by grep across all of prototypes/, including e2e). Test engine/scope/journey-rules.test.js unaffected.
- `EVAL-1` (implement, **still-satisfied**): DOC lines 239-241 mandate three cooperating components (two pure evaluators + side-effecting orchestrator). All three components and their cooperation via fixed-point.js remain byte-identical; only two dead barrel re-export lines go. engine/evaluate.test.js imports the engine directly.
- `EVAL-4` (implement, **still-satisfied**): DOC lines 254-257: orchestrator triggers system-handled obligations in a fixed-point loop. fixed-point.js:3-4 imports createSystemHandlerRun/reconcileDerived/wipeOutOfScope from the source files, not the barrel, so system-handler triggering is unchanged. system-handlers.test.js also imports './system-handlers.js' directly — same test coverage.

**Skeptic — strongest objection.** The change re-curates a paradigm-demonstration barrel's public surface, and in this spike a barrel's exported surface is a deliberately-curated contract (proven by contract/index.test.js pinning the contract barrel to "exactly 21 exports, no drift" under obligation TOOL-21). Trimming the orchestrator barrel to its consumed surface expresses a production instinct ("minimize surface, don't invite invariant bypass"), but the spike's explicit purpose is pedagogy/paradigm demonstration (README honesty ledger, EXTENDING.md), where the competing value "advertise the folder's full vocabulary for one-import discoverability" is legitimate. EXTENDING.md:487-488 headlines orchestrator/scope-exit-wipe.js ("wiped, not hidden") as a teaching behaviour — exactly the module wipeOutOfScope/reconcileDerived expose through the barrel. Which instinct wins on a demonstrator barrel is a judgement call the three PLAN.md Rulings do not settle and that the paradigm owner (Sam) should sign off.

---

## Unexport dead helper surface: canonicalise, journeyFrom, and renderClaimsAdd's unused slots

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** tests/helpers/script-runner.js, tests/helpers/render-page.js, tests/alignment-walker.test.js

**Current shape.** tests/helpers/script-runner.js:97 exports `canonicalise` but its only caller is internal (script-runner.js:165); tests/helpers/render-page.js:31 exports `journeyFrom` but its only caller is `evaluationFor` two lines below (render-page.js:48); render-page.js:93 returns `{ html, slots }` from renderClaimsAdd but the sole caller (alignment-walker.test.js:175) destructures only `html`. Verified by grep across the whole spike + prototypes/e2e — no other importers.

**Proposed shape.** Drop the `export` keyword on canonicalise and journeyFrom (module-private consts), and have renderClaimsAdd return just the html string. Doc comments stay; no behaviour change, no test change beyond the one destructure site.

**Rationale.** Dead exports advertise an API the layer does not have, inviting future callers to bind to internals; the spike's own contract-barrel discipline says public surface is deliberate, so helper modules should not export what nothing imports. touches_protected only because the one-word destructure fix lands in alignment-walker.test.js.

**Obligations affected.**

- (none of the touched files’ implement/reduce obligations are disturbed; see full cross-check in workflow journal)

**Skeptic — strongest objection.** The simplification is behaviour-neutral and green-preserving, but it edits a paradigm-load-bearing invariant-tier file (alignment-walker.test.js, changing line 175's destructure) and demotes two documented seams — journeyFrom and canonicalise, the latter carrying the TEST-30 canonical-projection doc comment — from named exports to module-private consts. In a paradigm-demonstration spike these named, doc-commented seams are the legible vocabulary a reader uses to trace how the invariant tiers drive the real contract (journeyFrom -> evaluate -> render; replay -> canonicalise -> deep-equal). Trimming them is defensible engineering, but it reshapes protected test-tier surface and touches an invariant-tier file, and whether contract-barrel public-surface discipline even applies to test helpers is a judgement call the Rulings do not settle — a human should sign it off.

---

## Unify three bespoke recursive Flow-tree walkers into one ancestry walk

- **Verdict:** `needs-human-ruling` | **Effort:** M | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** contract/status.js, contract/navigation.js

**Current shape.** Three hand-rolled recursive descents over the same Container tree: contract/status.js:53-61 findPageIn (used by pageById at 64-73 and re-scanned per-section by sectionOfPage at 76-84), and contract/navigation.js:36-53 taskGroupOfPage (a third walk that finds the page's direct parent group). guards.js:63 additionally calls sectionOfPage after containerStatuses already located the page — so a single guard request can walk the tree twice.

**Proposed shape.** One walker in contract/status.js: `ancestorsOfPage(flow, pageId)` returning the container chain [topLevelSection, ...groups, page] (or null). Then pageById = chain.at(-1) with the existing 'Unknown page' throw, sectionOfPage = chain[0] with the existing 'No section presents' throw, and navigation.js's taskGroupOfPage = chain.at(-2) (exported from status.js or derived where needed). Existing pins in status.test.js:141-147 and navigation.test.js:58-71 stay green unchanged.

**Rationale.** Three ~15-line recursive functions all encode the same 'descend children, stop at page' knowledge; a bug fix or Flow-shape tweak (e.g. new container kind) currently needs three edits. One ancestry chain answers all three questions as trivial index reads.

**Obligations affected.**

- `NAV-1` (implement, **still-satisfied**): nextAfter/taskGroupOfPage behaviour (advance within Section, hub fallback) is preserved: chain.at(-2) equals the current immediate-parent group returned by taskGroupOfPage, and object identity across the two nextAfter calls is preserved since both derive from the same cached flow. Pinned by navigation.test.js:38-56, unchanged.
- `NAV-2` (implement, **still-satisfied**): claims-gate advance via nextAfter still honoured; taskGroupOfPage-derived boundary logic is behaviour-identical. navigation.test.js:44-51 stays green.
- `NAV-3` (implement, **still-satisfied**): skip-already-Fulfilled advance unaffected; taskGroupOfPage identity preserved. navigation.test.js:53-56 unchanged.
- `NAV-4` (implement, **still-satisfied**): add-on task-boundary hub-return (spike-a parity) depends on taskGroupOfPage(page)!==taskGroupOfPage(current); chain.at(-2) yields the same SubSection/Section references, so the identity comparison is unchanged. navigation.test.js:58-71 stays green.
- `NAV-5` (implement, **still-satisfied**): linear advance within one add-on preserved by the identical boundary comparison; pinned by navigation.test.js:65-67.
- `NAV-6` (implement, **still-satisfied**): served on both navigation.js and guards.js; guards' sectionOfPage call returns chain[0] (same top-level section object) feeding firstApplicablePage — behaviour identical. Not disturbed.
- `NAV-7` (implement, **still-satisfied**): sectionEntry hub-fallback unaffected; but the shared task-boundary knowledge it neighbours is preserved by the unified chain.
- `NAV-16` (implement, **still-satisfied**): changeTarget/first-presenting-page path is untouched by the walker unification; still resolves identically.

**Skeptic — strongest objection.** The refactor is behaviour-preserving (all pins green, no obligation/Ruling/e2e broken), but it collapses three separately-named, individually-documented contract primitives — pageById (page identity), sectionOfPage (section ownership), and the spike-a-parity-documented taskGroupOfPage (hub-task boundary) — into index arithmetic over a shared chain, on the very two files (contract/status.js + contract/navigation.js) that constitute the spike's "contract barrel + thin routes" paradigm demonstration. It also introduces new cross-file coupling (navigation.js's nextAfter now depends on a status.js walker / a widened export surface) and pushes status.js against its ~150-line folder-module budget. Whether those three named primitives are meant to be visibly separate for legibility/demonstration, or are genuine duplication to unify, is a paradigm-visible judgement the PLAN.md Rulings do not settle — a human should sign it off rather than accept it as automatically "safe."

---

## checkSave returns a `findings` field no product code reads, and toFieldErrors carries a never-used injectable resolver

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** validation/index.js, validation/index.test.js, validation/field-errors.js

**Current shape.** validation/index.js:14 composes checkSave to return { blocked, findings, errors, errorSummary }, but the only product consumer (contract/mutation.js:88) destructures only { blocked, errors, errorSummary }; `findings` is pinned solely by an incidental toEqual in validation/index.test.js:43-48. Separately, validation/field-errors.js:16 declares toFieldErrors(findings, resolve = resolveMessage) — every call site in the spike (validation/index.js:13 and all of field-errors.test.js:6,21,37,46,58) uses the default; no caller ever injects a resolver.

**Proposed shape.** checkSave returns { blocked, errors, errorSummary } (adjust the one toEqual in validation/index.test.js to drop findings); toFieldErrors(findings) calls resolveMessage directly. The i18n test seam survives untouched via createResolver (OPEN1-13's locale-agnostic hook), which is the real injection point.

**Rationale.** A result field with zero consumers and a DI parameter with zero non-default callers are both generality that earns nothing — the locale seam the DOC requires (OPEN1-13) already lives in i18n/createResolver, not in this parameter.

**Obligations affected.**

- `FLOW-7` (implement, **still-satisfied**): Blocks Save on unfulfilled hard mandates via the `blocked` flag. contract/mutation.js:88 consumes only `{blocked,errors,errorSummary}` and maps blocked->!ok. The proposed shape keeps `blocked` (still `findings.length > 0`), only dropping the unread `findings` field. Test model/flow.test.js untouched; validation/index.test.js still asserts blocked.
- `FLOW-8` (implement, **still-satisfied**): Re-render with GDS error summary + per-field errors is driven by `errors`/`errorSummary`, both still returned unchanged. toFieldErrors still produces them; only the never-injected `resolve` param and the unread `findings` field are removed. Behaviour and the resolved round-trip assertions (index.test.js, field-errors.test.js) are preserved.
- `STATUS-29` (implement, **still-satisfied**): Hard-mandate validation on Page POST is expressed by the `blocked` gate that survives the change; the gate's inputs (saveCheck findings length) are unchanged, only the outward `findings` field on the return object is dropped.
- `ARCH-35` (implement, **still-satisfied**): field-errors.js's role (turning findings into the GDS error view-models that re-render the page) is intact — errors map and errorSummary hrefs unchanged. Dropping the default-only `resolve` param does not alter the emitted view-models. Test engine/mandates.test.js untouched.
- `OPEN1-10` (implement, **still-satisfied**): Reason/error codes still resolve through the frontend i18n mechanism: toFieldErrors calls resolveMessage (imported from ../i18n/index.js) exactly as before — inlining it as the direct call rather than a default param is behaviourally identical. The throwing-resolver graft-7 path (uncatalogued code throws) is preserved. Test model/messages.test.js untouched.
- `OPEN1-12` (implement, **still-satisfied**): Interpolation values are still passed: `resolveMessage(finding.code, finding.values)` is unchanged by the edit. field-errors.test.js:45-54 (values interpolation) continues to pass.

**Skeptic — strongest objection.** The `resolve = resolveMessage` parameter on toFieldErrors (field-errors.js:16) is not merely unused generality — it is a paradigm-visible i18n injection seam at the validation folder-module boundary, of exactly the kind the spike deliberately builds and praises elsewhere (PLAN.md:259 calls flow-eval graft 16 an "injectable registry seam" and treats it as demonstration-worthy). Removing it is the ONLY way to make toFieldErrors emit anything but the module-bound English `resolveMessage`; after the change there is no view-model-layer locale-swap point without editing field-errors.js source. The candidate's defence — that `createResolver` is "the real injection point" — is true but incomplete: createResolver only _builds_ a resolver; nothing wires a non-English resolver into toFieldErrors once the param is gone. Whether the paradigm should visibly demonstrate locale-swappability at the field-errors layer (the DOC's OPEN1 i18n cluster: code-as-key, interpolation values, locale-agnostic resolution) via this param, or rely solely on createResolver, is a judgement call a human owning the paradigm must settle — not a skeptic.

---

## createJourneyFlowConditions is a one-caller factory — build the singleton directly

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** flow-eval/applies-when.js

**Current shape.** flow-eval/applies-when.js:59-87 wraps the journey's three condition registrations in an exported factory whose only call in the entire spike is the module-scope singleton on the next line (applies-when.js:89, `export const journeyFlowConditions = createJourneyFlowConditions()`). No test calls the factory (applies-when.test.js:106-141 tests journeyFlowConditions itself; every fixture test builds its own registry via createFlowConditionRegistry), and no other module imports it — it is also one of the dead barrel exports (index.js:10).

**Proposed shape.** Replace the factory with direct module-scope construction: `const registry = createFlowConditionRegistry()` followed by the three define/defineFamily calls, then `export const journeyFlowConditions = registry`. createFlowConditionRegistry stays exported from applies-when.js (fixture tests across five test files genuinely use it — the generality rail). The closed-name-list test (applies-when.test.js:143-163) is unaffected.

**Rationale.** A factory earning re-instantiability that nothing re-instantiates: the journey conditions are a closed list asserted against model/flow.json, so there is exactly one of them by design — wrapping the registrations in a function adds a naming and indirection layer over what is really module initialisation.

**Obligations affected.**

- `SHAPE-28` (implement, **still-satisfied**): DOC 212-214 mandates scoping/conditions as functions, not a declarative rule language. The conditions remain plain (evaluation)->boolean functions registered via define/defineFamily; whether the three registrations run inside a factory or at module scope changes nothing about the function-based mechanism. Test (engine/evaluate.test.js plus applies-when.test.js registry mechanics, lines 34-64) never calls the factory.
- `EVAL-30` (implement, **still-satisfied**): DOC 476-478: JourneyEvaluator must be testable against arbitrary hand-built ObligationEvaluator output. That property lives in containerApplies' injectable `conditions` parameter (applies-when.js:95-104, unchanged) and the hand-built `evaluationOf` fixtures in applies-when.test.js — the singleton tests at 106-141 pass hand-built evaluations to journeyFlowConditions, which the proposal exports identically. No tested surface is the factory itself.
- `STATUS-9` (implement, **still-satisfied**): DOC 1453-1456: everything affecting applicability is an obligation. This is realised by the condition bodies reading only evaluation.obligations/fulfilments via valueByName (applies-when.js:16-23, 62-84) — the proposal moves those registrations verbatim to module scope, so applicability still flows exclusively through obligation output. Test flow-eval/applies-when.test.js unchanged and still passing against the same singleton export.
- `OPEN1-X2_1` (implement, **still-satisfied**): DOC 2082-2088: construction injection extends to the JourneyEvaluator. The demonstrated seam is the injectable registry — createFlowConditionRegistry (kept exported, used by five fixture test files) plus the `conditions` default parameter on containerApplies (untouched). The factory is not the injection point, just a wrapper building the default registry; its cited test (engine/evaluate.test.js) never imports it. Caveat: the dead re-export at flow-eval/index.js:10 (also a handled_by file for this id) must be removed in the same change or the barrel fails to load.

**Skeptic — strongest objection.** The factory is not dead scaffolding — it is the coverage-matrix-credited demonstration surface for OPEN1-X2_1, an IMPLEMENT-scope obligation titled "Construction injection applies to the JourneyEvaluator as well as the ObligationEvaluator." I verified the matrix (coverage-matrix.json:4615-4624): OPEN1-X2_1's handled_by is EXACTLY [engine/evaluate.js, flow-eval/applies-when.js, flow-eval/index.js] — i.e. the factory (applies-when.js:59) AND its barrel re-export (index.js:10) are both named as the artifacts that realise this obligation. DOC 2082-2088 (which I read directly) names "the JourneyEvaluator" as an equal target of construction injection: "The same pattern extends to any other evaluator dependency that becomes injectable ... for both the ObligationEvaluator and the JourneyEvaluator." The paradigm the spike exists to demonstrate is the two-evaluator construction symmetry: the ObligationEvaluator is built by `createObligationEvaluator({...})` (DOC:2078) and the JourneyEvaluator's condition set is built by its mirror-image `createJourneyFlowConditions()`. In a deliberately one-journey spike, a factory whose only call is its own next-line singleton is exactly the kind of unearned-looking seam the prompt warns exists to demonstrate the paradigm — deleting it (and its barrel export, which the candidate concedes it must delete) collapses that visible symmetry and removes two of the three handled_by artifacts for OPEN1-X2_1. Crucially, no guardrail catches this: OPEN1-X2_1's test is engine/evaluate.test.js, which never imports the factory, and I confirmed by grep that createJourneyFlowConditions has zero test callers and rename-survival.test.js has no barrel-surface assertion — so the demonstration surface can silently vanish while every test stays green. That is precisely a paradigm-visible-seam trade a human (Sam) must sign off, not an automated "safe."

---

## decodeFieldName is dead product code — only its own test calls it

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** apply-answers/field-names.js, apply-answers/field-names.test.js, apply-answers/index.js, orchestrator/index.js, PLAN.md

**Current shape.** orchestrator/apply-answers/field-names.js:15-23 defines decodeFieldName, re-exported through apply-answers/index.js:8 and orchestrator/index.js:25. The only consumer anywhere in the spike + prototypes/e2e is its own test (apply-answers/field-names.test.js:11-24). Product code only ever encodes: page-answers.js:27 builds the input name and looks the payload key up; no code path parses a form name back into { name, fulfilmentId }.

**Proposed shape.** Delete decodeFieldName from field-names.js, drop its re-exports from both barrels, delete the decode test cases (field-names.test.js:11-24), and amend the one PLAN.md architecture-table cell that says the module 'owns encodeFieldName/decodeFieldName' (PLAN.md:277 — a descriptive table row, not a Ruling). encodeFieldName, SEPARATOR and the encodeURIComponent convention stay untouched.

**Rationale.** An unused inverse kept for symmetry: the 'one seam between HTML form names and slot identity' only ever crosses in one direction in this spike, because applyAnswers encodes and looks up rather than decoding posted keys. No obligation id or matrix entry names decoding; no test outside its own unit test pins it. Dead exports in a paradigm demonstration mislead readers about the real surface.

**Obligations affected.**

- `EVAL-31` (implement, **still-satisfied**): Render-and-await (obligations.md:362-363) crosses the form-name seam only in the encode direction: contract/view.js:29 and page-answers.js:27-28 build inputName via encodeFieldName and look the payload key up. encodeFieldName, its consumers and its test case (field-names.test.js:5-9) all survive; decode is never on this path.
- `STATUS-30` (implement, **still-satisfied**): Write-fulfilments-on-POST is applyAnswers (page-answers.js) + orchestrator commit (orchestrator/index.js:40-68), tested by page-answers.test.js and orchestrator tests. Only the dead decode export is dropped from orchestrator/index.js:25; commit/applyPageAnswers untouched.
- `SHAPE-21` (implement, **still-satisfied**): Convergent-data unitarity (obligations.md:200) is delivered by the shared-fulfilment writers in page-answers.js/canonical-value.js; its cited test (matrix 'orchestrator/apply-answers.test.js' = the folder's test files) loses only the decode-only cases, which pin nothing but the deleted function.

**Skeptic — strongest objection.** Deleting decodeFieldName severs the return half of an explicitly-named, paradigm-visible seam and leaves a settled design doc factually stale. field-names.js:4-5 frames "Encode and decode are the one seam between HTML form names and the slot identity"; DESIGN-DECISION.md:149 describes the module's role as "decode form field names (obligation name + encoded fulfilmentId) to ids." The candidate's remediation amends only the PLAN.md:277 table cell and misses DESIGN-DECISION.md:149 entirely — so post-deletion the honesty-ledger design doc still asserts a decode responsibility that no longer exists. In a spike whose stated purpose is to demonstrate a portable paradigm for a real multi-journey app, decode is the inverse a real POST handler (iterating arbitrary posted keys rather than known slots) would need; shrinking a documented two-way seam to one-way is a paradigm-demonstration trade-off Sam should sign off, not an automatic safe.

---

## enumerate-states re-implements the engine's name→id lookup with its own model load

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** tests/helpers/enumerate-states.js

**Current shape.** tests/helpers/enumerate-states.js:17-31 does its own fs read of model/obligations.json plus a hand-rolled `idOf` (find-by-name with a throw) — duplicating exactly what `loadJourneyModel().identifiers.idOf` provides and what the sibling helpers already use (render-page.js:32, script-runner.js:30). Its consumer completability.test.js:26 already imports `loadJourneyModel` in the same test run.

**Proposed shape.** Replace lines 17-31 with `const { identifiers } = loadJourneyModel()` from '../../engine/index.js' and use `identifiers.idOf`; `satisfyingValueFor` and the enumeration logic are unchanged. ~15 lines and one divergent lookup mechanism deleted.

**Rationale.** Two mechanisms for the same job (name→id) in the same tests/helpers folder is near-duplication with a real cost: the hand-rolled one bypasses the engine's duplicate-name validation the rename-survival tier proves matters. No obligation on this file (TEST-3/13/14, EVAL-37) requires an engine-free load — tier 1 is the only 'pure JSON' tier, and this helper serves tier 3 which already runs the engine.

**Obligations affected.**

- `EVAL-37` (implement, **still-satisfied**): Property-based/state-enumeration testing of the pure evaluators is delivered by completability.test.js over the states enumerateStates() produces. The change only swaps the helper's name->id source (hand-rolled find-by-name -> engine identifiers.idOf), which is behaviourally identical (both find by name in the same model/obligations.json, both throw on unknown). Enumeration output and the test assertions are unchanged, so the property-based coverage is intact. The test file itself is untouched.
- `TEST-3` (implement, **still-satisfied**): The dynamic reachability/completability tier is completability.test.js ('completes the journey from every enumerated state'), fed by enumerateStates(). Replacing lines 17-31 with `const { identifiers } = loadJourneyModel()` and identifiers.idOf yields the same state list (same names -> same ids from the same catalogue), so the tier still asserts every plausible state reaches Fulfilled. completability.test.js:26 already loads the engine and uses identifiers.idOf, so no new dependency is introduced into a pure-JSON tier.
- `TEST-13` (implement, **still-satisfied**): State-aware reachability over generated fulfilments is the same completability.test.js 'completes the journey from every enumerated state' assertion, unchanged. The helper still emits the identical 162-state cross product; only the private idOf lookup mechanism changes and it produces the same ids. No test coverage is lost or relocated in a way that weakens the property.
- `TEST-14` (implement, **still-satisfied**): Dead-mandate detection is completability.test.js 'detects dead mandates: every authored mandate fires somewhere', which runs over enumerateStates() output. Since the enumeration is unchanged (same ids for hadClaims/claimType/claimAmount/voluntaryExcess/addons), the dead-mandate check still fires against the same state space. The engine's duplicate-name guard the new idOf adds only strengthens correctness; loadJourneyModel already runs green in this file so no new throw is introduced.

**Skeptic — strongest objection.** The change edits tests/helpers/enumerate-states.js — the state-space generator for the completability tier, which is on the explicit paradigm-load-bearing invariant-tiers protected list. Beyond that categorical rule, it collapses a real (if partial) oracle-independence seam: the completability tier currently builds its universe of starting fulfilment states from a raw JSON read plus a hand-rolled name→id map, independent of the engine identifier layer that the tier's own assertions (runToFixedPoint/unfulfilledMandatory/journeyState) exercise. Swapping to loadJourneyModel().identifiers.idOf couples state-space construction to the same machinery under test, so an engine id-resolution bug would corrupt both the generated states and the assertions identically instead of being caught. Whether that is unearned duplication to delete or a deliberate portable-data-model demonstration is a judgement call for a human.

---

## i18n/ is a one-file module hidden behind a pass-through barrel

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** i18n/index.js, i18n/resolve.js, i18n/resolve.test.js

**Current shape.** i18n/index.js:1-6 is a 6-line pure re-export of the folder's only source file, i18n/resolve.js (42 lines) — every consumer (validation/field-errors.js:1, contract/view.js:3, contract/submit.js:2) imports from '../i18n/index.js', so the barrel adds a hop over exactly one sibling.

**Proposed shape.** Move resolve.js's content into i18n/index.js (createResolver, resolveMessage, resolveReason plus the messages.en.json load) and delete resolve.js; rename resolve.test.js to index.test.js with its import updated. All external import paths ('../i18n/index.js') stay byte-identical, the folder-module seam survives, and the module is one file shorter with no indirection.

**Rationale.** The barrel convention pays when a folder has multiple internals to hide; with a single source file the barrel is pure ceremony — merging keeps the seam (the import path) while deleting the indirection.

**Obligations affected.**

- `OPEN1-10` (implement, **still-satisfied**): 'Resolve reason codes via the frontend i18n mechanism' — the only obligation whose matrix handled_by names i18n/index.js explicitly (alongside resolve.js). Merging resolve.js into index.js keeps the i18n resolution mechanism intact and keeps the public entry at i18n/index.js; consumers (validation/field-errors.js) still import resolveMessage from '../i18n/index.js' byte-for-byte. Primary test model/messages.test.js is on an untouched file. Behaviour unchanged.
- `OPEN1-13` (implement, **still-satisfied**): 'Keep the evaluator locale-agnostic' — also lists i18n/index.js in handled_by. Locale resolution stays downstream in the i18n folder-module; collapsing the barrel into index.js does not reintroduce inline literals into the engine or change the seam (import path preserved). Test model/messages.test.js untouched.
- `EVAL-13` (implement, **still-satisfied**): 'Shape reasons as coded records / dotted codes' — resolver participates via i18n/resolve.js. createResolver/resolveReason logic moves verbatim into index.js; dotted-code resolution and throw-on-unknown preserved. Matrix test engine/reasons.test.js is on an untouched file, so its coverage is unaffected.
- `EVAL-14` (implement, **still-satisfied**): 'Keep reason explanation developer-facing only' — resolver never surfaces explanation; that logic (resolve reads only the template + values) is relocated unchanged. Test model/messages.test.js untouched.
- `EVAL-15` (implement, **still-satisfied**): 'Carry interpolation values on reasons' — {token} interpolation in createResolver (line 27-33) moves verbatim into index.js. resolveReason still passes reason.values. Test model/messages.test.js untouched.
- `OPEN1-8` (implement, **still-satisfied**): 'Return reasons as coded records' — the coded-record shape is produced by engine/reasons.js + evaluate.js; i18n/resolve.js only resolves them. Relocating the resolver preserves that. Test model/messages.test.js untouched.
- `OPEN1-9` (implement, **still-satisfied**): 'Dot-separated naming convention' — resolver looks codes up in messages.en.json regardless of file layout; the messages.en.json load (resolve.js line 14-16) moves verbatim into index.js. Test model/messages.test.js untouched.
- `OPEN1-11` (implement, **still-satisfied**): 'Carry a developer-facing explanation' — resolver ignores explanation by design; relocation does not change that. Test model/messages.test.js untouched.
- `OPEN1-12` (implement, **still-satisfied**): 'Carry interpolation values with each reason' — same interpolation path as EVAL-15; also handled by validation/field-errors.js which keeps importing resolveMessage from '../i18n/index.js'. Test model/messages.test.js untouched.
- `OPEN1-14` (implement, **still-satisfied**): 'Leave translated-string location to the frontend' — translated copy stays in model/messages.en.json (untouched); the resolver's readFileSync of that path moves verbatim into index.js. Test model/messages.test.js untouched.
- `OPEN1-X1_1` (implement, **still-satisfied**): 'Welsh support is a statutory constraint on the reason surface' — createResolver stays parameterised over any catalogue (comment 'fixtures, future Welsh'), so a Welsh sibling file remains a drop-in; merging into index.js does not remove that seam. Test model/messages.test.js untouched.

**Skeptic — strongest objection.** The candidate's load-bearing premise — "with a single source file the barrel is pure ceremony" — is factually refuted by the spike itself. store/ is a SECOND single-source-file folder-module (store/index.js, 16 lines, is a re-export over exactly one internal source file, store/journey-repository.js). The spike has precisely two single-source folder-modules, i18n and store, and BOTH deliberately keep a separate re-export barrel. That is a demonstrated uniform paradigm: every one of the 15 folder-modules presents index.js as a re-export barrel separate from its internals, regardless of internal count. Collapsing only i18n makes it the sole folder-module whose public surface lives inside its implementation file, leaving the spike self-inconsistent (store keeps the split, i18n drops it) and degrading the uniform-folder-seam demonstration the barrels exist to show (README frames the barrel as interrogation Level 1; the i18n barrel docstring positions index.js as the documentation surface). So the change survives only by weakening a paradigm-visible seam and by contradicting the author's revealed preference in store/.

---

## journeyState re-implements the Group roll-up truth table inline — reuse rollUpChildStatuses

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** low | **Obligation cross-check:** pass
- **Files touched:** flow-eval/journey-state.js

**Current shape.** flow-eval/journey-state.js:38-52 filters NOT_APPLICABLE section statuses, then hand-codes: all-FULFILLED -> FULFILLED, some FULFILLED-or-IN_PROGRESS -> IN_PROGRESS, else NOT_STARTED, empty -> NOT_STARTED. flow-eval/container-status.js:33-48 (rollUpChildStatuses, the seven-row truth table) computes the identical result for every non-empty input — verified case-by-case: [F]->F, [NS]->NS, [IP]->IP, [F,NS]->IP, [NS,IP]->IP, [F,IP]->IP, [F,IP,NS]->IP under both — diverging only on empty/all-NA (NOT_APPLICABLE vs NOT_STARTED, because JOURNEY_STATES has no NA state).

**Proposed shape.** journeyState maps flow.sections through containerStatus, calls rollUpChildStatuses on the results, and maps a NOT_APPLICABLE outcome to NOT_STARTED (one ternary with a comment that a Journey has no Not Applicable state). The submitted short-circuit (journey-state.js:34-37) and the provisional NAV-33/34 doc block stay untouched; all existing journey-state.test.js cases pass unchanged since behaviour is identical.

**Rationale.** Two code paths encode the same seven-row truth table the DOC defines once (obligations.md:1226-1234); the journey-level copy can drift from the Group-level one without any test noticing, and NAV-34's 'journey state counts Section statuses' obligation is exactly 'roll the Sections up', so reuse states the design more directly than the inline re-derivation.

**Obligations affected.**

- `NAV-33` (implement, **still-satisfied**): Journey Not Started (obligations.md:1693) still falls out identically: pure-NS applicable sections -> rollUp NS, and the all-NA/empty case -> rollUp NOT_APPLICABLE mapped to NOT_STARTED by the proposed ternary, matching the current applicable.length===0 branch. The provisional NAV-33/34 doc block and its dedicated test (journey-state.test.js:119, fulfilment no applicable Section presents) are untouched and still pass since sections are still counted via containerStatus.
- `NAV-34` (implement, **still-satisfied**): In Progress when 'some Sections have In Progress / Fulfilled status' (obligations.md:1694): rollUpChildStatuses returns IP for any IP or F-mixed-with-NS set, identical to the current hand-coded some(F||IP) branch for every non-all-F input; the all-F case correctly yields Fulfilled under both. This is the obligation the candidate says reuse states more directly — behaviour unchanged, test 'is In Progress once some Section is In Progress or Fulfilled' still pins it.
- `NAV-35` (implement, **still-satisfied**): Journey Fulfilled iff all top-level Sections Fulfilled (obligations.md:1695-1696): rollUp all-F -> FULFILLED with NA filtered internally, same as current every(FULFILLED) over the NA-filtered list. The CYA Submit gate itself lives in contract/status.js which is not touched; test 'is Fulfilled iff every applicable Section is Fulfilled, NA filtered' unchanged.
- `STATUS-13` (implement, **still-satisfied**): The Journey Fulfilled rule (obligations.md:1472) is the all-F row of the shared truth table; rollUpChildStatuses produces FULFILLED exactly when every applicable child is FULFILLED, so the derivation is unchanged and now literally shares the Group-level rule the DOC states once.
- `DEF-X1_2` (implement, **still-satisfied**): The Journey-level derivation from top-level Sections (obligations.md:95-99/1472) still happens in journeyState: sections are still mapped through containerStatus and rolled up; only the roll-up implementation is shared rather than inlined. journey-state.test.js remains the covering test.
- `DEF-30` (implement, **still-satisfied**): Submit-enabled-only-when-Fulfilled is enforced in contract/status.js and e2e early-cya-access.spec.js, neither touched; journeyState's Fulfilled output that feeds the gate is behaviourally identical under the proposed shape (also matches PLAN.md ruling: hard gate at CYA POST only, unchanged).
- `STATUS-5` (implement, **still-satisfied**): The four-state vocabulary applied to the whole Journey (obligations.md:1431-1432, 1691-1707): JOURNEY_STATES and SUBMITTED constants are untouched; the proposed NA->NOT_STARTED mapping exists precisely because JOURNEY_STATES has no NOT_APPLICABLE, keeping the journey vocabulary exactly the DOC's four lifecycle states.
- `EVAL-28` (implement, **still-satisfied**): journeyState(flow, evaluation, {submitted}) keeps its signature and the Submitted short-circuit at journey-state.js:34-37 is explicitly untouched; the caller-carried submitted flag semantics (obligations.md:468-470, 1541-1543) are unchanged. Tests 'is Submitted whenever the stored flag says so' and the lifecycle pin remain valid.
- `EVAL-29` (implement, **still-satisfied**): The common JourneyEvaluator shape (obligations.md:472-474: small pure function over Flow subtree + obligationState returning one answer) is preserved — journeyState stays a small pure function and reusing a sibling flow-eval primitive (it already imports containerStatus from container-status.js) fits the 'collection of pure primitives' architecture (obligations.md:450-454); primary test engine/evaluate.test.js is untouched.

**Skeptic — strongest objection.** The change is behaviour-identical (I verified every case and all tests, incl. the line-119 NAV-33/34 pin, pass), so no obligation or test breaks — but it collapses a paradigm-load-bearing vocabulary seam and relocates the internals of a flagged provisional settlement, which is a design-owner's call the Rulings don't settle.

---

## Delete routes/page.js overlayField — project unsaved payload values through the contract's existing candidate-value machinery instead

- **Verdict:** `needs-human-ruling` | **Effort:** M | **Risk:** medium | **Obligation cross-check:** pass
- **Files touched:** routes/page.js, contract/view.js, contract/mutation.js

**Current shape.** routes/page.js:41-78 (`scalarOf` + `overlayField`, 38 lines) is a per-field-type switch (date parts, radios/checkboxes with reveals, select, text) that mutates the already-built FieldViewItem args to re-render typed-but-unsaved values on a blocked save (postHandler, page.js:101-106). This re-implements the value→args projection that lib/fields/input-views.js:14 and lib/fields/choice-views.js:26,55,73 already own (value/checked/selected per type), and re-implements payload decoding that contract/mutation.js already owns as `candidateValue(slot, payload)` (mutation.js:61-64). The file's own doc-comment (page.js:22) claims 'this file is plumbing', which overlayField contradicts.

**Proposed shape.** Extend `pageViewModel(pageId, evaluation, fieldErrors)` in contract/view.js with an optional fourth `payload` argument: when present, slot values are overridden with `candidateValue(slot, payload)` from contract/mutation.js BEFORE the lib/fields builders project them — so the existing single projection path renders drafts. routes/page.js postHandler becomes `pageViewModel(page.id, evaluation, check.fieldErrors, payload)` and overlayField/scalarOf are deleted. No new barrel export (the 21-export pin in contract/index.js is untouched — a signature extension only). Behaviour stays pinned by page.test.js:43-58 ('re-renders the typed (unsaved) values', 'preserving what was typed').

**Rationale.** Three places currently know how to map a value onto a govuk field view (lib/fields builders, contract candidateState, routes overlayField); the routes copy is the only one living outside the model/contract seam and must be kept in sync by hand whenever a field type is added (a new registry type today silently falls through overlayField's final text-input branch). Folding it into the contract strengthens the paradigm's 'thin routes over the contract barrel' claim rather than weakening it. Medium risk only because reveal-field and date-part edge cases need the candidateValue path to match exactly — the four blocked-save route tests are the safety net.

**Obligations affected.**

- `SHAPE-2` (implement, **still-satisfied**): 'Render user-facing obligations via the journey layer' (catalogue lines 136-137). handled_by already lists BOTH contract/view.js and routes/page.js. The proposed shape does not remove the projection — it relocates the value→govuk-widget projection for drafts from routes/overlayField into contract/view.js pageViewModel, which then flows through the SAME lib/fields builders (input-views/choice-views/block-views, untouched). candidateValue returns builder-shaped values (date→{day,month,year} for dateView; multi-select→array for checkboxesView; scalar for radios/select/text), so the rendered output is unchanged. Reveal children are their own revealedBy slots in the map, so they receive candidateValue too — replacing overlayField's manual recursion. Model/flow.test.js alignment plus page.test.js:43-58 stay green.
- `FLOW-8` (implement, **still-satisfied**): 'Re-render blocked page with GDS error summary + per-field errors' (catalogue lines 1077-1078, obligations.md quote 'The page re-renders with a GDS error summary + per-field errors'). This obligation is on templates/page.njk, not a touched file; the error-summary + fieldErrors wiring in postHandler (checkSave → pageViewModel(...,fieldErrors)) is untouched. Only the typed-value overlay (a parity nicety, not itself a catalogue obligation) moves into the contract. Blocked-save round trip preserved.
- `EVAL-31` (implement, **still-satisfied**): 'Render user-facing in-scope obligations and await input' (catalogue lines 362-363). Rendering path (expandSlots→userFacing→fieldSlot→slotViews) is intact; the change only pre-overrides slot.value with candidateValue before the identical projection. GET handler still calls the 3-arg form (no payload) so prefill of stored answers is unchanged.
- `ARCH-32` (implement, **still-satisfied**): Thin-routes-over-contract seam. The change STRENGTHENS this: it deletes 38 lines of value→field projection that lived route-side (the only copy outside the model/contract seam, contradicting page.js:22's own 'this file is plumbing' doc-comment) and folds it into the contract barrel. No new barrel export; pageViewModel gains an optional 4th param only (backward-compatible — tests/helpers/render-page.js:57 and contract/view.test.js keep their 3-arg calls; the alignment-walker invariant tier is undisturbed).

**Skeptic — strongest objection.** The simplification is behaviourally sound and breaks no test, Ruling, or E2E spec, but it is not free: it pushes payload-decoding into contract/view.js's pageViewModel, falsifying that module's documented "[view] — evaluation in, renderable view-models out" invariant and changing the DESIGN-DECISION.md-pinned contract signature pageViewModel(pageId, evaluation, errors?). That strengthens the "thin routes over the contract" paradigm claim only by weakening the deliberate view/mutation purity split in the contract barrel — a paradigm-demonstration seam. Whether folding typed-but-unsaved redisplay into the pure view projector (vs keeping it as route plumbing that bridges a rejected POST's raw payload) is the right paradigm call is a judgement the Rulings do not settle and that a human should sign off. A secondary UI-unreachable divergence (partial-date payload nulling stored parts via decodeDateParts) rides along.

---

## Dynamic {% extends layout %} seam is never varied — two extend mechanisms for one layout

- **Verdict:** `needs-human-ruling` | **Effort:** M | **Risk:** medium | **Obligation cross-check:** pass
- **Files touched:** templates/page.njk, templates/check-your-answers.njk, templates/confirmation.njk, templates/claims-add.njk, templates/claims-list.njk, templates/quote-summary.njk, templates/test-helpers.js, journey/config.js, routes/page.js, routes/endings/check-your-answers.js, routes/endings/quote-summary.js, routes/endings/confirmation.js, routes/claims/handlers.js, tests/helpers/render-page.js

**Current shape.** Six templates use a context-variable parent — page.njk:7, check-your-answers.njk:8, confirmation.njk:6, claims-add.njk:6, claims-list.njk:6, quote-summary.njk:6 all say `{% extends layout %}` — while hub.njk:4 and start.njk:3 hard-code `{% extends "standalone/obligations-standalone-spike/templates/layout.njk" %}`. The variable is always the same value: journey/config.js:14 exports LAYOUT = `${TEMPLATES}/layout.njk`, threaded as `layout: LAYOUT` through routes/page.js:81, routes/endings/check-your-answers.js:31, routes/endings/quote-summary.js:57, routes/endings/confirmation.js:30, routes/claims/handlers.js:28+53, injected by templates/test-helpers.js:31 into every template test, and repeated in tests/helpers/render-page.js:60+87. Nothing anywhere passes a different layout.

**Proposed shape.** Hard-code the extends path in the six dynamic templates (matching what hub.njk and start.njk already do), delete the `layout:` key from the 6 route call sites and 2 tests/helpers/render-page.js sites, drop the injection at test-helpers.js:31, and remove the LAYOUT export from journey/config.js:14. One mechanism, zero threading. CAVEAT for the human ruling: journey/config.js:1-8 and DESIGN-DECISION.md item 20 state the shell mirrors spike-a so route code diffs line-for-line across paradigms, and spike-a has the identical dynamic-layout split — so either accept losing those diff lines, apply the same simplification to spike-a in a separate pass, or reject this candidate as parity-load-bearing.

**Rationale.** A config seam with exactly one value ever supplied is over-generality: it costs a config export, six route context keys, a test-helper injection and two walker-helper keys, and it buys nothing inside this spike — half the templates already bypass it, proving the layer works fine without the indirection. No obligation id on layout.njk or the pages (ARCH-5, TOOL-2/10/11) requires a swappable layout. The only thing it earns is line-for-line route diffability against spike-a (documented in journey/config.js:2-4), which is why this is ranked last and flagged for a human ruling rather than marked safe.

**Obligations affected.**

- `ARCH-5` (implement, **still-satisfied**): obligations.md 888-891: presentational pages and UI affordances (incl. hub/confirmation/layout furniture) live in the Flow layer. layout.njk is untouched and still Flow-layer furniture; hard-coding the extends path (identical string to LAYOUT) changes nothing about where the layout lives or what it renders. Handled_by templates/layout.njk + confirmation/quote-summary/CYA njk — all render the same markup.
- `TEST-2` (implement, **still-satisfied**): obligations.md 1455: model↔HTML alignment test renders the real templates. render-page.js:58-63/86-92 currently passes layout: LAYOUT; under the proposal the templates hard-code the same layout path so the FileSystemLoader (test-helpers.js:19-25, roots = govuk dist + prototypes) still resolves it. Rendered markup is byte-identical, so the alignment walker still sees exact app markup — coverage preserved.
- `TEST-7` (implement, **still-satisfied**): obligations.md 1466-1467: one walker per page over Flow+HTML. Same inputs, same rendered HTML after the layout: injection is dropped — the per-page walker is unaffected because the parent template still extends the same layout via a hard-coded path.
- `TEST-12` (implement, **still-satisfied**): obligations.md 1477-1479: hard-mandate a11y/required signalling asserted against rendered HTML. That markup comes from page.njk's journeyContent/field macros, not from how the parent layout is referenced; hard-coding extends leaves the required-field wiring identical.

**Skeptic — strongest objection.** The `{% extends layout %}` seam is not unearned over-generality invented by this one-journey spike — it is a deliberate, verbatim mirror of spike-a. I confirmed spike-a carries the byte-identical fingerprint: hub.njk/start.njk hard-code the literal layout path while confirmation/check-your-answers/quote-summary/claims-list/claims-add (and section-page/addon-step/addons-select) all use `{% extends layout %}`, with spike-a/journey/config.js:13 exporting LAYOUT threaded through its routes. DESIGN-DECISION.md names this as load-bearing twice: module-map item 20 ("Mirrors spike-a's shell so route code diffs line-for-line across paradigms") and graft-12 ("so reviewers can diff route and shell code line-for-line across all five spikes. Comparability is the point of the exercise"). Applied as written (obligations-spike files only), the candidate makes this spike's routes and shell diverge from spike-a's and the other spikes, degrading exactly the cross-spike diffability that the design doc calls the point of the exercise — and it edits tests/helpers/render-page.js, which is the TEST-2/7/12 alignment-walker plumbing (test-tier machinery). The candidate's own "half the templates already bypass it, so the indirection buys nothing" argument is self-defeating: that hub/start-hardcode-vs-dynamic asymmetry is itself copied from spike-a, so the apparent redundancy is the mirror.

---

## Remove blocksSave() — a never-called second implementation of the page-save gate

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** medium | **Obligation cross-check:** pass
- **Files touched:** engine/mandates.js, engine/mandates.test.js, engine/index.js, engine/index.test.js, coverage-matrix.json, PLAN.md

**Current shape.** engine/mandates.js:68-74 exports blocksSave(pageMandate, obligationEntry), which wraps composeMandate and decides the save gate off evaluation-state fulfilled-ness. Zero production callers anywhere in the spike or e2e (verified by grep); the real save gate is validation/save-check.js:48-71, which independently re-implements the decision at line 57 (`(slot.mandate ?? 'soft') === 'hard'` on the payload-merged blank value) with deliberately different semantics (blankness of THIS submit vs stored fulfilled-ness). Only engine/mandates.test.js:46-68 and the index.test.js:11 surface list pin it.

**Proposed shape.** Delete blocksSave from mandates.js, its barrel entry (engine/index.js:17), its describe block (mandates.test.js:46-68) and the index.test.js surface line. Keep MANDATE_COMPOSITION + composeMandate (the doc's four-row table, obligations.md:1047-1099, stays pinned verbatim by mandates.test.js:12-43 — its blocksSave/blocksCompletion columns still express the gate as data). Repoint coverage-matrix.json ARCH-35/FLOW-7 `test` fields from engine/mandates.test.js to the validation save-check tests that actually exercise the gate, and trim the blocksSave mention from PLAN.md:228.

**Rationale.** Two code paths claim the same job and disagree subtly: blocksSave gates on evaluation.fulfilled while the live gate (save-check.js) gates on the payload-merged candidate value. An extender following EXTENDING.md could call blocksSave believing it is the gate and get wrong behaviour for values typed in the current submit. The composition-table concept the DOC requires survives intact as data + composeMandate; only the misleading unused wrapper goes. Medium risk because ARCH-35/FLOW-7 matrix test pointers need an honest repoint.

**Obligations affected.**

- `ARCH-35` (implement, **still-satisfied**): Hard-mandate enforcement at Save and continue lives in validation/save-check.js (blank hard + in-scope blocks; re-render via field-errors/templates) and stays tested by validation/save-check.test.js ('blocks a blank hard mandate') plus e2e mandatory-fields.spec.js and mandate-composition.spec.js ('fullName blocks the save'). blocksSave was an uncalled duplicate; the candidate's proposed matrix test repoint keeps the pointer honest.
- `STATUS-29` (implement, **still-satisfied**): Page-POST hard-mandate validation (doc 1530) is implemented by save-check.js + validation/index.js and tested by save-check.test.js + e2e mandatory-fields.spec.js, all untouched. Caveat: its matrix test field points at engine/mandates.test.js whose deleted describe was literally named 'the page-POST gate'; the surviving composition-table test still pins blocksSave-as-data so the pointer isn't dead, but STATUS-29 deserves the same repoint the candidate gives ARCH-35 (non-blocking honesty fix).
- `ARCH-36` (implement, **still-satisfied**): Soft-passes-save half is duplicated by save-check.test.js 'lets every page-soft blank save freely' and e2e; stays-on-to-do half is unfulfilledMandatory, which is kept along with its describe (mandates.test.js:70-116); the soft×mandatory row (blocksSave:false, blocksCompletion:true) stays pinned by the composition-table test.
- `ARCH-37` (implement, **still-satisfied**): Soft-default pinning survives twice: composeMandate() default test (mandates.test.js:31-37, kept) and save-check.js:57 `(slot.mandate ?? 'soft')` exercised by the un-mandated email slot in save-check.test.js. Only the redundant blocksSave(undefined,...) pin goes.
- `DEF-X1_1` (implement, **still-satisfied**): Hard/soft per-page mandate support (doc 1312-1325) remains expressed by the kept MANDATE_COMPOSITION table + composeMandate (its matrix test, mandates.test.js, still pins them at lines 12-43) and enforced/tested at runtime by save-check.js + its tests + e2e.
- `FLOW-7` (implement, **still-satisfied**): Block-save-on-unfulfilled-hard (doc 1341-1343) is implemented by save-check.js (in its handled_by) and its test field is already model/flow.test.js — untouched. Candidate's claim that FLOW-7's test field points at engine/mandates.test.js is factually wrong (it doesn't), so no repoint is needed there; harmless.
- `ARCH-12` (reduce, **still-satisfied**): index.test.js still pins the single evaluator/orchestrator barrel surface (one line removed from the sorted-keys expectation) and the end-to-end barrel-only test is untouched; one-set-per-language cardinality unchanged.

**Skeptic — strongest objection.** The change edits two ground-truth honesty artifacts (coverage-matrix.json, PLAN.md) and under-scopes its own repoint: it repoints ARCH-35 but leaves STATUS-29 ("Validate hard mandates on Page POST") pointing at engine/mandates.test.js, whose only page-POST test is the very describe block being deleted (`blocksSave (the page-POST gate)`, mandates.test.js:46-68). ARCH-36 and ARCH-37 sit in the same position. The candidate's own cross-check concedes STATUS-29 "deserves the same repoint" but self-classifies it "non-blocking" — a judgment call that, for a spike whose entire value proposition is a trustworthy coverage matrix, must not be made unilaterally. Separately, blocksSave is not merely a dead duplicate: it is the engine barrel's only primitive that APPLIES the composition table to a live EvaluationResult entry at engine altitude (pure, payload-free), while save-check.js is the validation-altitude implementation coupled to the HTTP payload. That two-altitude expression is a plausible deliberate paradigm seam of exactly the kind the brief flags (like the two-evaluator split) — and it is surface-pinned by the engine index.test.js drift test. Whether the fulfilled-vs-candidate divergence is a footgun to delete or a legitimate post-write interrogation to keep is a genuine two-way judgment about a paradigm-visible engine primitive.

---

## claims-add.njk is a byte-identical duplicate of page.njk — delete it and render the generic page template

- **Verdict:** `needs-human-ruling` | **Effort:** M | **Risk:** medium | **Obligation cross-check:** pass
- **Files touched:** templates/claims-add.njk, templates/claims-add.test.js, templates/page.test.js, templates/claims-list.test.js, model/flow.json, routes/claims/handlers.js, README.md

**Current shape.** templates/claims-add.njk:7-24 and templates/page.njk:8-25 are byte-identical (verified by diff — only the doc comment differs): same extends, same error-summary include, same h1/form/crumb/renderFields/govukButton body. It is wired in three places: model/flow.json:223 (addPage "template": "claims-add"), routes/claims/handlers.js:52 (h.view(`${TEMPLATES}/claims-add`, ...)), and its own mirror test templates/claims-add.test.js which duplicates page.test.js's renderPage harness (claims-add.test.js:28-38 vs page.test.js:15-24) and re-asserts the same error-summary-above-h1 and field-name behaviours page.test.js already pins.

**Proposed shape.** Delete templates/claims-add.njk. Change model/flow.json:223 to "template": "page" and routes/claims/handlers.js:52 to render `${TEMPLATES}/page` (or better, `${TEMPLATES}/${page.addPage.template}` so the route reads the model like tests/helpers/render-page.js:86 already does). Keep the graft-9 'bespoke frame' comment on the route, where the actual bypass lives. Fold the one assertion unique to claims-add.test.js (the pinned 'Add claim' button text from flow.json addPage copy) into claims-list.test.js or page.test.js and delete the rest as duplicates. tests/helpers/render-page.js renderClaimsAdd needs no change — it already renders page.addPage.template from the model. E2E is unaffected: prototypes/e2e/journey.js asserts rendered markup ('Add a claim' heading/button), which is byte-identical either way.

**Rationale.** The graft-9 point (README.md:131, DESIGN-DECISION.md:353) is that the claims add page lives outside the Container tree but its fields still arrive through the generic FieldViewItem projection. A duplicate template demonstrates nothing extra — the frame content is identical, so the bespoke-ness is entirely in the route and the model's addPage key. Rendering page.njk makes the demonstration stronger (only the route is bespoke; frame AND fields are generic) while deleting ~24 template lines plus a ~70-line mirror test. Risk is medium only because README's honesty ledger (row at line 131) names claims-add.njk and would need its wording updated; TOOL-14's reduce entry in the coverage matrix maps to the claims templates and should be re-pointed at claims-list.njk + the route.

**Obligations affected.**

- `ARCH-25` (implement, **still-satisfied**): Dynamic page membership via presentsForEach. The behaviour is the field projection (renderFields over fulfilment slots), which lives in partials/fields.njk + lib/fields, not the wrapper template. page.njk's block body is byte-identical to claims-add.njk, so rendering it preserves the projection. Test surface is model/flow.test.js plus alignment-walker.test.js:173-185 renderClaimsAdd, which renders `${page.addPage.template}.njk` from the model and asserts model-driven controls (claimType radios + claimAmount input) — output is identical under page.njk. Matrix handled_by should drop claims-add.njk (bookkeeping only).
- `ARCH-27` (implement, **still-satisfied**): presentsForEach works for any indexed source. Template-agnostic — enforced in flow-eval/presents.js and tested by flow-eval/presents.test.js, neither of which references the wrapper template file. Unchanged.
- `ARCH-X1_2` (implement, **still-satisfied**): Per-fulfilment hard mandate on presentsForEach. Enforced in flow-eval/presents.js + fields.njk error wiring; page.njk includes the same error-summary partial and renderFields, so error-summary-above-h1 and per-slot error binding render identically. Test is flow-eval/presents.test.js (template-name-agnostic). Unchanged.
- `TOOL-12` (reduce, **still-satisfied**): Reduce: no generator; hand-written template follows generator-shaped conventions (one input per presents entry, typed). page.njk + fields.njk still demonstrate exactly this projection. The reduction note (TOOL-28 reference impl) holds; only claims-add.njk drops off handled_by.
- `TOOL-14` (reduce, **still-satisfied**): Reduce: 'presentsForEach -> a template loop'. The actual loop is in claims-list.njk (rows over fulfilments), which is untouched and stays in handled_by; claims-add.njk is a single-fulfilment add form, not the loop. Candidate correctly repoints the matrix off claims-add.njk to claims-list.njk + route. Reduction demonstration intact.

**Skeptic — strongest objection.** The change overturns graft-9 — a SCORED design decision (DESIGN-DECISION.md:353, "Generic-renderer discipline") that the authors explicitly made to KEEP bespoke templates with honest bypass comments as a demonstration artifact, noting it "partially recover[s] the demonstration score." Deleting claims-add.njk and flipping flow.json:223 addPage.template 'claims-add'->'page' collapses the alignment-walker test's sole "bespoke page" exemplar (tests/alignment-walker.test.js:172-185) into the generic-page category at the MODEL level, erases the template's distinct seam comment (which teaches something the route comment cannot: a conceded bespoke frame whose fields are still generic), and forces edits to two human-owned self-description artifacts — the README honesty ledger (README.md:129-131 names claims-add.njk as one of exactly four bespoke bypasses) and the coverage matrix across five obligations (ARCH-25, ARCH-27, ARCH-X1_2, TOOL-12, TOOL-14, not just the TOOL-14 the candidate volunteers). Whether this strengthens ("only the route is bespoke; frame+fields generic — more honest") or weakens ("byte-identical duplicate overstated bespoke-ness" vs "erases the teaching seam") the paradigm demonstration is a judgement the PLAN.md Rulings do not settle.

---

## expandSlots copies three per-slot flags (inScope, engineStatus, fulfilled) that no production code reads

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** medium | **Obligation cross-check:** pass
- **Files touched:** flow-eval/presents.js, flow-eval/presents.test.js

**Current shape.** flow-eval/presents.js:48-58 slotOf copies inScope, engineStatus and fulfilled onto every slot. Grep over all consumers — contract/view.js:24-53 (uses obligationId, entry, name, fulfilmentId, value), contract/mutation.js:45-55 (obligationId, name, fulfilmentId, pageMandate, value), orchestrator/apply-answers/page-answers.js:22-30 (obligationId, name, fulfilmentId), validation/save-check.js (consumes mutation's re-mapped shape), tests/helpers/script-runner.js:69-80 (name, fulfilmentId) — shows the three flags are asserted only by flow-eval's own presents.test.js:121-142,165-170. Status logic reads scope/mandate/fulfilled-ness through presentedObligations (container-status.js:67-97), never through slots.

**Proposed shape.** slotOf returns { obligationId, name, fulfilmentId, pageMandate, value, entry } and presents.test.js drops the assertions on the removed flags (keeping the per-fulfilment value/order assertions, which pin real behaviour). If the team instead judges the verbatim-flags principle worth keeping as a documented seam for future renderers, the alternative is a one-line honesty-ledger entry — but as shipped the fields are unread output.

**Rationale.** No obligation mandates these slot fields (ARCH-24..29/SHAPE-30 pin expansion, ordering and the dynamically-empty case — none pin per-slot scope/status flags, confirmed in obligations-catalogue.json), and the status dimension deliberately flows through presentedObligations instead; carrying a second, unread copy of the evaluation's flags on every slot invites the two views to be confused for one another. Risk is medium only because it shrinks the portable slot interface presents.test.js currently pins as 'flags copied verbatim'.

**Obligations affected.**

- `DEF-X2_1` (implement, **still-satisfied**): Doc 113-114 requires flow-eval to use ObligationEvaluator output as an input, never recompute. Removing the three copied flags removes duplication, not layering — every remaining slot field (obligationId, name, fulfilmentId, value) still comes straight from the evaluation, nothing is recomputed. Primary test engine/evaluate.test.js untouched. The presents.js header comment ('flags copied verbatim') must be updated with the change.
- `SHAPE-30` (implement, **still-satisfied**): Fulfilment-set projection surfaces via the expansion loop over obligation.fulfilments (presents.js:73-80), untouched. presents.test.js keeps the per-fulfilment obligationId/fulfilmentId/value and dynamically-empty assertions that pin it.
- `SHAPE-X2_2` (implement, **still-satisfied**): The weakest case: its matrix test is presents.test.js and the dropped slot.fulfilled assertion (lines 165-170) is the closest per-item-state assertion there. But the per-item dimension is realised in the engine (per-fulfilment fulfilled, pinned at engine/evaluate.test.js:124-127 with mixed [true,false] states — the 'agreed flow-eval interface' per evaluate.js:14) and still flows to flow-eval via presentedObligations → obligation.fulfilments; enforcement per item happens in save-check via pageMandate + candidate-value blankness, both kept. Kept per-fulfilment value assertions ['north corner','',undefined,'3'] still demonstrate per-item variance through expansion. Recommend the coverage-matrix note for this id point additionally at engine/evaluate.test.js.
- `EVAL-30` (implement, **still-satisfied**): presents.test.js continues to drive expandSlots from hand-built ObligationEvaluator output (the allotment fixture) with no real engine run — the isolation the obligation demands (doc quote at §Conditional patterns region) is unchanged.
- `ARCH-26` (implement, **still-satisfied**): 'One virtual page-presentation per in-scope fulfilment' (doc 1271-1272) pins slot COUNT and identity, not slot flag payload; the loop and the kept fulfilmentId-mapping assertions ('hiveLocation/h1'...) preserve it exactly.
- `ARCH-27` (implement, **still-satisfied**): 'Same shape works for any indexed source' (doc 1272-1273) — expansion stays source-agnostic (fixture is a non-insurance journey); no flag participates in source handling.
- `ARCH-28` (implement, **still-satisfied**): Mixing presents + presentsForEach (doc 1275-1276) is pinned by the kept presents-first ordering test (presents.test.js:188-198), which asserts only obligationId order.
- `ARCH-X1_2` (implement, **still-satisfied**): 'mandate: hard // applies per-fulfilment' (doc 1266): the proposed shape KEEPS pageMandate on every expanded slot, and enforcement is in validation/save-check.js:57 via slot mandate + blank candidate value with out-of-scope skip read from the id-keyed evaluation map (save-check.js:51-52) — none of the three removed flags participate. Verified mutation.js:43-58 forwards only obligationId/name/fulfilmentId/type/constraints/inputName/mandate/value.

**Skeptic — strongest objection.** The three slot flags are the visible artifact of DEF-X2_1's paradigm claim — "flow-eval consumes ObligationEvaluator output and never recomputes scope/mandate/fulfilled-ness." presents.js's own header (lines 3-5) documents this in words ("per-slot flags are copied verbatim from the evaluation"), and presents.test.js's it-title is literally "expands presents entries to single slots, flags copied verbatim" (line 110), asserting engineStatus/inScope/fulfilled propagate untouched for mandatory/optional/out-of-scope obligations (lines 126-141) plus the per-fulfilment fulfilled=[true,false,false,true] tier (165-170). This is the ONLY unit-level demonstration on the flow-eval side that the two-evaluator split's consumer copies the engine's verdict verbatim rather than re-deriving it — and it is the SHAPE-X2_2 "value-dependent per-item mandate" obligation's PRIMARY matrix test. The candidate removes the fields, must edit the DEF-X2_1-anchored header comment, deletes that named assertion tier, and — by its own admission — "shrinks the portable slot interface presents.test.js currently pins," a slot contract the spike deliberately exposes as a forward seam for future renderers. In an ordinary codebase this is clean dead-output removal; in a spike whose entire purpose is to make the paradigm legible, the unread output IS the demonstration, and gutting a documented seam + a named test tier is a trade a human should sign off — which is precisely why the candidate itself offers "keep the fields as a documented seam + honesty-ledger entry" as the alternative.

---

## reconcileJourney is a dead entry point — no route, contract, or journey code ever calls it

- **Verdict:** `needs-human-ruling` | **Effort:** S | **Risk:** medium | **Obligation cross-check:** pass
- **Files touched:** orchestrator/index.js, orchestrator/index.test.js

**Current shape.** orchestrator/index.js:110-117 exports reconcileJourney (reconcile-on-load: re-run the pass over stored fulfilments and persist the amended set, citing obligations.md:690-720). Grep across the whole spike plus prototypes/e2e finds exactly one caller: its own test, orchestrator/index.test.js:134-151. The app's actual on-load drop surfacing is the read-only path in contract/status.js:120-141 (evaluate returns result.drops without persisting), and the prune obligations PERSIST-14..17 are pinned to engine/prune.js in coverage-matrix.json:1711-1735 — nothing in the matrix cites reconcileJourney.

**Proposed shape.** Delete reconcileJourney (orchestrator/index.js:110-117) and its test block (orchestrator/index.test.js:134-151, plus the import at line 8). Behaviour-neutral: nothing invokes it, drops are already surfaced read-only by contract evaluate, and every mutating path runs the identical pass via commit. Alternative if the team actually wants persisting reconcile-on-load: wire it into the journey GET path — but that is new scope; the simplification is deletion.

**Rationale.** A test-only public entry point is pure conceptual noise: it advertises a persistence-on-load capability the running app does not have, which is exactly what the README honesty ledger is supposed to prevent. Its coverage duty is already owned by engine/prune.js per the matrix. Medium risk only because it deletes a DOC-described capability (obligations.md:690-720) that a human may prefer to wire rather than drop.

**Obligations affected.**

- `DEF-4` (implement, **still-satisfied**): Orchestrator-wraps-engine-and-owns-I/O is realised by settle/commit plus the four route-called entry points (applyPageAnswers, addIndexedFulfilment, markIndexedCollectionReviewed, removeIndexedFulfilment), all retained; matrix test engine/scope/journey-rules.test.js untouched. reconcileJourney is just a fifth thin wrapper over the same commit.
- `EVAL-1` (implement, **still-satisfied**): The three-component shape (two pure evaluators + one side-effecting orchestrator) is unchanged; deleting one unused orchestrator export does not alter the component count or roles. Test engine/evaluate.test.js untouched.
- `EVAL-4` (implement, **still-satisfied**): System-handled obligations run via runToFixedPoint/system-handlers inside commit on every retained mutating path; reconcileJourney adds nothing to that mechanism. Test engine/evaluate.test.js untouched.
- `EVAL-19` (implement, **still-satisfied**): Closest obligation to the deletion. Tolerate-and-amend is honoured by prune running inside evaluateObligations (engine/evaluate.js:124) on every read (contract/status.js:120-141 surfaces drops) and by commit adopting+persisting the pruned set on every mutation. The deleted test block's assertions are duplicated by fixed-point.test.js:32-41 ('adopts the pruned set, surfacing drops') plus index.test.js's first test (repository.get equals saved post-pass state); matrix tests engine/prune.test.js and tests/rename-survival.test.js untouched.
- `SUBMIT-7` (implement, **still-satisfied**): Storage-layer freeze is enforced in store/journey-repository.js and pinned by index.test.js:153-164 ('a submitted journey rejects every mutating path') which survives the deletion, plus the post-submit-freeze e2e spec. PLAN.md Ruling 1 (freeze ON) unchanged.
- `ARCH-11` (implement, **still-satisfied**): Orchestrator remains the sole side-effecting wrapper — commit is still the single write->fixed-point->save funnel; removing an uncalled entry point narrows, not widens, the effect surface. Test engine/evaluate.test.js untouched.
- `PERSIST-18` (implement, **still-satisfied**): Not mapped to the touched files — matrix pins it (and PLAN.md:229 pins the whole reconcile-on-load family) to engine/prune.js, test engine/prune.test.js, both untouched. The 'orchestrator persists the amended set' half (obligations.md:345-346, 969-970) is genuinely exercised by commit on every mutating pass (fixed-point adopts the pruned set, repository.saveFulfilments persists it). The persist-on-pure-load variant was already unreachable in the running app (no route calls reconcileJourney), so deletion changes no honoured behaviour — it removes latent capability code only.

**Skeptic — strongest objection.** reconcileJourney (orchestrator/index.js:115-117) is the ONLY code in the spike that realizes the DOC's literal claim at obligations.md:345-347 — "The orchestrator persists the amended set — it becomes the new source of truth. On subsequent runs the amendment is idempotent" — as a LOAD-triggered orchestrator action. The candidate's "drops are already surfaced read-only by contract evaluate" defense is precisely NOT this obligation: contract/status.js:120-141 returns result.drops WITHOUT writing them back, so it never makes the amended set the stored source of truth — the stale fulfilment survives in the repository until the user next edits a page. The matrix pins PERSIST-18 ("Persist the amended set as new source of truth") to engine/prune.js, but prune.js is PURE and cannot persist; the genuine orchestrator-persists demonstration is split between commit() (mutation-triggered) and reconcileJourney (load-triggered). Deleting reconcileJourney therefore narrows the spike's demonstration of PERSIST-18/EVAL-19's orchestrator-persist-on-reload / idempotent-on-subsequent-run to mutation-only. In a paradigm-demonstration spike, dropping the sole in-code realization of a DOC obligation — versus wiring it into the journey GET path, which the candidate itself offers as the alternative — is a call the maintainer of the honesty ledger should make.
