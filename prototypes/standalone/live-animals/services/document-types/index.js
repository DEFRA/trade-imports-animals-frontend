import { DOCUMENT_TYPE_OPTIONS, ATTACHMENT_TYPE_OPTIONS } from './stub.js'

/** The accompanying-document types, in reference-data order — for select options and validation membership. */
export const documentTypes = () => DOCUMENT_TYPE_OPTIONS

/** The attachment file-format types — for select options and validation membership. */
export const attachmentTypes = () => ATTACHMENT_TYPE_OPTIONS
